# Counterparty Seed Pre-Triage — 2026-07-23

Pre-triage of the review/SPLIT/name-check rows in `per_entity/{sg_pte_ltd,au}/seed_counterparties.csv`.
367 rows triaged (240 SG + 127 AU) by two parallel judgment agents briefed with the locked decisions
(RMS Fleet = TRANSFER/5001, 6700 product-infra vs 6701 team-SaaS, IC entities never get expense defaults),
then merged with mechanical gates: every code ∈ `chart_of_accounts_v2.csv`, `rec_account_name` always the
COA name for its code, zero sentinels (`SPLIT`/`ENTITY-DEPENDENT`/`TRANSFER`) left in either seed.

## Outcome

| Verdict (`coa_flag`) | SG | AU | Meaning |
|---|---|---|---|
| `triaged` | 173 | 84 | Final concrete default code assigned this pass |
| `approved` | 190 | 191 | Was already correct via the approved bridge |
| `blank-ok` | 48 | 33 | Low-freq, unidentifiable — deliberately NO default; engine falls through to rules/RAG/human |
| `intercompany` | 4 | 1 | Related entity, type set to `intercompany` — IC machinery, never an expense default |
| `transfer-artifact` | 3 | 3 | Not a party (bank/transfer text) — drop at apply time |
| `SHORTLIST` | 12 | 6 | Genuine judgment call for Gaurav — see below |

## Shortlist — ALL 18 DECIDED by Gaurav (2026-07-23, one-by-one review)

| Item (freq) | Decision |
|---|---|
| Stripe Customer SG (566) | **ignored** — settlement machinery, never a counterparty |
| Technology (313) | **ignored** — QB payee artifact; real vendors (DocuSign/Anthropic/OpenAI…) live in bank memo text and match their own rows |
| DRIVE MATE MELBOURNE (266) | **ignored** — our own AU trading name in bank text |
| Card transaction (122) | **ignored** — per-line categorization by real merchant |
| Vigneshsuran So Dhevan (117) | **employee**, default 6000 |
| Cheng Chuan (92) | vendor, car repair → **5032** |
| Jay Balan (73) | **5062** On-Ground Team |
| Elite car (64) | **5032** Workshop |
| MUHAMAD HAZIQ BIN MUSTAFFA (47) | **5062** On-Ground Team |
| Tokio Marine (35) | **5035** Trip Premium |
| NA (deleted) (33) | **ignored** — placeholder; lines reviewed in replay |
| GS (31) | **ignored** — not a counterparty |
| Sarala D O Letchumenan (16) | **employee**, default 6000 |
| Maricon REviza (14) | **5063** — confirms the PH-individuals = remote support team pattern |
| Stripe customer AU (12) | **ignored** — consistent with SG |
| ATO (8) | keep as party (type government), **no default** — split per line |
| General Expense Payment (8) | **ignored** |
| Fin-Nor Holdings (2) | lender, **2400** Loans from Others |

`coa_flag` vocabulary in seeds: `approved` / `triaged` / `decided` (Gaurav's calls) / `blank-ok` (deliberately
no default) / `ignored` (NEVER becomes a counterparty — drop at apply) / `intercompany` / `transfer-artifact`.
Final gates: 0 invalid codes, 0 code/name mismatches, 0 shortlist rows left.

## Invariant established

`rec_account_code` + `rec_account_name` are always sourced together from `chart_of_accounts_v2.csv` —
a code implies its name; no mixed-source pairs. Sentinels live only at the bridge (label) level, never
in seeds.

## Addendum — Rules promotion + bridge audit (same day)

**Rules seed (`per_entity/*/seed_rules.csv`, via new `Tools/PromoteRules.py`):** SG = 61 rules + 3 transfer-rules
(the 593-hit Stripe settlement patterns, per the locked decision) · AU = 24 rules · dedup: 92 covered-by-counterparty,
35 already live QB rules, self/IC-name patterns dropped. Gates: every rule code ∈ COA v2, name paired, 0 violations.

**Bridge audit (126 June machine-approved rows, 2 parallel agents):** 119 OK · 2 WRONG fixed
(`Other Income` → SPLIT; `Intangible Assets:Brand:Brand - Amortization` → 1800 accum-amort, was P&L 7401) ·
2 resolved by the ENTITY-DEPENDENT convention (`Due From Australian Account`: sg→8000/ven→8021; `Due to Ventures`:
sg→8101/au→8111) · 3 UNSURE → Gaurav. Also approved 4 high-usage previously-unapproved labels within existing
conventions: `AI, Internet & Website Maintenance fees`→6700 · `Uncategorised Income`→SPLIT · `Operating
Expenses:General Expense`→SPLIT · `Other Income-Refund`→7001. Root cause of the 4 bad draft rules was a
PromoteRules precedence bug (unapproved labels leaked rec_code) — fixed; June wave was 94% clean.

**Counterparty seeds re-synced** through the corrected bridge: 43 SG + 7 AU rows updated (mostly 6104→6700).

## Addendum 2 — Gaurav's v2 review → clean counterparty model (v3)

Gaurav full-reviewed the SG seed (`seed_counterparties_v2.csv`, Remarks column): **112 Remove** (one-off
merchants, artifacts — "not every transaction becomes a counterparty"), 3 Employee, 2 renames, Abwin
Leasing → 5001. Principles locked: **counterparties = entities/people we have a relationship with**;
one-off transactions don't qualify; strip "(deleted)"; one row per vendor (variants merged into aliases).

**`seed_counterparties_v3.csv` (SG): 431 → 289 clean counterparties.** All remarks applied; zero
"(deleted)" names; variants merged (AWS, Facebook, Zapier incl. misspellings, Twilio, Cheng Chuan Motor
Services, Towlah…); 3 code conflicts resolved by decided>triaged>approved (CPF→2300, Sharetribe→6700,
Upwork→6700 — note the mined rule sends upwork lines to 5063, which wins at runtime anyway).

**Multi-entity decision (from the model):** `entity_id NULL = global` — vendors present in >1 entity load
as ONE global record (new `scope` column: 16 global candidates); people/local vendors stay entity-scoped.

**Next:** same treatment for AU (agent pass applying Gaurav's demonstrated remove-principle + his review),
then Ventures (18 rows), then corpus persistence.

## Addendum 3 — SG LOCKED (2026-07-23)

**Scope rule decided (Gaurav, after ERP-practice review): nature-based, per the master-data principle
"identity is global, relationship is per-entity."** Multinational orgs → `scope=global` (one record,
`entity_id NULL`); people, local businesses, statutory bodies, ambiguous → `scope=entity`.

**SG final: 289 counterparties (111 global / 178 entity), canonical file = `seed_counterparties.csv`**
(v2 = Gaurav's review record; v3 = identical copy of the locked seed). Canonical-name fix applied
(highest-signal variant wins: Twilio not "Twilio SGD", Typeform not "type form", Cursor AI, GoDaddy,
Cheng Chuan Motor Services, Towlah). Gates: 0 violations. SG rules re-promoted against the locked seed:
**50 rules + 3 transfer-rules** (83 candidates now covered by counterparty defaults).

**AU + Ventures: NOT yet locked** — awaiting the same review treatment.

## Addendum 4 — SG truly locked: 244 (Gaurav's second pass)

Second Gaurav review of the locked file (46 remarks, preserved in `seed_counterparties_review2_gaurav.csv`):
42 more removes (generic names, unknown one-off individuals, Stripe Customer/Technology/GS/Wise/Transfer/
OTHERS physically dropped) + **principle locked: ENTITIES ARE NOT COUNTERPARTIES** — all intercompany rows
removed (incl. Drive Lah Australia Pty Ltd); related entities are `finance_entities`, cross-entity flows work
on entity ids; entity-name bank text belongs to transfer/IC machinery (rules workstream, parked).
Fiverr → 6103 (was 2200); Grenader pres + Kumar removed; ANKISH → employee 6000.
**FINAL SG: 244 counterparties (204 vendors / 38 employees / 1 govt / 1 lender · 109 global / 135 entity),
0 gate violations.** Canonical: `per_entity/sg_pte_ltd/seed_counterparties.csv`.
