<!-- HUMAN-OWNED -->

# Status — finance-api

**Last updated:** 2026-08-02
**2026-08-02:** Revenue+cost re-architecture POSTED LIVE 100% (AUD-12/RA-10 cutover done, Gaurav confirm). Stripe Connect recon PARKED (openings sound; connect closings under-captured — see ▶ Stripe Connect PARKED). **New mega-workstream opened: §2.7 Vendor Payment Reconciliation** (per-vendor Statement of Account / true outstanding).
**Overall:** Multi-entity (SG + AU) double-entry accounting platform. The **Capture → Classify → Record** core is strong and green; the **last mile** — financial reports (P&L / Balance Sheet / Business-Line Margin), period close, consolidation — is the thin, mostly-unbuilt part. We're ~75% an ingestion engine, ~25% an accounting system. **Active workstream:** the **previous-years reconciliation (2019–2025)** — Stage-1 prep (S-1..S-5 counterparties/rules/corpus) DONE + applied to live (2026-07-24); **Stage-2 execution structure now LOCKED = POL-124 / §2.0c / `wip/HISTORY_RECON_PIPELINE.md`** (year-at-a-time, engine+economic-events, agent lanes, HTML scorecard, re-park, jurisdictional closes).

**Verified ground truth (2026-07-31):** `pytest tests/ --ignore=tests/stripe_sync` = **637 pass / 8 fail** — the 8 are ALL `test_economic_events` (view-map seed missing: "no view map for trip_revenue_accrual"); **proven pre-existing** (fail identically on HEAD~1, untouched by this session). This session's modules green: categorization **138/0**, invoice **21/0**. **Committed locally at 41eb1fe, 2026-07-31** (branch `finance-system-2026-07-31`, unpushed). **DB migrations applied through 050** (`alembic current` == head). Canonical docs: STATUS (state) · IDEAL_STATE (vision) · KNOWLEDGE (business facts).

**Session 2026-07-31 — invoice engine + counterparty quality (committed 41eb1fe, migrations→050):**
1. **Invoice review dashboard** — full action-audit trail (submit/approve/void/reject: who·when·why), NO direct draft→approved (must pass pending_approval), hard-leave-draft gates (COA+counterparty+amount+real invoice_date) + not-invoice soft-block (mig 046/047). FE (admincontrols): two-pane edit modal, Retool-tags column + sticky header, Retool-ID/duplicate filters, searchable COA combobox, zoom-scroll invoice preview, inline quick-add counterparty. **Code-complete + tsc-clean, NOT pixel-verified** (Interceptor unavailable this session).
2. **AP offset engine** (POL-75/77/78/79; mig 048-050) — invoice credit leg resolves purely from COA: employee-claims(6010-14/5062)→2303, super→2302, CPF→2300, income-tax→2305 (**new account**), else 2000; scoped payables-only (revenue/liability/equity NULL). Prepaid constant 1200→1300 fix (DQ-67).
3. **Counterparty matching** — invoice ingestion LLM-always over the WHOLE book, all types active+inactive, never fuzzy (POL-80); txn matching tiered L1 (exact name > name-substring > description-footer) + LLM-on-tie, first-match-wins removed (POL-81, DQ-69).
4. **Data cleanup** (DB, one-off scripts) — counterparty dedup 631→602 (Gaurav CSV 34 actions + 6 audit-found merges), alias scrub 1458→687 (raw statement lines stripped); L1 ambiguity 189→0 (DQ-65/66/68). Historical txn attributions NOT re-run (forward safeguard).
**Open from this session:** super double-count check (does payroll already accrue super?); optional re-enrich of historical txns with the tiered matcher; alias enrichment (~314 counterparties still no aliases); pixel-verify the invoice-dashboard FE once Interceptor is available.

**Session 2026-08-09 — cutover build across 3 repos (PRs raised, NOT merged, no prod migration):** branch `finance-cutover-build` on all three. **finance-api PR #25 · admin-bff PR #18 · admincontrols PR #58.** Verified on a local Postgres (`finance_aw2_local`); every migration is additive and PR-only (054/055/056 up+down proven in isolation). Pixel screenshots deferred (Interceptor test profile not configured on the build machine — flagged, not substituted).
1. **AW-2 COA config + Finance Settings** (mig 054) — `finance_coa_config`+audit; one finance-owned per-COA table both gates read (door=anchors, sign-off=approver routing); chart-driven grid (no CSV), audited upsert, per-row history; approver identity = dashboard role/user; new `finance.settings` module. Verified: endpoints+audit+gate helpers on a live local server.
2. **AW-3/4/6 approval chain** (mig 055) — `finance_invoice_metadata` + append-only `finance_invoice_approvals`; two-step chain service routes via `finance_coa_config`, posts only on final step via existing `approve`; scoped queue. **AW-1 (Slack rules) superseded by AW-2.** Verified: 2-step routing, scoped-queue approver shift, return→needs_fix, live queue over HTTP.
3. **Incident sub-ledger STEP-1..3** (mig 056) — `counterparty_id`+`role` on journal_line; `finance_incidents` (IMS shape) + `finance_incident_coa_map` (POL-114, keyed on IMS type_codes); incident→balanced counterparty-tagged JE projection; guest/host via `external_system='platform_user'` (POL-112). No ingestion (supervised). Verified: balanced 4-line damage JE, coa_map lookup, idempotent resolution.
4. **RS-4** — claims relocated into the Requests surface (My Claims sub-tab). RS-2 Track + EC-4 claims core pre-existed. **RS-3 Raise + reimburse remain parked** (payment-rail seam parked per Gaurav).
**Cutover-left:** browser pixel-verify all tabs once Interceptor is set up; access grants for `finance.settings` (Gaurav/DJ/Zilla) at cutover; seed `finance_coa_config` + `finance_incident_coa_map` in-app; migrations 054-056 apply to prod (supervised) at merge.

**Pointers:** ideal state + mental model → `IDEAL_STATE.md` (vision only; the *gap* + current state live here in STATUS) · deep architecture (archived) → `wip/SYSTEM_OVERVIEW.md` (§-refs below) · diagrams → `visuals/` (`ARCHITECTURE`, `CATEGORIZATION_ROUTES`, `JOURNAL_ENTRY_FLOWS`, `HR_PAYROLL_PROCESS_DIAGRAM`, `FINANCE_SYSTEM_STATE_VS_IDEAL`).

---

## ▶ Closing Path — LOCKED PLAN (Gaurav, 2026-07-25): H1-26 first, then historical, then future-forward

> **Phase-B "historical" is now SPECCED: POL-124 / §2.0c / `wip/HISTORY_RECON_PIPELINE.md` (2026-08-15).** H1-26 (Phase A) is closed; the mammoth is the active phase.

**Phase A — H1-2026 financial finalisation** (the current goal; "finalised" = TB tie-out + P&L + Balance Sheet + GST summary + consolidated SG+AU in USD, per D5). Steps run in order; 0–6 need nothing from Gaurav. Done: **A-2** (depreciation study → D1 hybrid) · **A-3** (RAG wired into Phase 4D; suite 596/0).

| Step | ID | What | Owner |
|------|----|------|-------|
| 0 | A-0 | ✅ **DONE 2026-07-25** — sandbox soft check (live tables copied read-only → sqlite; full-year 2023 DBS/Ventures statements; AI off). **140 txns imported clean · 11 auto-categorized (8%) · 0 counterparty-enriched · 129 Pending.** Success metric locked (Gaurav): **% auto-categorized per run**. Findings: **(1) OCBC PDF adapter parses 0 rows silently** (uppercase-month bug + stub descriptions) — every SG statement on hand is PDF, so this BLOCKS A-4; **(2) CBA adapter also 0 rows on real PDF**; (3) DBS adapter works (140/140, real text, correct signs); (4) **zero tests exist for any adapter**; (5) DBS statement text carries no payee names (refs only) → Ventures enrichment can't come from bank text (matches DQ; needs QB cross-ref at replay / AI+review). Tool: `.claude/skills/CorpusMining/Tools/SandboxRun.py`. **SG run (OCBC 1001 Nov-24, 728 txns, adapter fixed same-day): 697/728 = 96% auto-handled** — 518 transfer-claimed (Stripe settlements ✓), 141 party-default (⚠️ mostly hijacked by artifact party #522 "Debit Purchase"→6701, DQ-8 live), 38 rule hits (⚠️ rule #239 pattern contains our own entity name → misfires on cardholder lines, ENT-5), 31 → AI/review. **Fix queue from A-0: party #522 ✅ HARD-DELETED from live DB + row removed from seed (per POL-22 — it was WRONG, not dormant; Gaurav, 2026-07-25) · engine now enriches against INACTIVE (dormant) parties too (POL-22; test added, suite 597/0) · OPEN: party #540 "Girobiz Girocoll" (same artifact class → likely delete) · rule #239 pattern fix · re-review the 18 apply-time deactivations: split wrong→delete vs dormant→keep-inactive (they now match again under POL-22).** Bonus catch: tests were making REAL Anthropic calls (src/app loads .env) — conftest now strips the key; suite 596/0 in 21s | closed |
| 0b | A-0b | Re-run soft check with RAG-AI ON (grounded prompt, review-gated) → measure the true auto-categorization % incl. AI | me, cheap |
| 0d | A-13 | ✅ **BLANK SLATE APPLIED (Gaurav, 2026-07-25):** all 761 live bank txns reset → `IMPORTED`; categorization fields nulled; 189 bank-origin JEs (categorization_engine + counterparty_default) + 378 lines deleted; **88 Stripe JEs preserved**. Post-verify: 761/761 IMPORTED, 0 txn↔JE links, 0 orphan lines. Backups: `backups/20260725-075545-blank-slate/`. Tool: `Tools/ResetTransactionsToImported.py`. **Engine deliberately NOT re-run yet — awaiting Gaurav's go.** Follow-up 2 (Gaurav, 2026-07-25): the 88 preserved `stripe` JEs turned out to be DEV TEST OUTPUT (3 test months Jan-20/Jan-25/Dec-25, created Mar–May-26, unverified) → deleted too (+176 lines; backup `backups/20260725-082646-stripe-test-jes/`). **Ledger is now completely empty: 0 JEs, 0 lines; real Stripe entries regenerate at A-5 post-E2E-verification.**
| 0e | A-14 | ✅ **LIVE ENGINE RUN (Gaurav's go, 2026-07-25):** full ladder on all 761 staged txns (2 passes; AI on). **721/761 = 95% auto-handled**: 285 party-default · 51 transfer-awaiting-counterpart · 26 rule · 35 AI≥0.80 (DRAFT JE) · **324 AI low-conf → NEEDS_REVIEW (suggested code + reasoning stored)** · 40 Pending (no route). 466 enriched. Route recorded per txn (JE `source` + ai fields). Report: `wip/reconciliation/engine_run_20260725_report.csv`. **Pass-1 bugs found+fixed:** AI phase died on one bad chunk (now chunk-independent) · `_entity_short` returned 'Ltd' for all live legal names → IC pairs NEVER matched in production (fixed + test). **Next: review walk of the 324 + the 40** | done |
| 0f | A-15 | **Review walk of the queue — IN PROGRESS.** Buckets of the 324: 161 Wise "sent money to" (AU parties/individuals missing from universe; incl. 5 AU→SG intercompany sends = transfer-rule gap) · 117 card purchases (93 = Drive Mate own-platform tests) · 40 Wise top-ups (own money → needs transfer rule) · 2 FX conversions · 4 misc. **Done: Drive Mate 93 → NEW COA 6702 Technology-Platform Testing + 2 rules (live #283/284, POL-23) — re-run claimed all 93. Queue now 231 + 40 Pending.** **Progress 2026-07-25 (remarks round): queue 324 → 52.** 32 employee defaults (19×5063/13×6000) · URA substring bug fixed (L1 word-boundary <6 chars) · artifact parties deleted (#522 Debit Purchase, #267 Dom Drive lah; DOMINADOR GAMIT #525 → employee) · 35 CSV remarks applied: +6 AU parties (Altimobility 5030 · AFCA 5025 · Victorian Auto 5032 · Rohde 5032 · Danyal 5033 · Riezel 5063), Norman Chan→5001, U R Drive→5060, Bus/mrt rule→6400, McDonald's manual 6401 (first RECONCILED txn). **Live: 595 MATCHED · 86 transfers awaiting · 52 in review · 23 pending (incl. 6 parked IC sends).** Next calls: remaining 52 walk · cross-entity corridors (6 IC sends) · FX-conversion pair (2) · #239 pattern fix · #214 kept resolved-by-amount-split | 🔄 |
| 0g | A-16 | ✅ **TRANSFER RULES SORTED (Gaurav walk, 2026-07-25).** Model locked: transfer rule = source-scope (`bank_account_ids`) + one target; **one rule per corridor, one side only** — the other statement leg pairs in Phase 0 to the same JE (JE books at claim; AWAITING_MATCH = second statement line missing, a built-in completeness flag). Final book = **15 scoped+targeted rules** (OCBC 1001↔3001 ·1001/3001→Wise ·Stripe→3001 (ALL CSDB → Platform per Gaurav; #30 prio-1 outranks #214 which is KEPT pending the Fleet discussion) ·Stripe-payout ·DBS USD→SGD ·CBA→Wise AUD ·Drivemate deposit ·settlement-variant #270→Platform ·Wise top-up #285). Deactivated 7: #3 (patternless catch-all) ·#4/#206 (generic Wise) ·#33/#34 (ambiguous Wise-side legs) ·#71 (FX-corridor second side) ·#238 (bank FEES not transfer → new expense rule #286 'TRANSFER AGENT CHARGES'→6600). **Queued: cross-currency AWAITING_MATCH can't auto-pair (FX amounts differ) — engine gap; transfer rules should REQUIRE non-null scope (guard).** **Corridor walk (Gaurav, 2026-07-25): C1 OCBC 1001↔3001 CLOSED (exactly 4 rules; #31 dup deactivated) · C2 OCBC→Wise SGD CLOSED (#28/#32 own it; Wise legs pair via Phase 0) · C3 Stripe↔3001 CLOSED: same CSDB text SPLIT BY AMOUNT — ≥$1,000 → Platform (#30, awaits future payout feed) / <$1,000 → Connect (#214 repurposed, amount-conditioned, scoped [18]); engine change: no-feed targets (Stripe Connect) complete standalone-MATCHED (test added, suite 598/0); 4 fake test txns (JE#24 artifacts) deleted; #35 retained dormant for the Platform feed. NEW A-5 ITEM: build the Stripe Platform payout-line importer (few, chunky rows) + drop the sync's aggregate payout JE spec to avoid double-booking. Verified live: 21 CSDB → AWAITING(Platform), $45.10 → standalone-MATCHED(Connect). Remaining corridors: DBS USD↔SGD · CBA↔Wise AUD · Drivemate deposit · missing cross-entity corridors** | 🔄 | Follow-up (Gaurav, 2026-07-25): 136 system-written counterparty_names restored to raw import values from `original_csv_row` (0 diffs remain; 427/755 txns carry a source-provided name). Found: old OCBC **CSV** import mapped a DATE column into counterparty_name (adapter bug → A-12 scope) | done |
| 0c | A-12 | **Adapter hardening (BLOCKS step 8/A-4):** fix OCBC PDF (month case + real descriptions + balance-delta signs) · fix CBA PDF · add adapter tests with real-statement fixtures for all three | me |
| 1 | A-7 | Build P&L + Balance Sheet · consolidation (IC elimination + FX→USD) — the long pole | me |
| 2 | A-9 | Hard period close/lock (Q3): closed period = no JE create/edit/void; corrections = adjusting JE | me |
| 3 | A-10 | **Bank balance recon checkpoint** (spec enriched, Gaurav 2026-07-25): per account per period — OPENING balance (H1'26: 31-Dec-25 close per account, seeded once) + Σ **POSTED** JE lines on the account's COA code = computed close, must equal the STATEMENT's actual closing balance (adapters already capture running_balance). Only POSTED counts (ledger gate) — DRAFT/MATCHED are invisible to the checkpoint, so it passes only when the period is fully reconciled. Becomes the GATE for A-9 period lock. Build: opening-balance seed (account+as_of) · statement-close capture · recon-check endpoint+screen (computed vs actual, diff drill-down: unposted/missing lines) | me |
| 4 | A-11 | Review-gate + feedback wiring (Q4/Q5): ALL AI results → NEEDS_REVIEW · auto-append approvals to live corpus · record categorizing route per txn → deterministic-override exception report | me |
| 5 | A-6 | Depreciation execution per D1 (mirror Jan–Mar-26, compute Apr–Jun-26, seed schedules from QB carrying values) · GST return summary | me |
| 6 | A-5a | **IN PROGRESS 2026-07-25.** ✅ ClickHouse CONNECTED (H1-26 complete in views: SG trip revenue Jan 400k → Jun 499k, data through Jun-26) · ✅ migration 037 LIVE (first live migration; additive): `finance_economic_events` staging + `finance_je_templates` registry (DB table per Gaurav) · ✅ SG registry seeded (24 active + JE24 inactive — payouts → line importer). ✅ SG catalog COMPLETE (lineage audit `wip/reconciliation/CLICKHOUSE_SG_VIEW_LINEAGE.md`: 43 views, 2 source families; found: phantom view JE#4 silently dropped S$256k H1 incidentals · 9 unread views ~S$185k · fuel = 3 streams FLOW-11 · JE3 rewire). Registry: 33 rows / 32 active (8 Gaurav-approved additions incl. RMS 5001). ✅ AU catalog seeded (36 active; trip_distance trio = the real code='2', ~A$284k H1) · ✅ economic-event lane BUILT (stager/projector/4 endpoints/payout-line importer; migrations 037-039 live; sign policy `flip_on_negative`; suite 610/0) · ✅ **SG Jan-2026 STAGED LIVE: 29 events, 0 errors, 0 ledger effect — awaiting Gaurav review → project**. REMAINING: stage Feb–Jun SG + H1 AU · misc_charge_collected AU sign dissection (−A$62k, held) · MISMATCH re-project action (open period: void+repost; locked: delta adjusting JE) · Gaurav review of staged table → PROJECT · payout sync run (Wise-style, FE live) · commit FE/BFF repos. FE DONE 2026-07-25: Economic Events tab (stage/post) + Bank Accounts 'Sync payouts' (Wise pattern) — uncommitted in admincontrols/admin-bff | me |
| 7 | A-1 | **Jan–Jun-26 bank statements (all 4 accounts) + ClickHouse access + last-12-mo invoices** | **Gaurav** — unblocks 8–10 |
| 8 | A-4 | Import H1 (`IMPORTED`) → bulk-approve invoices → run ladder → review walks (in Claude Code, per Q5) | both |
| 9 | A-5b | E2E-verify Stripe views vs ClickHouse → run Jan–Jun-26 economic JEs. **2026-07-30 — Stripe PLATFORM recon built + RMS/deposit re-architecture (POL-67, DQ-47/48, FLOW-30):** cause of Jan platform gap = `host_transfers_cash` swept internal transfers to our own connect+deposit accounts, and `connect_internal_transfer` double-credited 1017 on trip-end accrual. FIX BUILT (views + templates + view_map): host_transfers now excludes our-own-account destinations (identified via `stripe_connected_accounts` rms/flexplus/caretaker email + deposit acct ids); `connect_internal_transfer` re-sourced cash-basis (transfer+reversal, flip both ways); new `deposit_internal_transfer` → 1021/1022. **PROVEN non-mutating** (`wip/stripe_recon_proof.py`): Jan platform residual collapses SG −6,495.50→**+415.45**, AU −12,121.79→**−5,505.30** — remainder = 4 unbooked edge cats (dispute_reversal/refund_failure/charge_failure/connect_collection). **2026-07-30 CLOSED (projection-proven, DQ-50):** three-account model locked (POL-68) — ① platform every lane ties to cent, residual SG −18.40 / AU −665.72 (diffuse charge-timing, immaterial); ② deposit = the 4 charge/refund views; ③ connect = `connect_internal` in + Rule-214 out. Fixes: disputes net+reversals, AU stripe_fees excl deposit fees, new 5011 `stripe_platform_adjustments` catch (refund_failure/connect_collection/charge_failure), AU views TZ→Sydney. Tools: `wip/stripe_recon_platform.py` (per-bucket), `wip/stripe_recon3.py` (partition). **POSTED 2026-07-30 (Gaurav go):** Jan re-staged+re-projected — deleted 8 stale JEs (host_transfers/connect_internal/disputes/stripe_fees ×2 entities), re-posted from new views + new `stripe_platform_adjustments`. Verified on PURE POSTED JEs: SG −18.40, AU −665.72 (diffuse charge-timing, immaterial); every other lane ties to the cent. Integrity: 0 unbalanced JEs, 102 events POSTED, 0 orphans. **Feb–Jun DONE 2026-07-30 (DQ-51):** all 5 months full-purge re-staged to current views + reconciled. Integrity: 0 unbalanced, 505 events POSTED, 0 orphans. Every Stripe CASH lane ties to the cent both markets (transfers/payouts/fees/disputes/refunds/connect/deposit/reserve/adjustments). Residual = CHG lane only (accrual revenue vs cash charges): SG immaterial (<$310/mo); AU −$419 to −$9,260/mo = revenue-view coverage+timing (task #10/#11), NOT a Stripe cash defect. H1 Stripe reconciliation COMPLETE. Payouts imported all months (Jan matched by Gaurav; Feb–Jun await matching). **2026-07-31 — FEE PRINCIPLE applied both markets (POL-69, DQ-53/54):** converted `disputes`/`stripe_reserve`/`stripe_platform_adjustments` views AU+SG from net→GROSS and reverted `stripe_fees_paid` to the clean single-line sum (Σ fee-col ex-reserve + Σ standalone fee rows) — same platform total as the prior exclusion-based approach, now principled (nothing booked net, so no silent double-count if a new net category appears). Full delete + re-stage + re-project H1 both markets (deleted 613 events/1226 lines/613 JEs; re-posted, 0 mismatches, 0 query-errors); payout lane (`stripe_payout_import`, 411 txns) untouched. **Verified residual (econ-lane impact vs authoritative EXCL `payout`+`connect_reserved_funds`, per DQ-53): SG H1 −1,224 (immaterial, Feb/Mar timing offset); AU H1 −19,308 (~−2.9k/mo = the deferred charge-coverage gap, task #10/#11 — Unpaid Dues uncaptured + charge-lane overlap).** **2026-07-31 CHARGE COVERAGE CLOSED (POL-70, FLOW-32, DQ-55):** verified Unpaid Dues were NOT uncaptured (they settle receivables via `incidentals_paid`, FLOW-32) — the true AU gap was orphan charges (invoice-null + no trip id). Added AU-only `misc_charges_collected` (`v_AU_c_misc_charges_collected` → Dr 1019 / Cr 4025 Incidentals Other); AU `charge` category now a verified 100% partition (real lanes + misc = authoritative to the cent every H1 month). Re-staged H1 both markets. **Final platform residual (excl payout+reserve): AU H1 +943 (was −19,308); SG H1 −1,224** — both small, both-signed accrual-vs-cash timing, immaterial. SG misc lane tested + BACKED OUT (SG lanes already 100%, would double-count). Stripe PLATFORM reconciliation H1 COMPLETE both markets. | me |
| 10 | A-8 | **H1 cross-check vs QB** — diff adjudicated under POL-21 → Gaurav signs off = system passes its acceptance test (Q8 layer 4) | both |

**Stripe SYNC endpoint wired 2026-07-30:** `POST /api/accounting/economic-events/sync` {entity_id, date_from?, date_to?} — one call STAGES every month in range (events land STAGED, zero ledger effect; POSTED stays immutable → MISMATCH not silent re-post) + IMPORTS Stripe payout lines. PROJECT (posting) stays the explicit human gate. Tested via Flask test client (200; June staged 0 as already-posted-matching; payouts deduped; no-entity_id→400). **LAST MILE DONE 2026-07-30:** BFF passthrough `POST /accounting/economic-events/sync` (admin-bff/finance-accounting.ts); FE `syncEconomicEvents` service + `useSyncEconomicEvents` hook + `EconomicEventSyncRequest/Result` types + a **"Sync (stage + payouts)" button** (blue, DownloadCloud) on EconomicEventsTab → stages selected month (not posted) + imports payouts in one click. tsc green both repos; 0 new lint errors; backend endpoint tested via Flask test client. Button code-complete, NOT pixel-viewed. Old `stripe_sync/` still the dead path (task #19 retire). **POL-67(C) verdict: FALSE ALARM — no change.** `host_trip_earnings_rms` (Dr 5001/Cr 2120) is the REAL RMS cost from payout_entries `payoutType='rms'` (POL-56/#17), a legitimate host payable; the internal transfer is separately on 1018/1017 (connect_internal_transfer) — different accounts, no double-count. Cr 2120 is correct.

**Revenue/cost re-arch ACTUALLY DONE (task list was stale, corrected 2026-07-30):** #7/#8 (P2P/RMS/Flex+ split — 4000/4001/4002 populated both markets), #10/#11 (incidentals accrual — 4020-4032 populated), #12 (subscription leak-routing — flexplus/insurance leak views + 4010/4011) all VERIFIED done via posted balances + view_map wiring; marked completed. The AU Stripe charge residual is accrual(invoice-date) vs cash(charge-date) TIMING, not missing views.

**Reports restructured 2026-07-30:** `report_service.py` P&L + Balance Sheet + consolidated now render **hierarchical category → sub_category → account, code-ordered, with subtotals** (was flat, magnitude-sorted). P&L is multi-step: revenue − COGS = gross_profit − opex = operating_income + other_income − other_expense = PBT − tax = net_income (Other Income + Tax broken out of the old flat `expenses`). All runtime off the ledger (incl. consolidation → USD; IC eliminated, residual surfaced). Verified: entity sheets balance to 0.00; consolidated faithful (Σ entity×rate), IC residual $608 surfaced; 8 report tests pass. New API shape: `documentation/wip/REPORT_API_SHAPE.md`. **FE DONE 2026-07-30:** updated `admincontrols` (branch feature/finance-api-integration) `types/accounting.ts` (grouped `ReportGroup`/`ReportAccountSection`, `PnlBody` multi-step, split opex/other/tax) + `components/accounting/ReportsTab.tsx` (new `GroupedSection` renders sub_category subtotals + indented code-ordered account lines; multi-step P&L rows). BFF (`admin-bff/src/routes/finance-accounting.ts`) is pure passthrough — no change. `tsc --noEmit` green, 0 new lint errors (repos at `new-drivelah-code/drivelah-code/{admincontrols,admin-bff}`). Uncommitted on both branches.

**Late-session 2026-07-25 (recorded for recap):** JE25 double-count killed (Gaurav catch, ~S$3M/H1; template inactive both entities) · financial display view live (groups/labels/desc-sort, migration 040) · staged-table review surface complete (350 events) · payout sync = Wise-style (full-then-incremental) · **imports ALWAYS land IMPORTED (auto_categorize=False everywhere)** · **WISE FULLY RESET** (1,148 txns + 663 JEs deleted, sync state cleared, backup kept — never-synced state) · `finance_sync_runs` receipts table live (041, wired: wise/payouts/staging) · transactions pagination (X-Total-Count + id tiebreak; FE sticky header, dual pagers, Added column) · Wise-SGD mixed-state question MOOT (superseded by the full reset).

**PENDING QUEUE (2026-07-25 close):** ① SYNC STANDARD LOCKED (Gaurav): every bank-account sync = last-sync incremental, else 90-day cap (payouts aligned ✅ 5ac697d; Wise already conformed). Historical one-go loads = run operationally from Claude Code with explicit dates — Wise backfill pending (start date TBC, likely 2026-01-01) ② multiselect + bulk actions on Transactions (bulk endpoint + FE) ③ MISMATCH re-project action ④ sign-by-effect display + exact-total FE polish ⑤ Sync-runs UI surface ⑥ Gaurav: review staged H1 → POST month-by-month ⑦ then A-7 reports · A-9 lock · A-10 recon checkpoint · A-6 depreciation+GST · A-11 gate/corpus wiring ⑧ Gaurav: H1 bank statements + invoices (A-1) ⑨ parked: 52 review items · cross-entity corridors · FE-repo commits · A-12 CBA/OCBC-CSV adapter fixes ⑩ **C1 corridor REOPENED for PDF wording (2026-07-25):** OCBC PDF statements label 1001↔3001 transfers `FUND TRANSFER CT00nnn SMITnnn` (same CT ref on BOTH legs — proven on the Jan-2026 statements of both accounts) while rules #2/#26/#27 match CSV-era wording only, so PDF-imported legs fall to AI → NEEDS_REVIEW (Jan 3001: 5 rows, AI guessed 2200). **RESOLVED (Gaurav, 2026-07-26): assume-target extension applied** — #2/#26/#27 patterns now carry PDF-wording alternatives (`|FUND TRANSFER CT00` / `|TRANSFER CT00`); the 5 January rows re-ran → all claimed by #2 as INTERNAL_TRANSFER → AWAITING_MATCH on ba1 with JEs 1750-1754; they pair automatically when the 1001 Jan statement is imported. January ba18: 13 Awaiting + 1 Matched, zero Review/Pending. FE fix shipped same-day: status badge rendered "Pending" for ALL non-Matched/Await/Reconciled statuses (Needs Review + Imported included) — root cause of "pending rows I can't filter to"; now ⚠ Review / · Imported ⑪ **ENGINE-VERDICT SURFACING BUILT (Gaurav "yes to all", 2026-07-26):** found+fixed silent failure — migration 030's `categorized_by_rule_id`/`categorized_by_logic` columns were never declared on the model, so every engine stamp since March vanished (all 3,226 rows NULL; now declared, stamped at ALL decision points: rule/transfer_rule/transfer_pairing/counterparty_default/invoice_knockoff/payroll_knockoff/asset_parking/ai/manual/needs_review_resolution; cleared on reject/reset; 56 rows backfilled from JE source). API now returns expected_counterpart_ba_id + route fields; `resolve_needs_review` also now sets coa_account_code (was missing → resolved rows showed no CoA). FE: Category column = engine verdict (sign-guess marked "?"), Await rows show "→ {target bank}", Review rows show AI code @ conf% + reasoning, route line under status badge, ReviewPanel with Accept-AI-suggestion/override (new BFF resolve proxy + FE hook), CategorizePanel preselects from engine verdict. Suite 618/0; FE+BFF tsc clean ⑫ **AI RAN WITH AN EMPTY COA SINCE THE RAG WIRING — FOUND+FIXED (2026-07-26, via Gaurav's Wise-SGD run):** `_run_ai_classification` COA query was `entity_id == X AND status == "active"` — master COA is group-level (entity_id NULL, 136 accts) and the enum literal never matched → EMPTY chart for every entity; AI leaned solely on similar_past (explains 2200-style guesses). Fixed: `(entity_id == X OR NULL) AND AccountStatus.ACTIVE` → 142/146/141 accts per entity. Wise-SGD 9 re-run: 6 booked (4 FX→1330 Currency Exchange @0.95 · IRAS→6502 · top-up), 3 → Review (2× 6000, 1× 5032). Rule #290 wording gap fixed (`|Topped up account` — API text differs from authored pattern); top-up rejected+re-run → claim-only AWAITING via #290. reject() now also clears coa_account_code+categorization_type. NEW: `journal_entry_id` list filter (API+BFF+FE) → expanded matched transfers show "Paired with" legs (proven: JE 1740 = Wise +12k ↔ OCBC −12k). Needs Review added to FE status filter. **OPEN RULINGS FOR GAURAV: ① IRAS $3,261.99 → 6502 Fines&Penalties (draft JE — likely s/b tax) ② bless/reject 1330 Currency-Exchange clearing as the standard FX-conversion treatment (if blessed → make it a RULE) ③ txn 3207 carries stale coa 1001 from the voided AI JE (one-line clear pending).** ⑬ **CBA WIPED (Gaurav, 2026-07-26):** all 40 ba17 txns + 38 draft JEs + 76 lines deleted for fresh re-import (backups `*_cba_backup_20260726`; no cross-account legs affected). Note: ~~CBA PDF adapter is still broken (A-12)~~ **REBUILT same-day (A-12 CBA leg ✅):** old parser = 0 rows on every real PDF (header regex demanded one-line 'Statement Period:'; PDF wraps it). New parser proven on ALL 23 real statements 2022-2025: 12,006 rows, 100% balance-chain, 0 errors, self-reconcile gate (opening+Σ=closing else refuse), wrong-account guard via `Account Number` extraction, year-spanning periods (Dec-Mar), format-agnostic fingerprints [date,amount,balance] (CSV aligned; CBA table was empty so no fingerprint rewrite needed). Sign law discovered: debit = bare number + ' $', credit = '$'-prefixed. 10 new tests (`tests/test_cba_adapter.py`); suite 628/0. CBA re-import UNBLOCKED. **Same-day follow-ups:** ① 2026 CBA layout (new generator: spaces collapse at default extraction; '$' debit/credit markers GONE) — adapter now detects the vintage, re-extracts at x_tolerance=2 (real word spacing back) and derives signs from the balance chain; 2 more tests, all 25 statements 100% chain ② **H1'26 CBA LOADED operationally: 810 rows IMPORTED** (Q1 428 + Q2 382, 31 Dec 2025→30 Jun 2026; chain Q1 close 10,442.91 = Q2 open) ③ wrong-account guard caught a SECOND CBA account in Gaurav's files: `Statement20260330.pdf` = acct 06 3882 10950841 (161 rows, Jan-Mar 2026) — NOT in our bank accounts table; Gaurav to identify ④ **reject() now CASCADES**: voiding a transfer pair's shared draft JE resets BOTH legs (RECONCILED partners never touched); +2 tests ⑤ **self-JE guard**: engine refuses contra == bank's own COA (AI booked Dr X/Cr X twice: Wise top-up→1001, Stripe-AU payouts→1019); +1 test ⑥ **AU payout corridor built (rules #335/#336, POL-24)**: ba21 'Stripe payout'→CBA + CBA 'Direct Credit 158824 DRIVEMATE'→ba21; the 3 junk Jan self-JEs rejected, re-run → all 3 pairs MATCHED via #335 + transfer_pairing (JEs 1998-2000). Gotcha recorded: rules authored via raw ORM must store bank_account_ids as JSON text (`json.dumps([21])`), NOT a Python list (psycopg2 writes '{21}' and the engine's json.loads skips the rule silently). Suite 633/0. ⑦ **"bulk action failed but actually worked" (Gaurav, Wise-AUD run):** BFF bulk proxy timeout was 120s; run #421 took 123s — proxy aborted, engine finished fine. Timeout → 600s; FE now says "connection dropped — run usually completes" + auto-refetches instead of claiming failure. Long-term: async bulk-run job + polling (queued). ⑨ **A-7 v1 SHIPPED (2026-07-26): three-statement reporting, entity + consolidated.** Backend `report_service`: P&L (period; Rev−COGS=Gross−Exp=Net) · Balance Sheet (as-at; ALL P&L accounts since inception roll into 'Retained earnings (system)' → balances by construction, imbalance surfaced never hidden; SG Jan-31 verified imbalance 0.00) · Cash Flow (direct method: cash codes = bank-linked COA; each JE's cash delta bucketed by dominant counter-account type: REV→op-in, EXP/COGS→op-out, ASSET→investing, LIAB/EQ→financing, IC category→intercompany, cash↔cash→internal transfers) · Consolidated (SGD; AUD @ caller-set rate, v1 manual, default 0.85; Intercompany-category accounts eliminated with residual REPORTED — Jan'26 residual 0.00). Every report takes `basis`: posted (default, approved books) | all (posted+draft preview). Routes /api/finance/reports/{pnl,balance-sheet,cash-flow} (entity_id absent → consolidated). ENTITY_CURRENCY hardcoded {1:SGD,2:SGD,3:AUD} — entities table has no currency column (debt). FE ReportsTab rebuilt (replaced 1552-line MOCK-DATA tab that was already registered!) — entity/Consolidated selector, month picker, basis toggle, AUD rate input, three sub-views, eliminations box, per-entity strip; BFF proxies added. Jan'26 basis=all verified: SG Rev 496.5k/Net −49.5k · consolidated Rev 1.058M/Net −144.5k SGD. KNOWN v1 LIMITS: multi-currency accounts inside an entity (Wise INR/PKR/USD under SG) book raw amounts unconverted; opening cash = 0 until pre-Jan history loads; no FX reval. NEXT: Gaurav eyeballs Jan'26 → then balance tie-out to bank statements (A-10 recon). **AMENDED (Gaurav, 2026-07-26): consolidation is ALWAYS USD** — sgd_usd_rate (0.74 default) + aud_usd_rate (0.62 default) params, manual v1 rates; FE dual-rate inputs (agent updating). Trial-balance FE 404 fixed (path was missing /reports/). **OPEN ISSUES FROM REVIEW: IC ledgers don't mirror** — 8010 Due-from-SG(AU) CR 47k vs 8110 Due-to-SG(AU) DR 31k vs 8100 Due-to-AU(SG) CR 38.7k; signs suspicious (due-from carrying credit), no CROSS_ENTITY_ALLOCATION rules exist, 6 IC sends still parked PENDING — full IC reconciliation pass needed. **IC DESIGN LOCKED (Gaurav, 2026-07-26, POL-27):** cash-only IC trigger (transfers + cross-entity invoice/payroll settlement, all system-generated) on SIX net IC accounts; no on-behalf marking, no allocations. **A-18 implement POL-27 — STARTED 2026-07-26:** ✅ 6 net IC accounts created (8200/8201 SG · 8210/8211 AU · 8220/8221 Ventures, entity-scoped, category Intercompany) ✅ ALL five QB relics retired (#155/#202/#23/#235/#236, stamped in descriptions) ✅ 12-rule IC book LOCKED by Gaurav (IC-1..12: SG↔AU Wise legs from live evidence; Ventures legs from mined GL wording with ≥1,000 floor; full-entity-name patterns per DQ-16; new category INTERCOMPANY_TRANSFER). **PROGRESS same-day:** ✅ old 8xxx DRAINED to zero (4 RECONCILED txns operationally reverted — posted JEs voided on Gaurav's order; +2 orphan draft JEs incl. a DQ-16-trap booking voided) ✅ all 12 old 8xxx accounts SUSPENDED ✅ engine support live (TransactionCategory.INTERCOMPANY_TRANSFER + CategorizationType.INTERCOMPANY; IC rules ride Phase 0.5; route stamp 'ic_rule'; counterpart entity reuses allocation_entity_id — NO migration needed) ✅ **12 IC rules AUTHORED (#342-353)**, scoped + full-name patterns + Ventures ≥1,000 floors ✅ 4 reverted txns re-run → booked via IC-3/IC-4 onto 8210/8200; **mirror TIES at actual rate: 8210 Dr 31,000 AUD ↔ 8200 Cr 26,704.35 SGD (0.861)** ✅ FE: IC category badge + 'IC rule #n' route label. Suite 636/0. REMAINING: ~~cross-entity pair LINKING~~ **DROPPED per POL-30 (Gaurav, 2026-07-27): legs book independently, consolidation elimination nets them; only the periodic FX-residual true-up to 7100 remains** · account register (task created) · unpark 6 IC sends · Gaurav re-run engine over remaining IMPORTED Wise legs. **A-17 CURRENCY LAYER ✅ BUILT (2026-07-26):** `finance_entities.base_currency` already existed + correctly set (no work) · migration 044: journal-line `currency`/`native_amount`/`fx_rate` (all existing lines backfilled functional/same/1.0) + `finance_fx_rates` table · **49 REAL ECB rates seeded** (frankfurter API, Jan–Jul 2026, SGD-base×5 + AUD-base×2 per month; seeded Jan AUD→SGD 0.85917 vs observed Wise actual 0.861 ✓ POL-26 spread confirmed) · FX account = existing **7100 Foreign Exchange Gains/Losses** (no creation needed) · `fx_service` (monthly rate + inverse fallback; **missing rate REFUSES booking** — no more silent raw-currency corruption) · `_create_simple_entry` converts to functional at booking, stamps native facts per line (GST-split proportional) · `journal_service.create` accepts/defaults line currency metadata · JournalLineResponse exposes the 3 fields · report currency now reads base_currency. **PKR not in ECB set — Wise PKR bookings refuse until Gaurav supplies a manual rate.** NOT in v1 (by design): statement-actuals override automation (lands with A-18 knock-off) · month-end reval (POL-26 ④ deferred) · FE display of native/rate. Suite 639/0 (3 new currency tests; 1 legacy test updated to seed a rate — refusal law). **OPENING BALANCES ✅ BOOKED (POL-28, 2026-07-26):** JEs 2460/2461/2462 POSTED (SG plug 66,142.08 SGD · AU 202,771.59 AUD · Ventures −95.07 SGD → 3200 Opening Balance Equity). Method self-validated: derived CBA opening = statement opening to the cent; OCBC 3001 = PDF anchor. Recon identity now closes INSIDE the ledger (CBA: posted+drafts+unbooked = statement, residual 0.00). Skipped pending data: Wise PKR · Ventures Wise ×3 + DBS USD/EUR (need Nov-Dec 2025 DBS statements + Wise balances) · Stripe Connect (by design). **A-10 RECON CHECKPOINT ✅ BUILT + WISE RESET #2 (2026-07-26):** `report_service.get_bank_recon` — per-account AS-AT-WATERMARK identity (statement = posted + drafts + unbooked + residual; native basis for foreign accounts; unpaired-transfer counter-leg lines excluded to prevent double-count) merged into GET /bank-accounts as `recon`. **First run caught 2 real Wise-sync defects** (DQ-18): newest-first inserts (reversed intraday id order → wrong opening/watermark row selection) + reference-only dedup dropping real rows (36 then 39 — incl. a hardcoded source_id-override in import_from_rows bypassing caller fingerprints; removed). FIXED: chronological inserts + [date,amount,balance,ref] wise fingerprint. **WISE RESET #2:** all rows wiped + resynced Jan 1→today = **1,915 rows, ZERO chain breaks across every account**; 17 paired non-Wise counter-legs reset PENDING; all Wise rows land IMPORTED (engine run pending). Opening JE 2461 VOIDED → corrected JE 2467 (AU plug 200,284.08: Wise AUD true opening 1,124.13 not 2,478.76; Wise USD 0.00 not 757.15 — misordered-first-day artifacts). **RECON: 11 green / 0 red — every reconcilable account ties to the cent.** FE ✅ landed: Reconciliation column on BankAccountsTab (green "Reconciled ✓ through <date>" / red "RESIDUAL <amt>" / gray "No statement feed"; click-to-expand decomposition Statement = Posted + Drafts + Unbooked (+Residual), native-basis note; tsc clean). A-10 COMPLETE. FE polish (2026-07-26): TransactionsTab columns drag-resizable (per-column min widths, localStorage persistence, dbl-click reset, horizontal scroll when over-wide; description default 320px) · #214 Connect CSDB rule scope extended to OCBC 1001 (Gaurav; 3 rows re-booked standalone-Matched: 657.13/356.50/848.00). **ENGINE RE-RUN OVER WISE H1 ✅ (2026-07-26, Claude-run per Gaurav):** 1,757 Wise H1 rows + 17 counter-legs + OCBC-1001 H1 processed. The checkpoint caught its SECOND live defect mid-run: AI booked 76 pseudo-transfer JEs with contra = ANOTHER bank's COA code (CBA "Transfer To Wise" leftovers → Dr 1010; double-counted vs corridor JEs; −1.53M residual). NEW BANK-CODE GUARD: AI can never book contra ∈ any bank COA — routes to review ("[ai:bank-code-guard]"); 76 rejected + re-run clean. Also fixed native-amount rounding (full-amount lines carry txn's TRUE native, never func÷rate — was −0.20 on Wise INR). **FINAL: RECON 11 green / 0 red. H1 system-wide: 2,994 MATCHED · 379 review · 398 imported (July+DBS gaps) · 212 reconciled · 46 awaiting.** Suite 639/0. NEXT: Gaurav review sweep + approvals; IC mirror 8200 −43,555.01 SGD ↔ 8210 +50,814.66 AUD (implied 0.857 blended actual ✓). **H1 REVIEW WALK (2026-07-27, bucket-by-bucket): 375 → 253.** Closed: ① POL-23 CBA wording (17 → 6702; #283/#284 patterns extended with 'DRIVE MATE'/'DRIVE LAH PTE. LTD. SINGAPORE'; 2 mangled resolved manually) ② PayNow/OTHR receipts into OCBC 1001 (53 → 4000 GBV-P2P via NEW rule #362 — wording variants of #90's 204 siblings; Gaurav ruling) ③ CBA individual Fast-Transfers (48 → 4025 ONE-TIME, deliberately NO rule — future ones stay in review; Gaurav ruling) ④ CreditLine BPAY = company-laptop monthly financing → 6310 via NEW rule #363 (CP-32). **REMARKS ROUNDS APPLIED (2026-07-27): queue 375 → 75.** Card merchants (46 remarks → 57 txns resolved; Employee-Claims mapping 6011/6012/6014; RMS washing → 5022 per CP-16; 46 corpus entries). Wise payees (67/67 remarked → 105 txns resolved: 48× 'incidental payout'→5025 · workshop→5032 · Hollard trip premium→5035 · Riezel→5063 · PRECISION CLEARING HOUSE asset→1520 one-off · etc; 14 vendor/employee counterparties created + 8 updated with defaults+aliases; **45 customers deliberately NOT created per NEW POL-29** — customers never enter the directory; 65 corpus entries). Also: fee rows → 6600 convention · #224 pattern fixed (was 'Transfer Wise', NEVER matched) · mangled −100k row description recovered from source PDF → claimed by #224 · rule #362 (PayNow→4000) + #363 (CreditLine→6310) live · DQ-19 (HR-sync no-default gap) + CP-32 + POL-29 recorded. **FINAL-75 ROUND APPLIED (2026-07-27): H1 review queue = 1.** 69× 'ok' (AI ratified incl. CodeRabbit/Termius→6701, CF- credit→1220) · 'Consult work' −34,648 = **Gaurav director salary → 6003** (cp #260) · +42,000 Misc payout = **LOAN → 2400, lender TBC (OPEN: Gaurav to confirm from whom)** · FCM Pty Ltd created as assessor vendor, refund credits 5034 · Autorola refund → 5032 (cp #41; dupe #340 still to merge per DQ-8). **HELD (the 1): +10,000 REVIO LABS 'Refund' on OCBC 3001 — the ONLY Revio txn in the system; the original outbound payment is NOT in our books (pre-2026 or unimported 2025 account). OPEN ruling: what was originally paid for + treat refund as expense-credit vs other income.** 5 corpus entries. NEXT: Gaurav bulk-approve sweep (drafts → posted) — recon board should hold green through it. **DBS WIPED (Gaurav, 2026-07-27):** 2 txns + 2 JEs deleted (backup `finance_transactions_dbs_backup_20260727`); Ventures opening JE 2462 VOIDED (was derived from the wiped data). **Ventures openings RE-BOOKED (2026-07-27): JE 5901** from the Jan'26 DBS statement's own B/F facts — Cr 1013 SGD 95.07 (overdrawn) · Dr 1012 USD 206.95 @ 1.285529 = SGD 266.04 · EUR 0 (no line) · plug Cr 3200 170.97. This explains the "residual 95.07" Gaurav saw on DBS SGD after his Jan upload: the residual WAS the missing opening (identity −165.07 = 0 posted + 0 drafts + −70 unbooked + −95.07 gap). After Gaurav's Feb–Jun DBS uploads + JE 5901: **DBS SGD & USD residual 0.00**; EUR zero-txn → no recon line (known gap: recon needs a txn watermark; stamp shows 0.00 so nothing hidden). Note: USD Jan B/F 206.95 ≠ Oct-25 C/F 9,206.95 — movement sits in the missing Nov-Dec 2025 statements (Phase B history). **DBS REVIEW TRIO RESOLVED (Gaurav rulings, 2026-07-27):** ① −30 USD OTT charge → 6600 ② −71,000 USD May-19 "OUTWARD TELEGRAPHIC TRANSFER" = **IC Ventures→SG, proven pair** with OCBC-1001 txn 6048 "+90,162.90 TT IN SGD USD 71000.00" same-day → Ventures leg to 8220 (JE 5919, 90,609.77 SGD func @ std 1.276194) + SG leg RE-BOOKED from AI's wrong 1330 → 8201 (JE 5920; old JE 5495 voided); mirror residual 446.87 SGD = std-vs-actual spread, goes to the 7100 true-up at close per POL-30 ③ +71,520 USD May-18 "TRANSFER OF FUND FT…" = LOAN → **BOOKED 2400 (JE 5925, 91,273.39 SGD func @ 1.276194, native 71,520 USD) — lender TBC, Gaurav to name** (2nd open lender ID alongside the 42,000 SGD loan). **BUG FOUND+FIXED en route: `resolve_needs_review` bypassed POL-25 conversion** (built JE lines straight from native — USD 71,000 was about to book as SGD 71,000 @ 1.0); patched with fx_service conversion + per-line native stamps; blast radius audited: only 3 JEs ever affected (all DRAFT — the 2 just-created + txn 11075 Riezel −550 USD on AU books), all 3 voided + re-booked correctly (JEs 5917-5919). +1 regression test; suite **640/0**. Recon: DBS SGD/USD + OCBC 1001 all residual 0.00. **AWAITING-MATCH SWEEP (Gaurav ask, 2026-07-27): 8 H1 waiters → 1.** Root causes + fixes: ① Apr-11 OCBC triangle — pairing had matched TWO txns inside the SAME account (5213 −10k 3001→Wise with 5874 +10k 3001→1001 on JE 2480, both ba-mislabeled): 2480 voided; 5874+5214 (same CT ref, DQ-14) now share JE 3218; NEW JE 5926 pairs 5213↔9393 (3001→Wise SGD top-up, Dr 1001/Cr 1016) ② Jan Stripe 45.10 — CSDB <1000 rule booked a Connect standalone story (JE 1741) while the REAL Platform payout 2309 existed in the feed: 1741 voided, 5106+2309 share JE 1802 ③ Jan-2 25k — counterpart was payout 2308 dated Dec-31 sitting IMPORTED (outside H1 runs): attached to JE 1739 ④ CBA→Wise-AUD April chain — a 100k send arrived as 2×50k credits; 1-to-2 defeats exact-amount pairing and shifted every later pair off-by-one: JEs 4241/4243/4244/4250/4253 voided; 6385+10348+10350 share JE 5636 (100k); new JEs 5927/5928/5929 re-pair 6403↔10432, 6427↔10459, 6439↔10525. **Recon after: ba1/2/11/17/18 ALL residual 0.00.** LAST WAITER + THE HELD REVIO ITEM = ONE STORY, ✅ CLOSED (Gaurav's reversal hunch, 2026-07-27): the Jun-16 −10k "FAST OTHR WISE" from 3001 never reached OUR Wise — it was a mistaken payment to REVIO LABS' Wise account (CP-33: Revio banks with Wise, wording identical to our top-ups; balance arc 58.49 → refund +10k Jun-18 "…Refund"). Wrong transfer JE 3248 voided; ~~1220 wash (JEs 5934/5935)~~ **REVISED per Gaurav: direct NET-OFF — one JE 5936 (Dr/Cr 1016 10k, both txns attached, no receivables leg, no P&L)**. **H1 AWAITING = 0 · Revio review item = 0 · ba18 residual 0.00.** ENGINE GAPS → **3 GUARDS BUILT same-day (suite 643/0):** ① `_pick_counter` pairing law — different-account hard filter, ±2%/±5d, ambiguity resolved ONLY by shared bank-reference token (DQ-14) then tightest date, else REFUSES to guess (leaves awaiting for a human) — used by Phase 0 AND the claim-time counter-search ② both-sides-know guard `_find_awaiting_mirror_je` — a knowing transfer rule now ATTACHES to the counterpart's existing awaiting JE instead of writing a second JE for the same cash (kills the C1 #2/#26 duplicate class) ③ counter-search candidates now include IMPORTED rows (out-of-window payout legs, the Dec-31→Jan-2 class). STILL QUEUED: 1-to-N amount matching (100k arriving as 2×50k stays a manual re-pair — rare; revisit if it recurs). **STRIPE RECON ✅ BUILT (Gaurav "build straight away", 2026-07-27):** ① get_bank_recon STAMP-FALLBACK — accounts with no running-balance feed reconcile against api_sync_state (Stripe Platform via ClickHouse Σnet; also gives DBS EUR zero-txn a line); adds `staged` bucket (STAGED events × template Dr/Cr on the account's COA) + `balance_source` tag; unbooked now floor-bounded at 2026-01-01 (pre-opening rows live inside the opening JE — the all-history payout pull was double-counting 4M) ② payout-sync stamp now lane-aligned: balance_as_of = MIN(events lane, payouts lane, today) — Σnet cut at that date (was all-time-at-today → phantom July residuals) ③ Stripe design LOCKED (discussion): ONE sync moves payout lines + event staging together · sync=mirror-refresh NEVER posts · staged upsert by (source,entity,event_type,period) · posted+changed → MISMATCH flag, open-period void+repost / locked-period delta JE · balance from Σ(balance txns) with API balance endpoint as drift tripwire (unified sync button + MISMATCH action still to build). **Follow-ups same-day (Gaurav):** balance cutoff now at ENTITY-LOCAL midnight (Asia/Singapore · Australia/Melbourne) — matches the Stripe-dashboard view (UTC cut was off by the overnight, AU by 4,072) · new `synced_through` stamp = min-lane coverage, FE "Synced through" prefers it over last_synced_at (was showing the sync-ACTION date 27-Jul). Post-tz recon: SG 36,064.09 residual 23,007.90 · AU 37,484.84 residual 18,478.23; both repos clean. **FIRST STRIPE RECON RESULT (Jun-30): openings tie to the cent (1017 41,123.59 · 1019 89,329.02 = CH pre-2026 Σnet ✓). SG residual 23,219.84 = ~25k Dec-31 payout IN TRANSIT at Jan-1 (left Stripe Dec-31 → inside opening; landed bank Jan-2 → booked against 1017 AGAIN via JE 1739) + ~1.8k tail. PROPOSED (awaiting Gaurav): opening amendment Dr 1017 25,000 / Cr 3200 (POL-28 method amendment: Stripe opening = CH pre-2026 Σnet + payouts-in-transit at Jan-1). AU residual 22,550.40 has NO boundary payout — spread Feb (−14.8k) + Jun (−8.4k): view-coverage/timing questions = A-5b's job.** **EVENTS-VS-STRIPE INVESTIGATION (Gaurav ask, 2026-07-27; month × category instrument built):** ① PAYOUT LANE EXACT both regions every month (sole exception = the known SG Jan 25k in-transit) ② SG ACTIVITY essentially healthy: ±hundreds/month, H1 net +1.8k (timing + micro-categories) ③ AU ACTIVITY under-captured EVERY month (H1 ≈ −25.5k). Feb dissection (−9,647.38): host lane ties EXACTLY (transfers+transfer_reversals+application_fees net = host_transfers_cash to the cent) · refunds tie to the CENT (6 refund events = Stripe refund total) · stripe_fees event = total fees to the CENT (per-charge 7,767.06 + billed 4,333.38) — so the engine core is RIGHT and charge events must book GROSS; the holes are: **~9.4k/Feb of GROSS CHARGES claimed by NO view** (prime suspects: the 9 unread views / code streams) + uncovered micro-categories (connect_collection_transfer −1,526 · reserve_transaction +1,894 · dispute FEES −325 · payment_failure_refund/refund_failure +206) + ~498 unexplained tail. NEXT (A-5b core): enumerate AU charges unclaimed by the view catalog → registry additions for the micro-categories → re-run the instrument to zero. **Stripe API access PROVEN (restricted live keys in ai-agents/new-monitor-api .env — values carry inline-comment junk, strip at first space): mirror + API tail = live balance TO THE CENT both regions → ClickHouse mirror is COMPLETE, only lags 1-2 days. Drift tripwire to wire into sync.** **CLOSE-OUT SWEEP (Gaurav orders, 2026-07-27):** ① SG in-transit opening amendment BOOKED — JE 6233 (POL-28a; my duplicate 6234 voided: the first booking ran despite a rejected tool result — beware, rejected Bash may still have executed) → SG Stripe residual −1,992.10 = A-5b micro-tail ② **H1 FULLY POSTED: all 3,681 H1'26 txns RECONCILED** (bulk run receipt: 2,743 posted + 125 pair-cascade completions + Gaurav's own morning postings of Feb–Jun events; zero matched/review/awaiting/imported left in H1) ③ Wise sync now stamps latest_balance (live API) + balance_as_of = synced_through = coverage date (balance-date law, POL-28a; all 11 Wise accounts stamped as-of today; +38 AU / +2 SG July rows imported) ④ FE page size option 500 added (API already capped 500). **NEW from the stamp fallback: Ventures Wise SGD/AUD/USD now show recon residuals 87.57 / 25.49 / 26.49 = their MISSING POL-28 openings (previously skipped for no data; live balances now proven, no txns in coverage window) — proposed: book into the Ventures opening JE, awaiting Gaurav.** Residual board otherwise: only ba19 −1,992.10 + ba21 18,478.23 (both = task #5). Suite 643/0; FE tsc clean. **INDEPENDENT 4-AGENT AUDIT OF H1 (Gaurav order, 2026-07-27 — currency-tech · CFO-grade · classification-skeptic · coherence):** ✅ PROVEN CLEAN: currency math 0 violations over 8,024 lines (rates/native/balance/openings exact) · double-entry integrity green (all TBs tie to the cent, all BSs balance 0.00, linkage/VOID/events-1:1/fingerprints perfect, H1 100% RECONCILED) · IC discipline POL-27-clean · zero double-books · CFO verdict: "cash layer better controlled than most audited SMEs". **FIX QUEUE (ranked):** ① DEFT INSURANCE 69,472.07 AUD sitting DR in 2500 GST-Payable (counterparty_default) → reclass to insurance cost (AU loss −69.5k) ② ATO Direct Credit 37,722 booked as negative income-tax (9000) → reclass 2500/receivable (−24.2k) ③ GLOBAL TRANSPORT +11,982 AUD incoming credited to 6400 Travel (false cp match) — identify true nature ④ 3 customer PayNow receipts → 6001 CPF (cp 520 false match; payer's siblings went 4000 via #362) ⑤ 30 platform-test charges → 6400 via "Didi Mobility" false match (POL-23 says 6702; −99 AUD) ⑥ Ventures 7002 interest sign error (−180) + $10 Yifei Yu in 2400 ⑦ 584 economic_events lines carry NULL currency/native/fx (projector doesn't stamp; backfill functional/same/1.0 + fix writer) ⑧ 358 pre-2026 IMPORTED Stripe rows = standing double-count hazard (neutralize) ⑨ ROOT CAUSE (3 of top-4 misbookings): counterparty_default weak name-matching overrides correct routes — build direction-aware default guard (expense-default cp + INCOMING money → review, not booked). **GAURAV RULINGS NEEDED:** Ⓐ gross-GBV vs agent-net revenue presentation (F1: SG rev 3.51M incl ~2.07M host share; net view needed for external use) Ⓑ AU receivables 587,981 AUD, 50% uncollected, ZERO provision (CFO's biggest objection: 175-295k AUD potential) Ⓒ GST wholly absent from 2026 books (~100-140k AUD exposure) — accrue vs disclose Ⓓ loans 1.25M AUD + 71.5k USD: 2 lenders unidentified, no terms/interest, related-party status (incl 485k J ter Horst, "DJ" series) Ⓔ confirm INSURET claim-receipts-vs-5035 and monthly-discount-credit mappings Ⓕ non-cash placeholder openings (already-agreed plan: lift QB Dec-25 TB) = the direct answer to CFO F6 "BS is position-incomplete". CFO sign-off summary: cash layer YES · SG P&L as mgmt account YES (subject Ⓐ/Ⓒ) · AU P&L NO until ①②+Ⓑ · any external BS NO until Ⓕ/Phase B. FE: bulk-action bar hides actions for AWAITING_MATCH selections (only approve/reject of matched/review supported) — retrospectively lucky, bulk-approving these would have posted the double-counts. **DBS UPLOAD UX FIXED (Gaurav finding, 2026-07-27):** ① generic per-account import on ANY DBS account now delegates to the multi-currency fan-out (shared `import_dbs_statement`, per-currency sync receipts; dedicated /dbs/import route uses the same method) ② NEW currency guard in import_from_rows — a row can NEVER land in an account of a different currency (refused with error note; protects every adapter) ③ FE (agent): upload success now refreshes bank-account cards; "Synced through" falls back to the data watermark for file-fed accounts. Port tests aligned to the 8081 .env fix. Suite 639/0. **ACCOUNT REGISTER ✅ SHIPPED (2026-07-26):** `GET /reports/account-ledger` (entity × account × period × basis: opening + composing JE lines with running balance, Draft/Posted chips, source, native-currency `@rate` annotations, linked txn ids; opening JEs sort first within their day) + BFF proxy + FE: every account line on P&L/BS/CF (entity mode) AND every Trial Balance row is clickable → shared AccountLedgerPanel (TB drills inception-to-as-of, posted basis). Consolidated lines + synthetic retained-earnings line deliberately non-clickable. Suite 639/0; both repos tsc clean. ⑧ **Bulk approve "11 failed: JE already posted" (Gaurav):** transfer pairs share ONE JE — approving leg A posted it, then leg B errored. approve() now posts once and reconciles ALL matched legs of the JE; a leg whose shared JE is already posted completes quietly; bulk approve counts pair-completed legs as ok. The 11 stranded legs repaired (0 remain). Bulk actions now write `finance_sync_runs` receipts (source=bulk_action, detail: action/requested/ok/failed/errors_sample). +2 tests; suite 635/0.

## ▶ Stripe Connect Reconciliation — PARKED (Gaurav, 2026-08-02: "fix later, forget for now")

**State:** deterministic ground truth ESTABLISHED, catch-up JEs NOT booked (all recon read-only; nothing written to prod). Full model + method in KNOWLEDGE DQ-74 (corrected) + DQ-58/59.

- **Openings are RIGHT** (not the problem): AU connect 8,333.22 exact; SG re-derives to **30,175.93** (booked 31,225.86 in DQ-59 → a small −1,049.93 opening tweak, not the 4–9× error earlier passes wrongly claimed).
- **The error is entirely closing-side = under-captured H1 OUTFLOW, refund-heavy** (not sweep-only). Deterministic H1 net movement (cumulate `net`=amount−fee, market-local cutoffs): **SG −17,944.65 / AU −7,072.78**; only ~1,861.63 (SG, via <$1000 Rule 214) / ~−1,360.10 (AU, econ-events) got booked.
- **METHOD LOCK (why we flip-flopped):** cumulate balance-transaction **`net`, never `amount`**, from inception; do NOT use `current−flow` drift. Every wrong pass violated this.
- **DEFERRED FIX (do when unparked):** (1) type-split the connect-pool outflow — bank-sweep vs guest-refund contra (this was mid-run when parked); (2) book catch-up JEs (AU **+5,712.68**, SG **+~24,037.74** incl. the −1,049.93 opening tweak); (3) add the AU <$1000 sweep rule (DQ-58 — AU has none, so AU never reconciles without it); (4) re-derive DQ-59 SG opening to 30,175.93; (5) tighten pool filter to linked-bank ∈ {OCBC-1001, OCBC-3001, CBA-7311}, dropping the 9 mis-tagged host accounts (DBS/POSB/OCBC-2001, ~535 SGD).
- **Board impact while parked:** residuals remain on **ba19 (SG connect)** and **ba20/ba21 (AU connect)** — expected, not a regression. Every OTHER account (platform/deposit/reserve/bank) ties per DQ-60/61.

*Platform, deposit, reserve, and all bank accounts are DONE and tie to the cent (DQ-60/61). Only the Connect pool is parked.*

---

**Phase B — historical rebuild (2019 → 2025):** replay with the locked simplifications — payroll = direct expense (no historic runs, D2); AP legs only where invoices exist (≈Jul-25→Jun-26, D3); older = expense-on-payment; depreciation per D1; cross-check per year vs QB (reference, not truth — POL-21).

## ▶ Finance Access Modules — LOCKED PLAN (Gaurav, 2026-08-01)

**Goal:** retire the single `finance` mega-module; gate the admin console per functional area + add row-level `own` scope so employees see only their own expenses/payslip. Spans **admin-bff** + **admincontrols** (NOT finance-api code). Canonical facts: DQ-72/73 (KNOWLEDGE). Live audit ground truth (queried 2026-08-01): 318 grant rows / 65 users / 13 live modules; only 3 modules route-enforced (`ai-agents`,`finance`,`user-mgmt`); FE finance tabs have ZERO gating today (all tabs show to all).

**Final module set = 19** (12 kept + 7 new finance; only `finance` retired). Kept: `users`(marketplace), `user-mgmt`(console ops), `ai-agents`, `core`, `tech`, `listings`, `transactions`(marketplace), `resolution`, `claims`(insurance), `host-management`, `flexplus`, `verification`. New finance (map to FE tabs by GROUP): `finance.counterparties`→Counterparties · `finance.collections`→Collections · `finance.invoices`→Accounting▸Invoices+Contracts · `finance.ledger`→Accounting▸[entities,COA,bank-accts,transactions,journal-entries,economic-events,reconciliation,trial-balance,categorization-rules,amortization] · `finance.reports`→Accounting▸Reports+FP&A · `finance.expenses`(OWN-scoped, new "My Expenses" surface) · `finance.payroll`(OWN-scoped, new "My Payslip" surface). Retire FE "Revenue & Expenses" tab. Access tier gains `own` below read: `own<read<write<admin`; `PERSONAL_MODULES={finance.expenses,finance.payroll}` filter rows to `owner_user_id=req.user.id` unless level≥read.

**PRs raised 2026-08-01:** admin-bff #5 · admin-controls #24 · finance-api #2 (this). BFF+FE both `tsc --noEmit` exit 0; finance invoice/dup tests 25/25.

**REVIEW ROUNDS DONE (2026-08-01):** 4 specialist reviewers + 3 adversarial (Forge/cross-vendor) over all 3 PRs. Fixed: FE gate-bypass (gated component mounted when tab filtered out) + **Dashboard entry card** gated on retired flat `finance` (locked out sub-module users); BFF prod migration lock (→NOT VALID), silent-403 DB-error swallow, seed idempotency, shim over-match (→FINANCE_MODULES exact), grant `granted_by` CTE; finance-api **money regression I introduced** (`replace(',','')` turned EU `1.234,56`→`1.23456`, 1000×) → locale-aware `_coerce_amount` (16 cases incl EU verified) + surfaced parse failures + `/extract` 20MB cap. Verified: BFF/FE tsc 0, finance 25/25, gating proofs (inner 8/8 + card 6/6, deterministic vs real modules.ts). Pixel view DEFERRED (Interceptor binary uninstalled — logic proven behaviorally instead). **Grant script made flip-safe:** STEP 1 = 5 SHARED modules only; STEP 2 (payroll/expenses `own`) HELD/commented — blocked on M4 (owner-row scoping) or it's a payslip PII hole.
**FLIP (M5) GATED ON:** M4 owner-scoping (payroll/expenses `own`-filter, needs identity→row mapping) + M5 per-route BE enforcement (routes still gate flat `finance` inside the 1892-line accounting router). Merge is safe now (additive, shim-protected); the full switch-flip is the next build, not a bug-fix.

| # | Step | Breaking? | State |
|---|------|-----------|-------|
| M0 | `MODULES`/`TEAMS` const in admin-bff (single source of truth) + FE mirror | no | ✅ done (BFF `src/constants/modules.ts` + FE `src/shared/constants/modules.ts`; PR #5/#24) |
| M1 | `own` tier (CHECK widened + idempotent ALTER) + `satisfiesLevel`; seed/dev-login → `MODULES`; hasModuleAccess shim | no | ✅ done (PR #5) |
| M2 | Additive grants: 8 `finance:admin`→admin on all 7 `finance.*`; every employee→`finance.expenses:own`+`finance.payroll:own` (old `finance` row untouched) | no (additive) | 🔄 SQL script committed `admin-bff/src/database/manual/2026-08-01-finance-module-grants.sql` — **run manually vs prod after review** |
| M3 | FE nav: `requiredModule` per tab + filter behind legacy-`finance` shim; retire Revenue & Expenses tab | guarded | ✅ done (PR #24) |
| M4 | `owner_user_id` on expenses/payroll rows + `own`-filter in handlers + new My-Expenses/My-Payslip surface (hinges on identity→row mapping, DQ risk) | new surface | ⬜ next (data-mapping verify first) |
| M5 | Flip finance route gates `finance`→specific sub-module; drop dead `finance` + shim after verify | breaking→verified | ⬜ gated on M2 live+verified |

**Safety protocol (expand→migrate→contract):** additive grants BEFORE any gate/name removal · backward-compat shim (`finance` legacy ⇒ all `finance.*`) at both BFF gate + FE nav during transition · BFF stays the REAL gate (reads DB live per request — new grants take effect without re-login; FE nav is display-only, may need 1 re-login for JWT refresh) · Phase-1 locks NOTHING tighter than today (only nav visibility changes) · verify each phase with SQL row counts + login smoke test · rollback = drop new grants/shim (additive ⇒ reversible). **Name traps:** `finance.expenses`≠`claims`(insurance); Accounting▸Transactions tab gated by `finance.ledger` NOT the marketplace `transactions` module; `users`≠`user-mgmt` (DQ-73).

**Phase C — future-forward (from Jul-2026):** payroll runs live (D2) · live categorization with RAG · Stripe sync scheduled · TMS event feed when real (F-3) · promote the review/feedback interface from Claude Code sessions to a chat agent in the dashboard (Q5 deferral) · monthly mining pass suggesting rule/default promotions (walked one-by-one). Reporting cadence per IDEAL_STATE.

## 1. What's Done

| # | Item | Source |
|---|------|--------|
| | **Core ledger & infrastructure** | |
| 1 | Flask + SQLAlchemy + Pydantic; PostgreSQL; ~93 endpoints / 19 route modules | initial build |
| 2 | Chart of Accounts (group-level), entities, business lines; seed via `python -m src.seed_coa` | migrations 001/004 |
| 3 | Double-entry ledger: JE CRUD, posting, voiding; multi-currency; balanced-entry enforcement | migration 003 |
| 4 | GST handling — entity rate / account flag / rule override; input (1350) vs output (2500) split | migration 007 |
| | **Bank transaction import** | |
| 5 | CSV + PDF import (OCBC, CBA, DBS multi-currency); adapter registry, fingerprint dedup | §3.2 |
| 6 | Wise API connect + on-demand sync (auto-creates accounts/COA); bank-type selector | migrations 015/027/029 |
| | **Categorization engine** (the core asset) | |
| 7 | 5-phase pipeline: transfer pairing → counterparty enrichment (L1/L2/L3) → AP knock-off → payroll knock-off → rules/default/AI | §3.3 |
| 8 | Rules engine (text/type, no ID coupling), tags, manual categorization, NEEDS_REVIEW resolution, audit trail | migrations 006/009/030 |
| 9 | AI classification fallback (Claude Haiku, confidence-gated) + self-improving aliases on approval | migration 021 |
| | **Counterparties & HR** | |
| 10 | Universal party directory (entity-scoped + global, dedup guards); employee sync (users → counterparties) | migrations 010–014/022 |
| 11 | HR onboarding/offboarding — **now creates compensation + deduction rules** (SG CPF / AU Super defaults) | migration 034 |
| | **Invoices / AP · Payroll · Depreciation** | |
| 12 | Invoices: AI extraction (PDF+image), dedup, vendor match, approval + GST split; AP knock-off (3-case) + cross-entity IC pairs | migrations 016–019, §3.5 |
| 13 | Payroll: per-employee comp + deduction rules → balanced run JE (CPF/Super/tax); Phase-2.5 bank knock-off; duplicate-run guard | migrations 021/022 |
| 14 | Depreciation/amortization: COA-policy-driven, idempotent monthly posting, capitalisation trigger | migration 025 |
| | **Reconciliation & reporting** | |
| 15 | Reconciliation suggestions + confirmation; transaction review queue (approve posts JE / reject voids) | §5 |
| 16 | **Trial Balance** (the only financial report built) | §5.1 |
| | **Stripe sync (baseline)** | |
| 17 | ClickHouse client + views-based query builder → 25 JE specs → ledger; monthly sync orchestrator | §2.4 |

---

## 2. What's Pending

### 2.0c ★ PREVIOUS-YEARS RECONCILIATION (2019–2025) — THE MAMMOTH (Gaurav, 2026-08-15)
Rulings locked in POL-124; spec `wip/HISTORY_RECON_PIPELINE.md`. Year-at-a-time (chronological), engine+economic-events per year, agent lanes, running-balance tripwire, HTML scorecard → Gaurav feedback → post → re-park → jurisdictional year close. **HARNESS BUILT + SHAKEDOWN RUN (2026-08-15):** `wip/history_recon/history_runner.py` (run/check/scorecard; refuses `run` on prod; lifts POL-28 only inside the bounded batch). First shadow year: Ventures Wise (ba 8/9/10) × 2025 on the CLONE — **48/65 auto-categorized, 0 errors; 17 open = the Wise conversion/top-up inflow legs (no Ventures transfer corridors yet — first feedback item)**. Discovery: these accounts are pass-through movers (gross $100k+ legs, tiny net), not dormant. Month-end invariant table quantifies the gap until the 17 resolve. **SCORECARD v2 (Gaurav feedback, 2026-08-15):** header carries entity + bank accounts explicitly; per-txn table shows booked COA (code+NAME), counterparty, AI recommendation (COA name @ confidence + reasoning); EVERY row has a verdict dropdown + correction input; "Export my feedback" button downloads `scorecard_feedback_<year>.json` — Gaurav annotates in the HTML, sends the file back, feedback is applied (rules/counterparties/corpus), year re-runs. Balance section in plain language. **START = 2019 per Gaurav (not 2025).** **2019 SHADOW RUN DONE (clone, SG OCBC ba1+ba18):** 377/388 auto-booked, 0 errors; **OCBC 3001 ties EXACTLY at every 2019 month-end (first green account-year)**; OCBC Main diverges from Aug (−1,297 Jun–Jul then ~15k–32k) = the 11 unresolved + review targets for Gaurav's pass. Scorecard sent: `wip/history_recon/scorecard_2019_sg_ocbc.html`. **★ 2019 SG = FIRST GREEN YEAR (2026-08-15).** Gaurav's feedback JSON (10 verdicts, `wip/history_recon/scorecard_feedback_2019.json`) applied via `resolve_needs_review`: 4 AI-confirms (6400/6701/6402/7001) · Winata/ter-Horst $15k → 2405 Director loan (+alias on cp 268) · 2 Vernika-labelled inflows → 2405 (Gaurav ruling) · Upwork → 6103 + default set ("Upwork is always Marketing asset creation") · MAYBANK cheque → NEW account 7003 Other Income - Miscellaneous (created, clone) · $15,573 JPMorgan inflow = RETURN of the Nov-29 Journey Horizon TT (bounced via correspondent, re-sent Dec-26) → Cr 1710. Mid-loop rule fixes also landed: Google Ads rule 384→6100, director-loan rules 385/386 (counterparty-conditioned, POL-125), rules 239/240 ENT-5 fee-only fix + insurer rules 372/373 counterparty-conditioned (DQ-102). **Month-end invariants: 0.00 at ALL 14 month-ends, both accounts (OCBC Main + OCBC 3001).** Feedback loop proven end-to-end (HTML → JSON → rules/aliases/defaults/resolutions → green). NEXT: 2020 SG (same accounts + Wise SGD once it starts), then year-by-year; prod apply waits until Gaurav signs the year(s).

### 2.0e ★ INVOICE POSTING BACKLOG — IMMEDIATE ACTIONS (Gaurav ruling, 2026-08-15: "at approval, the invoice posts, PERIOD")
Prod P&L only contains PAID invoices; approval leaves the bill JE in DRAFT and paired invoices have NO JE at all. Full backlog (prod, all-time): **66 approved w/ draft JEs ($246,360: $116,934 dated 2026 + $129,426 pre-2026) · 97 paired w/ NO journal ($424,536: $75,136 2026 + $349,400 pre-2026) · 6 partially_paid stragglers ($30,656)** — ≈ **$670k of captured-but-unposted expense**, of which ~$192k belongs in the H1-2026 P&L.
- **I-1** Post the 66 draft approval JEs (Dr expense / Cr Trade & Other Payables at invoice date). Supervised, before/after P&L + TB proof.
- **I-2** Push the 97 paired invoices through posting (bill JE + payment JE vs the matched bank txn → paid). CAVEATS: pre-2026 payment JEs add bank legs → re-run the pre-books park after (I-6); preview GST handling on pre-2026 bills before executing (don't disturb the parked GST).
- **I-3** Sweep the 6 partially_paid for unposted/missing JEs.
- **I-4** CODE: `approve()` posts the journal immediately (no draft stage) — branch `260815_finance_recon_work`, +test, PR. Kills this backlog class permanently.
- **I-5** CODE: pairing completes → post atomically (paired ⇒ paid per POL-107), no stranded paired state.
- **I-6** Recompute `pre_books_park` after I-2 (new pre-2026-dated bank legs).
- **I-7** Inspect the **−$812,576 needs_fix anomaly** (11 AU invoices, negative total = garbage extractions) before anything books.
- **I-8** Cut-off: 5 post-June invoices w/ H1 service period ($10,973) — accrue into H1 or leave; ruling w/ Kaveesh.
- Context: with I-1/I-2 landed + depreciation (A-6) + AR provision (audit Ⓑ), the "too-good" H1 P&L corrects by an estimated $400-600k — hypothesis 3 (missing invoices) CONFIRMED as the main driver; hypothesis 1 (Stripe debris) cleaned 2026-08-15; hypothesis 2 (P&L math) REFUTED (independent recompute ties to the cent).

### 2.0a Bank recon — pre-books park (TEMPORARY, Gaurav 2026-08-15)
- **BR-1 ✅** Root cause of the sudden bank residuals: the 3-4 Aug vendor-SoA pairing runs (`matcher`/`pairing_skill_v2`/`insurance_ref_pair`, posting_user 'system') booked payment JEs for PRE-2026-paid invoices AT their true 2024/25 dates (Dr Trade & Other Payables / Cr bank) — historically true, but that cash is already inside the Jan-1 opening balances → double-count. CBA: 8 JEs $240,200.44 (incl 3 Penguin Risk ~$218k). Wise AUD: 515 JEs **$2,303,905.09 = the residual to the cent**. Pairing itself was correct; the replays also left phantom pre-2026 Prepayments/1350 legs (1350 already swept by gst_h1_opening).
- **BR-2 ✅** RULING (Gaurav): keep history true; POST A TEMPORARY PARK against Opening Balance Equity instead of voiding — removed together with the opening JEs when pre-2026 history reconciles. **JE 11477** (2025-12-31, source `pre_books_park`, PARK-AUD-1): Dr Wise AUD 2,303,905.09 + Dr CBA 240,200.44 / Cr 3200 2,544,105.53. **Verified after: Wise AUD residual $0.00**; CBA residual **$2,035.12** = exactly the 13-Aug opening_correction amount → next chase: reconcile the Jan-1 CBA statement figure (107,795.81 per Gaurav) against the imported txn chain (implies 109,830.93); one of the two has a ~$2,035 boundary defect.
- **BR-3 ⏳** Same park still needed for: Wise SGD (1001, residual $812k), Stripe SG (1017, −$604k = ORPHAN stripe/economic-events JEs — different variant, needs its own diagnosis), OCBC 1000 ($936), dormant Wise 1007/8/9, Stripe AU 1019 (−$4.6k). Also: JE attribution gap (machine-created JEs carry posting_user='system', no operator/run id).

### 2.0d Rulebook-lint findings — PARKED work queue for the pre-2026 recon (2026-08-15, read-only scan on clone; NOTHING changed yet)
- **RL-1 Engine gap:** rule `_text_matches` has NO short-token word-boundary guard (the counterparty matcher got it in July, DQ-13; rules never did) — 'RMS'(#171)/'PHOTO'(#162)/'Whale'(#205)/'Apple'(#38)/'Jay'(#17)/'Rahul'(#18) match inside words. FIX: port the <6-char boundary guard + test.
- **RL-2 Mis-book suspects (5), rulings needed:** #124 IBG GIRO → 7000 grants swallowing Income Insurance premiums ($8,040) · #16 'Salary' catching Dirk-Jan Wise transfers $39,648 → 6000 (lender — likely loan repayments; ties to the open Dirk GIRO question) · #17 generic 'Salary' booking Sanjay Kumar $31,715 → 5061 vs default 6000 · #90 customer-receipts→4000 swallowing 28 CPF rows + Honda + an incidental payee · #283 test-charges grabbing FIN-NOR HOLDINGS (default 2410).
- **RL-3 Redundant rules (12) → DEACTIVATE (cp default books identically, kills text-pattern risk incl. PHOTO/Whale bombs):** #111 Fleet Dr · #138 Liberty · #142 Metabase · #162+#164 ND Photography · #169 Randolf · #179 Semrush · #195 Towing · #205 Whale Ron · #377 Bombora · #379 Norman Chan · #18 Rahul · #16-Sarala leg.
- **RL-4 Pin-to-counterparty w/ account conflict, Gaurav to rule:** #42 Alpine (rule 5001 vs default 5003) · #378 CDC (5001 vs 5060) · #374 Insuret incoming→4030 (deliberate direction-split — convert to cp-pinned + keep).
- **RL-5 Healthy:** transfer/IC lanes untouched (own-entity wording legitimate; month-end invariant is their tripwire) · aliases: ZERO duplicates, short ones boundary-guarded · 73/86 active rules are description-only, of which ~15 retire/pin per RL-3/4; the rest genuinely wording-based.
- **Standing gate:** re-run this lint + the rule-vs-default disagreement report before EVERY year batch.

### 2.0b GST vendor-registration sweep — OPEN ITEMS (Gaurav, 2026-08-14)
- **VR-SWEEP-1** Registered so far (clone) as AU-GST vendors so the vendor gate claims them (replaces `gst_claim_by_default`): bank-lane payouts 504 Bombora / 310 Linkt / 659 AUTO & GENERAL / 663 PORT MOTOR / 668 Yarra / 673 FCM, plus 438 SafetyCulture / 634 TPG / 329 Uber AU / 385 Didi / 660 Precision Clearing House / 86 Insuret. Foreign stay OUT (63 Cycle & Carriage = SG; SaaS bucket; ASIC/govt).
- **VR-SWEEP-2 ⏳ ALTIMOBILITY CORP (cp 649) — $71,177 AU spend, UNIDENTIFIED, largest single unregistered vendor. Check what it is (name reads foreign) and rule AU-registered vs foreign. Gaurav: check later.**
- **VR-SWEEP-3** ~$1,085 of H1 bank-lane payouts have NO counterparty attached (AUTOBODIES Gold Coast, Combined Towing, AUTO TOW, individuals) → attach counterparties so the vendor gate can claim/exclude them.
- **VR-SWEEP-4** Bucket rulings still open: B foreign-SaaS (claim only where invoice shows AU GST), E airlines (domestic GST vs international GST-free, per-invoice), C individuals (hosts/contractors — NOT GST), D govt/non-supply (out). Register from invoice evidence, not name.

### 2.0 H1'26 AUDIT FIX QUEUE (4-agent independent audit, 2026-07-27) — work top-down, one at a time

**QUEUE LAW (Gaurav, 2026-07-27): a fix is complete only when BOTH the data AND its source are closed** — every reclass must also fix whatever produced it (counterparty default, rule, corpus row, engine mechanism) or it recurs on the next statement. AUD-1's source closure: rules #372/#373 (POL-32 amount-banded: insurer ≥5k → 5035, <5k → 5036, both outranking party defaults) · defaults stripped from all 7 insurer cps (76 GT, 86 Insuret, 114 NTUC, 144 Tokio, 339 DEFT, 368 Allianz, 658 Hollard) · 2 corpus guidance rows.

Verdicts: currency math 0 violations · double-entry integrity proven · IC clean · CFO: cash layer sign-off unconditional, SG P&L yes-as-mgmt-acct, AU P&L blocked on AUD-1/2/B, external BS blocked on AUD-F. Full narrative in the PENDING QUEUE block above; this table is the tracker.

**Mechanical fixes (me — after Gaurav's go per item):**

| ID | Fix | Impact | State |
|----|-----|--------|-------|
| AUD-1 | ✅ DONE 2026-07-27 — DEFT 69,472.07 reclassed 2500 → 5035 (annual premium via broker, POL-32). BONUS: POL-32 insurance rule (>~5-10k = premium, else excess) applied across H1 — 19 more reclasses 5035→5036 (SG Tokio 3,270×6 + all Income Insurance; AU sub-5k Hollard/GT/Allianz). 5035 now 52,089.63 · 5036 59,793.18; TB ties. Corpus guidance "Hollard trip premium" superseded by POL-32 | AU loss −69.5k | ✅ |
| AUD-2 | ✅ DONE 2026-07-27 — both ATO items (JEs 3677/4956) reclassed 9000 → 2500 (BAS settlement + refund; net 24.2k DR in 2500 pending AUD-C); 9000 now zero. SOURCE: cp 38 ATO default stripped (heterogeneous flows → always review) + corpus guidance row | AU loss −24.2k | ✅ |
| AUD-3 | ✅ DONE 2026-07-27 — GLOBAL TRANSPORT = **GT Insurance** (Gaurav ID): the 4 credits are claim payouts. All 17 insurer claim receipts unified on **4030 Insurance Recoveries** (GT 4 from 6400 · INSURET 11 from 5035 · SG Income claims 2 from 7000; commissions stay 7000; 4030 now 82,377.79 CR; 5035 truthful at 120,485.33). Kept as income line per Gaurav ("for now"). SOURCE: incoming rule #374 (GLOBAL TRANSPORT\|INSURET CLAIM → 4030) · artifact cp 330 retired, alias+txns merged into cp 76 GT insurance · corpus row · FLOW-12 recorded. **Closes the INSURET half of AUD-E too.** | claims cycle now readable | ✅ |
| AUD-4 | ✅ DONE 2026-07-27 — scope was WIDER than audited: 6001 held 3 species. Kept: 5× genuine CPF GIRO −4,393/mo (6001 now clean at 21,965; NB Feb contribution missing — eye later). Reclassed 12: 6 customer receipts (+1,910.64) → 4000 · 6 NGROK subs (−79.26) → 6701. SOURCE: cp 520 poison already purged in the alias sweep; NGROK cp 674 created (single-behavior SaaS, default 6701, alias 'NGROK INC.'); customers stay directory-free (POL-29) | 6001 truthful | ✅ |
| AUD-5 | ✅ DONE 2026-07-27 — all 30 test charges reclassed 6400 → 6702 (POL-23), cp link cleared; TB ties. SOURCE: cp 385 poison purged in the alias sweep; Didi keeps 6400 default (now genuinely single-behavior); rules #283/#284 own the test-charge wording going forward | tiny, systematic | ✅ |
| AUD-6 | ✅ DONE 2026-07-27 — 6× DBS overdraft interest 7002 → 6600; Yifei $10 2400 → 4025; + strays found in the trace: cp 328 FIN-NOR carried poisoned aliases ('Fast Transfer From Yifei Yu…', bare 'DRIVE MATE') → cut; a −35 'DRIVE MATE' charge hijacked into 2400 → 6702 (POL-23). SOURCE: cp 328 typed **investor**, default kept. **BONUS (Gaurav): 2410 Convertible Notes CREATED** — Fin-Nor's 470,430 moved 2400 → 2410, cp default → 2410 (CP-34); 2400 now holds only ordinary loans 874,503.39 pending AUD-D. Corpus row (negative INTEREST = expense). TB ties | financing side truthful | ✅ |
| AUD-7 | ✅ DONE 2026-07-27 — projector (`project_month`) now stamps currency=functional/native=amount/fx_rate=1 on every line; all 584 existing NULL lines backfilled (0 remain); +1 regression assertion in test_economic_events. Suite 643/0 | POL-25 completeness | ✅ |
| AUD-8 | ✅ DONE 2026-07-27 (Gaurav ruling: LEAVE the 358 rows as-is, they're Phase B) — instead of touching data, added a hard ENGINE FLOOR: `BOOKS_OPEN_DATE=2026-01-01`, engine never categorizes pre-open rows even under explicit txn_ids (POL-33). +1 regression test. Rows stay IMPORTED, untouched, reserved for Phase B replay | safety, structural | ✅ |
| AUD-9 | ✅ DONE 2026-07-27 — TYPE-driven direction guard (POL-34, better than account-nature: catches investors too): `_default_direction_ok` blocks a default when money contradicts the type's normal direction (vendor/employee/gov=out, investor=in, bank=both) → routes to review ('[cp-direction-guard]'). Kills the AUD-3/4/6 poisoned-alias-into-wrong-account class structurally. +1 test; suite 645/0 | root-cause fix | ✅ |
| AUD-11 | ☐ **STANDING (Gaurav, 2026-07-28): full counterparty ALIAS AUDIT** — yesterday's purge of 196 poisoned aliases (root cause of the CFO misattributions) was reactive; keep a recurring full audit + API-level write guard (reject >80-char/boilerplate/generic aliases; stop review-panel auto-adding whole descriptions). Task #6. | prevent recurrence | ☐ |
| AUD-12 | ✅ **DONE — POSTED LIVE 100% (Gaurav confirm, 2026-08-02): revenue + cost re-architecture cut over and posted; RA-10 complete.** REVENUE re-architecture (POL-36..41, ENT-7, FLOW-14..22; 2026-07-28) — MAJOR PROGRESS:** agent net-revenue + GMV reporting (design locked) · category set (POL-39: Distance folded INTO base per Gaurav reversal, Fuel split, Fines/Late-Return/Cancellation) · GBV Stripe-only (POL-40; 4000 PayNow leak FIXED→rule#362 to 4025). ✅ **REGULAR P2P/RMS SPLIT POSTED** — connect-account rule (ENT-7: our accts = bank OCBC-1001/CBA-7311; RMS+Flex++caretaker pool), verified to the cent, 12 monthly reclass JEs (SG RMS 85,812 · AU RMS 211,342 incl distance-by-base-ratio FLOW-19); distance events re-pointed 4024→4000 (base). ✅ **SUBSCRIPTION SPLIT VIEWS BUILT** (both markets, 10 MECE views, tie to cent): Flex+→4002 · device→4010(kept) · insurance→4011 · **payment-plan ISOLATED** (double-count fix FLOW-21, AU 45,628) · other. **REMAINING:** wire subscription type-views into events (Flex+/insurance/device; payment-plan→receivable Dr bank/Cr 1200) · retire broad subscription_invoiced views · resolve "Other" bucket · Flex+ P2P/RMS via rego→listing (deferred) · ✅ **INCIDENTALS CHART LOCKED** (POL-43: accts 4020-4031 + 4030 created live; Damage absorbs loss-of-use/breach; Fuel split Charge 4023/Refund 4029 contra; Late-Return 4031 · Penalty 4027 · Infringement 4028 · Platform-Fees 4026 all separate; aligned to IMS 29-code taxonomy) · ✅ **GST LOCKED** cash-basis, posting-time GST-control acct, everything-AU-cash-applicable, agent-net emerges by gross-in−gross-out (POL-44 supersedes POL-38 open; SG zero) · ✅ **SPLIT-AT-SOURCE law** (POL-42: one-to-one view→acct, no logic in finance-api; Regular & Flex+ P2P/RMS reclass JEs must move to views — tasks #7/#8) · ✅ build checklist `wip/REVENUE_SOURCE_MAP.md`. **NEXT:** build SG then AU incidentals accrual views (line-item `description` classifier, Excess-Mileage before Damage; leak-route Flex+/insurance/device out to subscription accts $191k AU) → prune lump/broad views to one-to-one · wire subscription + GST into events · going-forward split at STAGING. Cross-refs: KNOWLEDGE POL-36..44, ENT-6/7, FLOW-13..22. | the big one | ✅ |
| AUD-10 | Net-revenue presentation VIEW (agent model: revenue = take-rate; GBV + host-payout shown as memo) — report layer only, books unchanged | external credibility (F1) | ☐ |

#### 2.0.1 Revenue & Cost Re-Architecture — canonical build registry (children of AUD-12; stable IDs, traceable)

> Each row = one build task, cross-referenced to its governing decision (KNOWLEDGE POL/FLOW/DQ + wip doc) and its session task-tool #. **This table is canonical** (Rule 4); the task-tool list mirrors it. Design spec: `wip/REVENUE_SOURCE_MAP.md`.

| ID | Task | Traces back to | Task# | State |
|----|------|----------------|-------|-------|
| RA-1 | Build **SG incidentals** accrual views + ClickHouse description-lookup table → 4020-4031 | POL-42, POL-43, POL-47, DQ-20, DQ-24, REVENUE_SOURCE_MAP | #10 | 🔄 12 objects BUILT + KEPT (inert, tie-to-cent, parent 255,372.58). RESOLVED: trip-leak→4000 (POL-47); "discount" was MISLABEL = actually TAX → Q1+Q2 merged, SG 9% GST (header 1,238.72 + GST lines 4,453.96) bundled → 4026 Platform Fees (POL-48); reconciliation now closes to the cent. "remaining balance" RESOLVED → 4025 Other (POL-49: manual, no from_invoice link). **ALL SG incidentals decisions CLOSED, ties to cent.** Approved classifier updates to apply (key change, pending go): tax→4026 · trip/distance→4000 · rembal→4025. Optional later refine: Others tail (trip-fees→4000, no-show→4027). AU incidentals (RA-2) NOT yet built. Event-wiring pending approval |
| RA-2 | Build **AU incidentals** accrual views (+ Distance/Gap/Daily → 4000) | POL-42, POL-43, POL-39, POL-47, DQ-27 | #11 | 🔄 analysis DONE, PROPOSE-ONLY (no DDL run); ties to cent (parent 723,926.92 = classifiable 714,700.75 + paginated-hold 9,226.17). Damage=63%. AU GST 55,450 embedded-inclusive→Levy/2510 (not 4026). Leak H1 tiny (~$12k, DQ-27). Q-AU-4 127 paginated invoices RESOLVED → `paginated_hold` view→4025, tmpl 138 (DQ-36). Remaining: Q-AU-3 GST gross/net (→Levy 2510, D2). All other AU incidentals decisions closed; ties to cent. |
| RA-3 | **Leak-routing** — subscription-type lines out of incidentals → 4002/4010/4011 (~$191k AU) | DQ-20 | #12 | ✅ (posted in the 2026-08-02 cutover) |
| RA-4 | **Regular P2P/RMS** split → views (retire reclass JEs) | POL-42, FLOW-18/19, ENT-7 | #7 | ✅ (posted in the 2026-08-02 cutover) |
| RA-5 | **Flex+ P2P/RMS** split → views | POL-42, POL-61, DQ-35 | #8 | ⏸ **PARKED** (Gaurav, 2026-07-29, POL-61) — do later. All Flex+ (both feeds per DQ-35: subscription view + `_LEAK_FLEXPLUS`) stays in 4002; 4003 empty. Blocker: mechanism map 358/1,999 hosts. Split must cover BOTH feeds. |
| RA-6 | Wire **subscription** type-views into events + retire broad views + resolve "Other" | FLOW-20/21, DQ-36 | #14 | ✅ device/insurance/flex wired (DQ-34); subscription-Other→4025 wired (SG 2,119.52 + AU 9,141.10, tmpl 136/137); payment_plan excluded (FLOW-21). AU paginated hold (9,226, tmpl 138)→4025 (DQ-36). **Remaining sub-item:** retire broad `subscriptions_invoiced` views (deactivated, pending prune RA-9/#15). |
| RA-7 | **GST engine** — posting-time control-account + direct-expense GST map + return | POL-44, DQ-25 | #13 | 🔄 found dormant engine (1350/2500, SG=9% — contradicts POL-44); reconciling to single 2510 AU-only control acct + hooking into project_month; build+tests only, live H1 gated on AUD-C |
| RA-8 | **Cost-of-Sales arc** — host + incidental payout views → 5xxx (audit+rebuild) | POL-45/50-57, DQ-21/22/23/26/29/30/31 | #9 | 🔄 **STREAMS 1-3 DONE** (host-payout cash mechanics — the hard part): S1 host cost=net payable, platform_earning=commission revenue (POL-54); S2 P2P→5000 wired; S3 5000-3 renamed "Host Trip Earnings", 5001=owner-only (element-3 + rules 377/378/379/40, A$221k), internal RMS→Connect banks 1018/1020 via `connect_internal_transfer` event (Dr Connect/Cr Platform, no P&L), $1000-rule outflow, connect recon ties to cent, 2130 dropped, tmpl 29 retired (POL-55/56/57, FLOW-23-26). POL-53 filter EXECUTED across 40 views (SG 5000/5001 −S$4.07M phantom). Nothing posted live. **CLOSED (Atlas, DQ-39):** 5050 AU host_discounts wired (tmpl 141, H1 A$0 — never existed; SG tmpl 94 −S$19,858.20) · misc residual/corrections/carve-outs all confirmed-wired · fuel + 5037 confirmed (SG 5023 90,140.28 · AU 5023 15,875.23 · AU 5037 21,791.27) · 5025→5042 by-design · dead tmpl 47 deactivated (all active cost tmpls now resolve to a view). Retire candidates listed for RA-9. **Review-round-2 (DQ-40):** host-earnings PERIOD BUG fixed — 5000 P2P + RMS internal-transfer clearing re-periodized to trip-end accrual per POL-62 (exact p2p reconciliation, DQ-41); RMS-owner + Flex+ stay created-date. **STREAM-1 reconciliation DONE (DQ-42/43):** OLD posted H1 → NEW views ties (revenue clean; cost clean after POL-63 null-guard applied to 31 views — recovered SG flexplus 15,115.16 / SG sub-collected 6,413.03 / AU misc_charge −20,341 / AU referral 3,850). Residuals by-design (referral 'error' excl, cancelled-refundable excl). **Stream-2 nomenclature DONE (POL-65):** 125 used views renamed `view_`→`v_` (verified, view_map repointed, 0 missing); held-funds accts 1021/1022 built + deposit/verification templates repointed (POL-64). **REMAINING:** step-2b consolidate view docs into KNOWLEDGE + purge stale · retire 44 unused legacy `view_` · DQ-46 distance-by-ratio→4001 decision · Stream-3/RA-10 = cutover (posting gate). |
| RA-9 | **Prune** views to one-to-one with chart (after RA-1..8) | POL-42 | #15 | ☐ |
| RA-10 | **CUTOVER / posting gate** — reverse old-world posted H1 (350 events, DQ-37) + re-stage/re-project under new active templates so split accounts populate & lumps zero | DQ-37, DQ-38 | — | ✅ **DONE — cutover posted 2026-08-02 (Gaurav confirm).** Live ledger now carries the re-architected split accounts; old lumped H1 reversed. |
| RA-rem | **Gaurav's account-file remarks (DQ-33)** | DQ-33/38/39 | — | ALL CLOSED barring parked: #1 verification→2110 ✅(DQ-38) · #2 connect `_c_` rename ✅(DQ-38) · #3,4,6,8,9 ✅ · #5 4002 ✅ / 4003 ⏸parked(POL-61) · #7 5055-split ✅(DQ-39, tmpl 139/140) |

Adjacent canonical (already tracked elsewhere): RAG wire-in → §2.2 item 3a (task #16) · alias audit → AUD-11 (task #6) · AU Stripe gap → §2.4 (task #5).

**Rulings needed (Gaurav) — each unblocks work above or the sign-off:**

| ID | Ruling | Exposure | State |
|----|--------|----------|-------|
| AUD-A | Gross-GBV vs agent-net presentation for external statements (pairs with AUD-10) | optics ~2-3× revenue | ☐ |
| AUD-B | AU receivables 587,981 AUD (50% of H1 billings uncollected, guest recharges) — provisioning policy (30-50% ⇒ 175-295k AUD extra loss) | CFO's biggest objection | ☐ |
| AUD-C | GST absent from 2026 books — accrue (~100-140k AUD + SG 9% exposure) vs formally disclose; also the IRAS 3,262 item | blocking for credibility | ☐ |
| AUD-D | ✅ MOSTLY DONE 2026-07-27 — lenders identified (Gaurav): J ter Horst + all "DJ" = **Dirk-Jan ter Horst** 741,230 AUD (cp 268); 71,520 USD = **Gaurav's** director loan (cp 260). New account **2405 Related-Party/Director Loans** = 832,503.39; Fin-Nor stays 2410 convertible. 42k "Misc" = Drivemate platform money (507141), NOT a loan — VOIDED to review. 42k RESOLVED (Gaurav): a Stripe Connect **customer-deposit** sweep → booked 2110 Customer Deposits Held (FLOW-13), NOT a loan. **OPEN: terms/interest on the director loans (docs).** TB ties | financing attributed | 🔄 |
| AUD-E | Confirm mappings: INSURET claim receipts netted vs 5035 (or move vs 5021/other-income) · monthly discount credits (5050) from events lane | presentation | ☐ |
| AUD-F | Non-cash placeholder openings from QB Dec-31-25 TB (already-agreed plan; unblocks depreciation + external BS) — say go | BS position-completeness | ☐ |

### 2.1 Financial reporting last-mile — GAP, highest leverage

| Item | Status |
|------|--------|
| P&L report | ✅ Built 2026-07-26 (A-7 v1, entity + consolidated-USD) |
| Balance Sheet | ✅ Built 2026-07-26 (retained-earnings-system roll-up, imbalance surfaced) |
| Cash-flow statement | ✅ Built 2026-07-26 (direct method, counter-account bucketing) |
| Account ledger / register drill-down | ✅ Built 2026-07-26 |
| Business-Line Margin report | Not built (§5.4) — Phase C candidate |

### 2.2 Categorization engine — rebuild as a confidence/dependency cascade

**Design principle (recorded in `IDEAL_STATE §3`):** deterministic → enrichment → counterparty-dependent → RAG-grounded AI → human; a classifier runs **before enrichment iff its conditions are counterparty-independent**. Goal: AI never runs blind or before deterministic rules.

**Cascade work, in order:**

1. ✅ **Tier-1: move `INTERNAL_TRANSFER` rules ahead of enrichment** (Phase 0.5, before Phase 1). Deterministic + counterparty-independent → claim transfers up front so they (a) never get a wrong external-party counterparty and (b) never reach the L3 LLM. Fixes the "Dom Drive lah on a Stripe settlement" class of bug at the root (no write-then-delete). *Done 2026-05-22 — `test_categorization.py::test_internal_transfer_claimed_before_enrichment`; full suite 573/0.*
2. ✅ **Phase-structure review (DONE 2026-05-23)** — route map: `visuals/CATEGORIZATION_ROUTES.html`. Conclusions: **order is sound** (specific/knock-off → generic; enrichment correctly placed after transfers, before the cp-dependent routes); **do NOT do the fuller split** — running generic rules early would let them beat knock-offs (only transfer rules are safe early). **One gap found + fixed:** payroll lacked AP's retroactive knock-off, so a salary paid *before* its run exists got double-counted when the run posted → added `payroll_service.run_retroactive_knockoff` (triggered by `submit_run`): re-opens premature salary/CPF payments + links them to the run (voids the wrong JE). Test: `test_payroll.py::test_retroactive_knockoff_reopens_premature_salary`. Critical for the historical reconciliation.
3. **AI = RAG (genuine embeddings), not fine-tuning** (the centerpiece). ✅ **Core built (2026-05-23) — `src/services/categorization_rag.py`:** pluggable `Embedder` (deterministic `HashingEmbedder` default, no deps; neural provider swaps in via the same interface — "decide later"), `build_corpus_from_gl_csv` (QuickBooks GL → labelled description→account corpus), `CategorizationRetriever` (brute-force cosine top-k + similarity-weighted majority-vote suggestion). Tested (`test_categorization_rag.py`, 5) + **proven on the real AU GL: 1,776 labelled entries, 66% leave-one-out top-5 accuracy with just the hashing embedder** (a neural embedder lifts it; the misses are token-hashing's weak spots). **Remaining to close RAG:**
   - **(a) Wire into Phase 4D / Route 7** — inject the retrieved examples + company facts into the AI prompt so the LLM stops running blind.
   - **(b) Production neural embedder** — pluggable, deferred (local sentence-transformers / Voyage / OpenAI — decide later).
   - **(c) Full corpus** — load all entities + full QuickBooks history (have AU ~Q1 2026 so far; need SG, Ventures, 2019→now).
   - **(d) Feedback loop** — every confirmed categorization appended to the corpus.

**Correctness fixes:**

4. ✅ **Cross-entity internal-transfer IC codes (2026-05-23)** — now uses the receivable/payable PAIR via `_get_ic_codes` (same convention as allocation/AP; confirmed by Gaurav — "always A"). SG-pays-AU → SG `Dr 8000` receivable / AU `Cr 8110` payable. Test: `test_cross_entity_allocation.py::TestCrossEntityInternalTransfer`.
5. ✅ **`intercompany_group_id` persistence — VERIFIED NON-ISSUE (2026-05-23).** The callers (`_apply_rule`, `match_transaction`) `db.commit()` *after* creation, so the post-create group_id persists; the 16 cross-entity tests (incl. `test_creates_paired_jes_with_ic_group`) confirm both halves share a non-None group id. Earlier audit over-flagged it.
6. ✅ **Payroll knock-off entity preference (2026-05-23)** — `_try_payroll_knockoff` now sorts candidate runs same-entity-first, so a same-entity run wins over a coincidental cross-entity amount match (cross-entity still supported when no same-entity run matches). Test: `test_payroll.py::test_payroll_knockoff_prefers_same_entity`.
7. ✅ **Knock-off `except` blocks log at ERROR (2026-05-23)** — AP + payroll knock-off failures (always unexpected; "no match" is normal control flow) now log at ERROR with trace, so code bugs surface instead of hiding as warnings (cf. BUG-1). *(Per-JE `db.commit()` non-atomicity remains — architectural, lower priority.)*

> **Correctness fixes COMPLETE** (2026-05-23): AP fixed · transfer pairing sound · cross-entity allocation + transfer both use IC recv/payable pair · payroll entity-preference + except-logging · intercompany_group_id verified fine. **Remaining to close the engine: (1) phase-structure review, (2) the RAG pipeline.**

### 2.2.1 Reconciliation data on hand (inventory as of 2026-06-01)

Raw data lives in `documentation/wip/qb_ledgers/` (QuickBooks GL) and `documentation/wip/bank_statements/` (PDF statements). **3 real entities** (Fleet entities folded in — see §3 decision).

**QuickBooks General Ledger — ✅ COMPLETE for all entities** (the categorization corpus + the authoritative "answer key"):

| Entity | GL span | Notes |
|--------|---------|-------|
| Drive lah Pte Ltd (SG) | 2019-06 → 2026 | 4 overlapping export windows (dedup on load); **+ Drive lah Fleet folded in** |
| Drive lah Ventures Holding (SG) | 2019 → 2026 | all-time |
| Drive lah Australia (AU) | 2022 → 2025 | all-time CSV; **+ Drive mate fleet folded in** |

**Bank statements — partial. ⚠️ Adapter reality (verified A-0, 2026-07-25): only `dbs` parses the real PDFs; `ocbc` and `cba` PDF paths yield 0 rows silently (fix = A-12).** All statements on hand are PDFs:

| Account | Entity | Have (verified on-disk 2026-08-02) | ❌ Missing (user to supply) |
|---------|--------|------|------------------------------|
| OCBC 1001 (713147601001) | DL Pte Ltd (SG) | Feb 2020 → Dec 2024; **2025 ALL 12 RECEIVED** (2026-08-02); Jan–Jul 2026 | **Jun-2019→Jan-2020** (→ QB fill); Aug 2026 |
| OCBC 3001 (588154393001) | DL Pte Ltd (SG) | Jan 2023 → Dec 2024; **2025 ALL 12 RECEIVED**; Jan–Jul 2026; partial 2020/2021 + Apr-2022 | **2019/2020/2021/2022 gaps** (→ QB fill); Aug 2026 |
| DBS (072-669493-3, SGD+USD) | Ventures Holding | Jun 2022 → Oct 2025 continuous + Jan–Jun 2026 — account OPENED mid-2022 (DQ-75), no earlier gap | **Nov–Dec 2025** (Gaurav to supply); **Jul 2026 → now** |
| CBA (06-2246-10347311) | DL Australia | Sep 2022 → Sep 2025 (quarterly, continuous); **Q1+Q2 2026** (20260330, 20260630) | pre-Sep 2022 (fill from QB); **Oct–Dec 2025 quarter (20251230)** (Gaurav to supply); post-Jun 2026 |

**GAP-FILL POLICY (POL-82, Gaurav 2026-08-02):** old pre-2023 gaps → source the **bank txns from QuickBooks** (bank-txn lines, not JEs); **2025+ gaps → Gaurav supplies statements**. Per account: OCBC-1001 Jun-2019→Jan-2020 from QB · OCBC-3001 all 2019–2022 gaps from QB · CBA 2021–2022 from QB · DBS no historical gap (DQ-75). Account-open anchors: OCBC Jun-2019, CBA Aug-2021, DBS Jun-2022. **Folders reorganized 2026-08-02** to `bank_statements/<ACCOUNT>/<YEAR>/` (180 files, count-conserved; the 6 misfiled 1001-2026 statements moved OCBC_3001→OCBC_1001). Coverage artifact: `wip/statement_coverage/coverage_ALL.html` (regenerated from live re-scan 2026-08-02, 207 files). **UPDATE 2026-08-02: OCBC 2025 statements RECEIVED for both accounts** (+ Jul-2026 + a stray 3001-Apr-2022), filed into folders. **ALL 2025 statements now received across all four accounts** (DBS Nov–Dec 2025 filed last; 213 files total). **No outstanding statement requests** — only the current month (Aug-2026) is open, and pre-2023 fills from QB bank txns per POL-82. CBA also holds a Jul-2026 monthly interim (`CBA/2026/CBA_TransactionSummary_2026-07_(monthly-interim).pdf`; confirms CBA opened 12-Aug-2021). Root clean.

**Net:** the **bulk of 2020–2024/25 is reconcilable now**; the main hole is the **recent ~12–18 months** (all SG accounts) + OCBC-3001's 2022. QB GL has zero gaps.

### 2.2.2 Bulk historical import + `IMPORTED` decoupling (enabler for the 6-year reconciliation)

The historical reconciliation needs raw data loaded *without* auto-categorizing, then categorized deliberately in the right order (invoices→accruals first, then bank knock-off). To do:

1. ✅ **`IMPORTED` status + decouple import (DONE 2026-05-23)** — enum value added (no migration), `import_file`/`import_from_rows` gained `auto_categorize` (False → stage as `IMPORTED`, no run), the import route honours `auto_categorize=false`, and `run()` picks up `IMPORTED` + flips the processed batch → `PENDING`. Tests: `test_import_staging.py` (3). See §3 decision.
2. **Bulk-import the full bank history** → `IMPORTED` (all entities, 2019→now). *(needs the data + the import mode above)*
3. **Bulk-approve historical invoices** → creates the AP accruals at scale (no per-invoice clicking). Invoices already stage at `DRAFT` (§3).
4. **Run categorization** on the staged bank txns → AP knock-off settles the invoices; rest categorized. Pairs with the rules/counterparty/RAG analysis (§2.2.3 + the QuickBooks GL data).

### 2.2.3 Stage-1 corpus + rule mining — `CorpusMining` skill (built 2026-06-01)

The "analyze the GL before categorizing" step is now a **re-runnable project skill**: `.claude/skills/CorpusMining/` (SKILL.md + `Buckets.md` + `DataLayout.md` + `Workflows/{MineBuckets,PromoteBuckets}.md` + `Tools/MineBuckets.py`). It decomposes every **bank-section** GL line into 7 handling buckets — A vendors · B rule-candidates · C transfers/IC · D AP settlements · E accrual/depreciation (excluded) · F revenue/Stripe boundary · **G RAG residual (the corpus, only what's left after A–F)** — and writes them to `documentation/wip/reconciliation/` (`_aggregate/` + `per_entity/` + `README.md` index).

Handles **two GL shapes** (AU GL-CSV with a `Split` contra; SG/Ventures grouped xlsx → contra read from `Journal.xlsx` double-entry blocks), folds the mock Fleet entities into their parent, dedupes overlapping SG windows, and **diffs A vs the live `finance_counterparties` export and B vs the live `qb_rules` export** to flag NEW items.

**First full run (all 3 entities, 72,837 bank records):** A=776 vendors · B=346 rule-candidates (≥90% purity, ≥5 hits) · C=27,394 transfers/IC · D=2,023 AP · E=138 accrual patterns · F=21 revenue patterns · **G=6,944 corpus residual**. Already surfaced data-quality findings (SG Stripe payouts booked to "Uncategorised Income"; Ventures inflows = investor share capital). **Buckets A (counterparties) + B (rules) fully resolved — final artifacts + state in §2.2.4 (S-1…S-3). Next:** persist the G corpus into `CategorizationRetriever` (S-4), then wire RAG into Phase 4D (§2.2 item 3a).

**Scope:** `CorpusMining` = **Stage 1 only** (read-only GL analysis, runs per-entity + aggregate; reads the ledger, NOT the bank statements — the GL already carries each bank line's shape + COA translation). **Stage 2 (the reconciliation *execution*) — structure now DECIDED 2026-08-15 = POL-124 / §2.0c / `wip/HISTORY_RECON_PIPELINE.md` (supersedes the 2026-06-01 "decide later").** ⚠️ **Stage-2 requirement (confirmed):** before relying on the Stage-1 rules/corpus, **cross-check raw bank-statement descriptions vs the GL `Memo`** the corpus was trained on — catch QuickBooks-renamed descriptions (drift) so the rules actually fire on import text. Also do a bank↔GL coverage diff (every bank line recorded?).

### 2.2.4 Historical reconciliation — workstream state (canonical; business facts live in `KNOWLEDGE.md`)

| # | Item | State |
|---|------|-------|
| S-1 | **COA bridge** (238 QB labels → COA v2) | 140 resolved (June wave audited: 94% clean, 2 fixed; all high-impact labels approved incl. Fleet family, RMS decision); 71-row low-usage tail parked; 2 labels deliberately "review at replay" (R&D-Software, Gross-Other-Income). Sentinels: `TRANSFER` / `SPLIT` / `ENTITY-DEPENDENT`. |
| S-2 | **Counterparties** | ✅ **CLOSED 2026-07-23, extended 07-24** — `wip/reconciliation/seed_counterparties_FINAL.csv` = **557 rows / 535 active** (post identity-harvest + red-team audit + Gaurav's 14-decision walk of rule-name identities: +60 rule-derived aliases, 41 parties created incl. RMS individuals/support team/photographers/SaaS vendors, CircleCI merge, dead-alias fixes, new COA account 6004 Staff Health Insurance); manifest **287 insert · 61 enrich · 27 correct · 160 no-op · 22 deactivate**; both-way live-DB capture verified (0 live records missing); gates green (codes ∈ COA v2, names paired from COA, zero alias collisions). 6 Gaurav review passes; per-entity provenance files beside it. |
| S-3 | **Rules** | ✅ **REVIEW COMPLETE (2026-07-24)** — all 58 walked one-by-one. **Final rule book = 67 rules**: 21 transfer (incl. 1 new mined Stripe-settlement) · 39 pure-text · 4 kept exceptions (apple.com→6701, GSuite/Play/Storage→6701) · 2 party-no-default · vs **178 live rules → INACTIVE** (171 redundant-to-defaults, 6 superseded by arbitrated defaults, 1 identity-only). Many walk answers flipped PARTY defaults instead (POL-13 both ways). Final artifacts: **`wip/reconciliation/rule_book_FINAL.csv`** (81 physical / 67 logical rules, identity stripped, pipes split) + **`rules_deactivation_FINAL.csv`** (178 → INACTIVE) + `live_rules_manifest.csv` (per-rule fates). At apply: deactivations + strip counterparty actions (POL-12/17) + pipe-split rewrites (DQ-9) + entity-scoping pass (DQ-7). |
| S-3a | **Rules engine change** | ✅ **DONE 2026-07-24** — `_apply_rule` no longer honors `counterparty_name/type` actions (POL-12); `RuleCreate`/`RuleUpdate` reject them with a POL-12 error; `_text_matches` treats `'x \| y'` patterns as OR-alternatives (fixes the 53 dead piped rules). 3 new tests; suite 587/0. Remaining: the column-drop migration (with apply). |
| S-3b | **Entity-mismatch guard (queued — Gaurav: keep, don't act)** | When a matched party's entity-scope ≠ the transaction's entity, never book silently — route to IC allocation or review. |
| S-4 | **Corpus** | ✅ **corpus v2 BUILT + VALIDATED 2026-07-24** — `wip/reconciliation/corpus_v2/` (v1 G-files preserved). 6,962 → **781 kept** (3,613 party-covered + 545 rule-covered = determinism ate the corpus; 1,137 SPLIT-target; 501 no-text; 339 unapproved-label — recoverable via the bridge tail). 41 target codes. **Leave-one-out: top-1 69% / top-5 82%** (hashing embedder — up from 66% pre-cleanup). Remaining: wire into Phase 4D (retriever load + prompt injection + KNOWLEDGE company-facts). |
| S-5 | **Apply to DB** | ✅ **APPLIED TO LIVE 2026-07-24** (Gaurav's call: live DB, **finance tables ONLY** — allowlist guard enforced in code). Executed: 285 cp inserts · 64 enrich · 13 corrections · 18 deactivations · 178 rules → INACTIVE · 11 identity-strips · 1 new transfer rule · COA 6004. **Post-verify: 67 active rules · 514 active counterparties · 0 active rules with identity actions · 6004 present**; founder record re-scoped global per CP-1. Tool: `Tools/ApplyReconciliation.py` (dry-run→backup→transaction); backups: `wip/reconciliation/backups/20260724-*/`. Remaining from apply scope: POL-17 column-drop migration (deferred, needs alembic on live) · rule entity-scoping pass (DQ-7, queued). |
| S-6 | **Data gaps (Gaurav to supply)** | Recent ~12–18 mo bank statements (all SG accts) + OCBC-3001 2022 · booking-level RMS data (FLOW-6) · a non-prod DB. |
| S-7 | **Deferred** | Raw-bank-statement pattern mining (aliases from true bank text + bank↔GL coverage diff) · live COA drift 155 vs 135 (DQ-11) · live artifact-vendor cleanup (DQ-8). |

### 2.3 Payroll — make it real

Engine verified (mock SG CPF + AU Super/PAYG → balanced JEs) and onboarding now wires comp + deductions. **Live DB: every `hr_*` table + `finance_payroll_runs` = 0** — built but never run; 81 employees exist only as counterparties; the roster CSV's salary columns are **blank**. **The one blocker: source/fill each employee's salary + deduction data**, then onboard a pilot and run one real payroll. Triggers + FE wiring: §4.2. Canonical service: `hr_payroll_service` (§3). **For the historical reconciliation:** runs created after the fact now auto-correct already-categorized salary payments via the retroactive payroll knock-off (§2.2) — no double-counting when out-of-order runs are posted.

### 2.4 Stripe sync — finish (needs ClickHouse access)

Views-based adapter is restored + clean (`sync_month` → `_generate_all_je_specs` over 25 view-backed JE methods → ledger). **To do:** E2E-verify against live ClickHouse; patch the `code='2'` view gap (excess mileage, ~SGD 14.8k/2025); rewrite stripe_sync tests against the views path (old WIP tests removed; in `stash@{0}`). Deferred: Platform↔Connect views, RMS split, historical backfill, production schedule. *(Future: the source-adapter abstraction is deferred (YAGNI) until the TMS PGW ledger is real — see §3.)*

### 2.5 Period close, GST returns, consolidation — GAP

| Item | Status |
|------|--------|
| Period close / lock (A-9) | Not built — posted periods remain mutable; gated on recon staying green through the bulk-approve sweep |
| GST return summary (output − input) + clearing JE (A-6) | Not built |
| Multi-entity consolidation: IC elimination + FX → USD | ✅ Built 2026-07-26 (always-USD, dual manual rates, eliminations + residual reported) |
| Test categorization vs real data | ✅ Effectively done via the H1 live run + review walk (2,994 matched, queue 375→1) |

### 2.6 Technical debt

| Item | Status |
|------|--------|
| Under-tested modules: depreciation, payroll, invoices/AP, reporting | Add coverage |
| Categorization engine is a 2,022-line god-object; AI called inline in the run | Consider splitting (low priority) |
| Dead-code candidate kept: `wise_service.get_business_profile` (singular) — script-only | Leave / verify with script owner |

---

## 2.7 Vendor Payment Reconciliation — per-vendor Statement of Account (MEGA-TASK, Gaurav 2026-08-02)

**Owner:** dedicated agent (separate, single-purpose, persistent). **This section is canonical** (Rule 4/7); the agent's own working doc mirrors it.

**Ideal end state (the bottom line):** for every vendor we can state **true outstanding** — what we owe, what we've paid, what remains — as a **Statement of Account** the ops team can act on. ~1,900 invoices in the system today.

**The identity that must close, per vendor:** `Opening AP + Σ(invoices owed) − Σ(payments made) = true outstanding`. This runs on the EXISTING invoices module (invoices already carry counterparty + amount_paid); it does NOT need the incident sub-ledger (STEP-1/2 of FINANCIAL_LEDGER_PLAN). It IS the IT-6/STEP-8 vendor-reconciliation leg standing alone.

**Method (locked, Gaurav 2026-08-02):** load ALL bank transactions from 2019 (we HAVE the statements: OCBC-1001, OCBC-3001, CBA-7311, DBS; Stripe fully syncable) as IMPORTED, nothing posted. Preliminary auto-match every invoice flagged provisionally-paid against a real loaded payment. Confident matches → auto knock-off → **post that batch (Gaurav-blessed) → close those invoices.** Expected ~60–70% auto-closed. The residual ~30% (no confident match in our system) is **operator-gated review** — there is no other way for the residual, by design. We minimise it (fuzzy amount/date/ref, Stripe metadata, partial/FX-aware) and pre-triage so the operator sees the best-candidate match, not a blank.

**Ideal-State Criteria (verifiable, hard-to-vary):**
- ISC-1 — every one of the ~1,900 invoices sits in a terminal disposition: PAID / APPROVED-payable / VOID-with-reason. Zero in limbo. *(Falsifier: any invoice still draft/stub/undated.)*
- ISC-2 — every loaded vendor payment is matched to invoice(s) or explicitly marked unallocated. *(Falsifier: an unexplained payment.)*
- ISC-3 — Σ per-vendor outstanding = the GL **2000 Accounts Payable** control balance, to the cent. *(Falsifier: SoA total ≠ GL AP.)*
- ISC-4 — an ops-consumable Statement of Account per vendor exists (exportable), each line drillable to invoice + payment.

**Guardrail:** agent works propose-first; ALL postings Gaurav-gated (batch approvals). Recon read-only until blessed. Live DB inspected read-only per §4.0.

| # | Subtask | State |
|---|---------|-------|
| VR-0 | Ground truth: inventory ~1,900 invoices (status/date/dup/counterparty coverage) + confirm bank-statement availability per account back to 2019. Size the gap. | ☐ |
| VR-1a | **Parse-audit (READ-ONLY, no DB):** run every held statement through its adapter; verify per-statement balance chain (opening + Σ = closing) + cross-period continuity (prev close = next open); DBS per-currency. Report clean/fail/row-drops per statement + distinct-format inventory. Highest-risk: old 2020–22 OCBC PDFs + CBA monthly "TransactionSummary" format. | ✅ **DONE 2026-08-02** — 213 files / 279 rows, **269 PASS / 10 FAIL (96.4%)**, **ZERO true cross-period chain breaks**. OCBC 1001+3001 100% (incl. 2020–22 vintage). CBA 28/29. DBS 110/119. Report: `wip/reconciliation/parse_audit_2026-08-02.{csv,md}`; script `scripts/vr1a_parse_audit.py`. |
| VR-1b | Fix adapters for every flagged format; re-audit until 100% of held statements reconcile. **10 FAILs, 3 fix classes:** (1) DBS `_parse_line` column-collision on telegraphic-transfer rows (8 files, DQ-76) — the meaty one; (2) CBA monthly TransactionSummary period-header regex (1 file); (3) an `.xlsx` in the DBS folder fed to the PDF parser (1 — route/exclude). NOT adapter bugs: OCBC_3001 Jul-2021→Mar-2022 "missing" = pre-2023 QB-fill per POL-82 (no statement to source); CBA 2026-03-30 duplicate with conflicting balances = data-integrity item to adjudicate. | ✅ **DONE 2026-08-02 — audit now 278/278 = 100%** (re-verified in MAIN checkout, not just worktree). DBS root cause was a MISDIAGNOSIS (multi-page section anchor overwrite, not column-collision — DQ-76 corrected); fix = 1 guard in `dbs_pdf.py`. CBA monthly interim got a `_parse_interim` branch; redundant `.xlsx` excluded. +2 regression test files; adapter tests green. Only non-passes = the 2 out-of-scope items (POL-82 QB-fill gaps + CBA dup). |
| VR-1c | Load all parsed txns as IMPORTED (auto_categorize=False), fingerprint-deduped, idempotent. **LOAD TARGET (Gaurav 2026-08-02): live finance tables + pre-load backup** (reversible — IMPORTED rows carry no JEs; delete to roll back). | ⛔ **ATTEMPT FAILED + FULLY ROLLED BACK 2026-08-02 (incident, POL-83).** A detached background loader OOM'd on large files, mis-mapped ALL OCBC → catch-all acct id1, and auto-respawned; polluted `finance_transactions` twice (~13–22k pre-2026 IMPORTED rows). **JE/JL NEVER changed (4420/8854)** — ledger untouched. Cleaned: killed + neutralized the loader script + `TaskStop`ed the agent, restored via backup, then purged pre-2026 non-reconciled per Gaurav → DB clean at **3,884** (2026+ 3,880 intact + 4 pre-2026 kept: 1 RECONCILED + 3 ledger-linked Dec-31-2025 legs). Backups: `backups/finance_transactions_pre_vr1c_20260802-145741.csv` + `_pre_pre2026purge_*.csv`. **RETRY PRECONDITIONS (POL-83): resolve OCBC acct mapping (DQ-77) · fix/plausibility-check OCBC 2024 over-parse (DQ-78) · foreground+supervised+single-run-locked · memory-safe batched · no background agent.** **PARTIAL RE-LOAD DONE 2026-08-02 (correct, POL-83-compliant): OCBC-1001 (ba1) PDF slice Feb-2020→Dec-2025 loaded** — foreground/supervised, mapped to ba1, IMPORTED-only, backup `backups/finance_transactions_pre_ocbc1001load_20260802-154939.csv`. **38,934 rows created, 1 deduped; all 71 months present; 38,933/38,934 chain to the cent; JE/JL unchanged (4420/8854).** **OCBC-1001 (ba1) NOW FULLY LOADED inception→Dec-2025** (2026-08-02): 634 QB bank txns Jun2019→Jan2020 (`source=qb_gapfill_2019`, JEs excluded, 0 chain breaks) + 38,934 PDF Feb2020→Dec2025 + 977 existing H1'26 = **40,545 rows, span 2019-06-06→2026-06-30**, JE/JL unchanged, all IMPORTED. **Both recon items now RESOLVED 2026-08-02:** DQ-79 ✅ (restored missing −21,660 dispute row; Jan-2022 sums to statement to the cent) · DQ-80 ✅ (the 21,403.60 was the account's real 06-Jun-2019 opening — booked opening entry + shifted QB balances; QB Jan-31 close = PDF Feb-open 223,820.89 exactly; OCBC-1001 chains continuously 2019→2026, 0 breaks). ba1 now **40,547 rows**, JE/JL unchanged. **ALL BANK ACCOUNTS + WISE NOW LOADED 2026-08-02 (supervised, IMPORTED-only, JE/JL never moved at 4420/8854):** OCBC-1001 (ba1) 40,547 · OCBC-3001 (ba18) 571, QB+PDF, seam ties 6,700.31 · DBS (id13/15/16) 387 multi-ccy PDF, Jun2022→ (account opened mid-2022) · CBA (ba17) 7,698 rows, 2021-08→2026-06, ALL real statements, **continuous no-gaps** (QB slice removed; Gaurav supplied full 2021-22 statements incl. Oct-Dec 2021; adapter fixed DQ-82; every seam reconciles to the cent) · Wise all 11 balances (9,262 rows, VR-1e). REMAINING: **Stripe only** (deferred per Gaurav). | ✅ all non-Stripe accounts loaded |
| VR-1d | QB gap-fill: pre-2023 gap months (OCBC-3001 2019–22, OCBC-1001 2019, CBA 2021–22) from QuickBooks bank txns (POL-82) → IMPORTED, `source=qb_gapfill`. | ☐ |
| VR-1e | Wise + Stripe full-history API sync → IMPORTED. **WISE DONE 2026-08-02:** all 11 Wise balances backfilled 2019→today in 180-day chunks (foreground, IMPORTED-only, deduped, backup `pre_wisebackfill`), **9,262 new rows, JE/JL unchanged**. Earliest Wise data May-2020 (id2 SGD 7,095, id11 AUD 3,649, id7 USD 216, +smaller; id6 PKR empty). Balance-statement endpoint = settled movements only; no status filter (only currency-mismatch + zero-amount skipped). last_synced stamped today so incremental button works. **Stripe still deferred per Gaurav.** | 🔄 Wise done, Stripe pending |
| VR-1f | Completeness proof: per account, loaded ledger running-balance chains account-open→today with zero breaks + ties to each statement close. = the ISC that "everything is in and correct." | ☐ |
| VR-2 | Obligation hygiene: resolve 368 stubs (real date or void), void 61 duplicates, ensure every keep has counterparty+COA+amount+date → promotable payable. | ☐ |
| VR-3 | Preliminary auto-match: invoice (provisionally-paid) ↔ loaded payment, tiered confidence (amount+vendor+date+ref, Stripe metadata, partial/FX). | ☐ |
| VR-4 | Post the confident knock-off batch (Dr 2000 / Cr bank) after Gaurav bless → invoices PAID. Target ~60–70% auto-closed. | ✅ **708 POSTED + VERIFIED 2026-08-03 (Gaurav-blessed, ±1% accepted).** The full clean set (same-entity, same-currency, COA, payment=invoice within 1%) posted foreground: each invoice approved → bill JE (Dr expense / Cr its payable, GST per POL-87), knocked off → payment JE (Dr payable / Cr bank), both POSTED as `system`, match→logged. Verification on live ledger: **708 live posts, 0 double-booking** (every Case B prior direct-expense JE VOIDed — verified), **0 internally-unbalanced JEs**, AP fully cleared on 417 / small ≤1% residual on 291 (max $145.60 on a large invoice). Reversible: per-invoice backups `wip/post_backup_*.json` + `scripts/vr2_reverse_posting.py` (round-trip proven on inv 321/400). Invoice 321 (43% amount-gap) correctly EXCLUDED → review. Engine `scripts/vr2_post_pairings.py`. **UPDATE 2026-08-03: +102 same-entity cross-currency POSTED** (POL-95, book-at-payment-functional-amount, `scripts/vr2_post_xcurrency.py`; 0 double-booking/unbalanced, AP cleared, foreign face intact) and **2 pulled** (403/444 foreign-bank mis-statement, DQ-85, reversed). **GRAND TOTAL AP POSTED = 880** (2026-08-04): 706 same-currency + 102 cross-currency + 28 cross-entity same-functional (IC repointed to active 8200 net accounts, POL-93/94) + **44 IC+FX** (cross-entity+cross-currency; **POL-26** — invoice booked at its functional value [exact when already functional (20); else `fx_service` ECB/frankfurter monthly standard rate (24)], bank at actual cash, FX in the IC balance per POL-27, NOT blended rates; DQ-86). **`finance_fx_rates` BACKFILLED from ECB/frankfurter (`api.frankfurter.dev`): +1,360 rows → 2018-12..2026-07 (92 months).** **FX PRINCIPLE = POL-26**: ECB/frankfurter monthly standard rate via `fx_service`; statement-actuals override when both legs known; spread → 7100; refuses to book with no rate on file. **Remaining 74 → VR-5:** 24 foreign-invoice IC+FX + 1 USD-from-SG-bank (DEFERRED — need `finance_fx_rates` backfilled from ECB/frankfurter; USD 0 rows, coverage only 2026-H1) · 27 no-COA · 20 amount-gap · 2 foreign-bank (403/444). Earlier state: | Master = **930 MATCHED** (625 reference · 204 PAYLINE · 101 non-Retool PAYLINE, DQ-84), all payments globally unique. Engine `scripts/vr2_post_pairings.py` = batch orchestrator over the app's own approve() (bill JE) + match_transaction() (knock-off JE) + `_reopen_transaction` (void prior direct-expense JE) + post_entry (POST both). Per-txn: fresh→bill+settle; matched-draft→void draft (ledger-neutral) then bill+settle; reconciled-posted→void posted JE (real reversal, P&L-neutral) then bill+settle. **Pilot 3/3 POSTED + ledger-verified** (inv 348 AU GST+amort, 255 SG, 287 AU reversal): both JEs POSTED by system, AP nets to zero, invoice→paid, txn→RECONCILED, match→logged, priors voided. **GST rule POL-87: only AU (country=AU) posts 1350 GST Input; SG never (not GST-registered) — GST becomes expense.** **Postable now = 721** (single-entity, same-ccy, COA-resolvable; 78 are SG-with-GST → GST suppressed). **SKIPPED 206: cross-currency 155 (FX→7100, VR-5) · no-COA 27 · cross-entity 20 (intercompany 8000/8100) · amount-over 4.** Backups per run: `wip/post_backup_*.json`. **HOLD: Gaurav go required before the 721.** |
| VR-5 | Operator review queue: the ~30% residual (no confident match) — each ruled outstanding / paid-but-manual-match / void. Pre-triaged with best-candidate. Includes the 206 VR-4-skipped (cross-currency FX, cross-entity intercompany, no-COA). | ☐ |
| VR-6 | Statement of Account per vendor (opening + invoices − payments = outstanding); reconcile Σ = GL 2000 AP to the cent; export for ops. | ☐ |
| VR-7 | **Productize the posting engine into the product** (Gaurav 2026-08-03). The current `scripts/vr2_post_pairings.py` is a batch script, not an in-app service. Wire the reverse-knockoff+approve+post pipeline into a proper finance-api service method. **GOVERNANCE (POL-90): posting stays OUT of the console — NO "Post" button.** The console may only create/detach PROVISIONAL pairings (already the case); flipping a pairing to `logged`/creating JEs is a controlled TERMINAL operation only. Carry the GST rule (POL-87), per-expense payable (POL-89), cross-entity/FX, and the going-forward paid-then-paired lifecycle (POL-88) + ordering rule (POL-91) into the productized version. Steady-state posting is driven by the payout module (settlement) + categorization (reconcile), not manual console posts. | ☐ |
| VR-8 | **Wire the VendorInvoiceReconciliation matching skill into the product** (Gaurav 2026-08-03). The PAYLINE/VENDORLINK matching that produced the 930 (currently scripts + a skill) should become an in-product reconciliation feature, not one-off scripts. | ☐ |
| VR-9 | **PAYEE-based invoice ingestion + aliases into the invoice LLM matcher** (Gaurav 2026-08-04, POL-96/97). Two code changes to `vendor_matching_service`/vision extraction: (1) feed `cp.aliases` into the `llm_match_vendor` prompt (`id: name (aka: aliases) [type]`) so invoice ingestion recognises merged names (Hollard→Insuret, DEFT→Penguin) like the categorization engine already does; (2) add a `payee`/`remit_to` field to the vision extraction and key the counterparty on the PAYEE (who we remit to), not the biller — so a Penguin premium invoice → Penguin (5035) and a Penguin/claims excess → the underwriter (5036). TEST on one Penguin premium + one Penguin/excess invoice. Insurance consolidation (POL-98) already applied (id+name repointed). | ☐ |
| VR-10 | **Insurance open-invoice recovery** (2026-08-04). Insurance had 83 open after consolidation. Diagnosed into 4 flows: (a) **GT excess** 45 open — round amounts, no invoice#, +$1 surcharge, many identical, so reference-impossible & amount-ambiguous (DQ-87); resolved by **fungible FIFO pairing** (POL-99): **32 provisional paired** (`created_by=gt_excess_fifo`, backup `wip/gt_excess_fifo_20260804_062106.json`), 8 no-payment, **5 stub-dated (1900-01-01) UNFIXABLE — no recoverable date, stay open**. (b) **2 reference-confirmed paired** (inv1623→Penguin #225890 in BPAY ref; inv1636→Insuret FLE0015945; backup `wip/insurance_ref_pair_20260804_061411.json`). (c) inv552 Penguin 82,274.34 amount-only — HELD for eyeball. (d) inv678 rejected (would steal inv1636's payment). **Golden rule enforced throughout** (never reuse an already-matched payment; whole-table check + write-time re-assert; 0 double-use verified). Remaining insurance open: Penguin batch/catch-up (e.g. $174k "May Dec 25" = one BPAY over many months → batch-sum), Income 13 + Tokio 10 amount-gap (batch-sum), 5 GT stub-dated. | 🔄 |
| VR-11 | **Provisional posting sweep — 126 POSTED 2026-08-04** (Gaurav "post the paired now"). New engine `scripts/vr2_post_provisional.py` (provisional-table driven, not the MASTER CSV; preserves each pair's original `source`). Of 185 provisional: **126 posted** [108 first pass + 18 after excluding dups], provisional 185→**59**, logged 880→**1006**. **VERIFIED on live ledger:** 0 invoices/txns double-booked, 0 unbalanced JEs (252 posted JEs = 126×2), **0 currency-mismatched lines**, all →paid, AP nets ≤1%. Backups `wip/post_backup_provisional_2026080​4_*.json` (reversible via `vr2_reverse_posting.py`; pilot round-trip proven on inv444/1934/342). **TWO SAFETY GUARDS ADDED THIS RUN:** (1) **functional-currency guard (DQ-85)** — the same-ccy engine now also requires `invoice.currency == entity.base_currency`; caught inv444 (10k USD in AUD entity booked as 10k AUD fx=1) which passed "same-currency" but is really cross-functional → reversed, routed to FX engine. (2) **pdf-hash dup guard (DQ-89)** — `EXCLUDE_INVOICES` holds the 4 duplicate-doc invoices (888/2406/1311/1036). **Remaining 59 provisional** = 29 no-COA (stamp then post, GT/insurance via POL-86) · 18 amount-gap>1% (partial/batch, review) · 4 pdf-dups (VR-12) · 4 cross-currency (`vr2_post_xcurrency`) · 2 foreign-functional (FX engine) · 2 cross-entity (`vr2_post_crossentity`). **UPDATE 2026-08-04 (COA-stamp sweep, Gaurav "post the most"):** stamped COA on 21 no-COA provisionals from **txn.coa_account_code preferred, else vendor default** (`scripts` `/tmp/coastamp` logic; COA accounts are GLOBAL entity_id=NULL; backup `wip/coa_stamp_provisional_20260804_132647.json`), then posted the 9 that cleared all gates (insurance/incidentals; backup `post_backup_provisional_20260804_132701.json`). **Session total: 135 posted** (126 + 9); logged 880→**1015**, provisional 59→**50**. Also: 73 stub-dates backfilled from `retool_ref.created_at` (`wip/stubdate_backfill_20260804_132127.json`; 131 remain with no retool date); Gaurav voided the 30 amountless-junk invoices via UI. **Remaining 50 provisional** = 4 pdf-dup (VR-12) · ~18 amount-gap · 8 cross-ccy/entity/functional (FX/IC engines) · ~20 no-COA-no-source (no txn/vendor COA anywhere → need classification: Sydney Transport, Bytchenkov towing, individuals). Integrity re-verified after every tranche: 0 double-book, 0 unbalanced, 0 currency-mismatch. | 🔄 |
| VR-12 | **pdf_content_hash duplicate cleanup** (DQ-89) — 46 hash groups / 131 invoices are the same source PDF ingested 2+ times, sometimes at different amounts (inv881=300 vs inv2406=2000). Per group: decide true-dup (VOID one) vs coincidental-shared-doc (fix the hash). 4 block the provisional-postable set today (888/2406/1311/1036); 42 groups are broader cleanup. | ☐ |

**Data on hand (per §2.2.1):** OCBC-1001 Feb-2020→Dec-2024 + Jan-2025→now to supply · OCBC-3001 most 2023–24 + gaps · DBS Jun-2022→Oct-2025 · CBA Sep-2022→Sep-2025 + recent · Stripe fully syncable. Back-load depth = O-5 (FINANCIAL_LEDGER_PLAN). QB GL has zero gaps (the answer key).

---

## 2.8 Vendor Payout Module — Wise-initiated, invoice-anchored payouts (MEGA-TASK, Gaurav 2026-08-03)

PRD: `wip/VENDOR_PAYOUT_MECHANISM_PRD.md`. Branch `260803_finance_payout_mech`. Pay approved invoices from Wise; payment auto-pairs + auto-posts the knock-off on the next bank import (deterministic on `wise_transfer_id`, no re-run, no parallel txn). Dedicated `finance.payouts` tab/module. Facts: ENT-21 (Wise profiles per entity), POL-87 (SCA = RSA key signing).

| ID | Item | State |
|----|------|-------|
| VP-1 | Data model — `finance_payout_bank_accounts`, `finance_vendor_payouts`, `finance_vendor_payout_events` + `finance_transactions.wise_transfer_id` (tables live) | ✅ |
| VP-2 | Wise payout client (quote→recipient→transfer→SCA-signed fund) in `wise_service` | ✅ |
| VP-3 | `payout_service` — guards (approved-only, entity-match, threshold SGD 1,000 SGD-normalized, dedup), state machine, append-only audit events, dry-run gate | ✅ |
| VP-4 | Routes `/api/finance/payouts/*` + BFF proxies (actor headers forwarded) | ✅ |
| VP-5 | Deterministic import-pair hook (importer matches `wise_transfer_id` → pair + post knock-off) | ✅ (untested on a real import) |
| VP-6 | Professional Payouts tab (pick invoice → entity auto-locks → source Wise account → pay; payouts list; audit drawer) | ✅ |
| VP-7 | SCA keypair generated (`~/.wise_sca`), signing wired + tested. **Blocked on Gaurav: upload PUBLIC key to Wise (2FA).** | 🔄 |
| VP-8 | Dedicated `finance.payouts` gate. ✅ DONE 2026-08-04 (= B1/B2/B6): module in canonical registry; Payouts tab `requiredModule='finance.payouts'`; routes gated (read=view · write=raise/cancel · admin=approve → maker-checker). Grants to real people = B4. | ✅ |
| VP-9 | Arm live: flip `PAYOUT_DRY_RUN=0`, confirm token transfer-scope, send real $1 SGD to Dirk-Jan (recipient 297347886 confirmed, invoice 2487) | 🔄 (awaits VP-7) |
| VP-10 | v2 deferred: cross-entity (intercompany 8xxx), cross-currency (FX→7100), payment-confirmation email (entity sender), maker-checker UI polish | ☐ |

## 2.9 Invoice Ingestion — minimal-friction pipeline (MEGA-TASK, Gaurav 2026-08-03)

One capture surface only: the existing **upload** system (no email intake). Every invoice → **draft**. Design is largely EXISTING behavior + one new gate. Business rules confirmed by Gaurav 2026-08-03.

| ID | Item | State |
|----|------|-------|
| II-1 | Upload → draft; duplicates allowed at draft, **flagged at ingestion** (dedup service runs) | ✅ exists |
| II-2 | Duplicate-of-existing **hard-blocked from draft→pending** ("first one wins", exact number+amount) — per POL 2026-08-01 | ✅ exists |
| II-3 | Draft→pending is a **manual** human push (submit); pending→approved manual | ✅ exists (auto-approve deliberately OFF, submit L1658) |
| II-4 | Extraction fields today: vendor, tax_id, invoice_number, dates, total/subtotal/tax, currency, service period, description, suggested_coa, entity_hint, confidence | ✅ exists |
| II-5 | **NEW — per-COA mandatory context fields, GATED AT UPLOAD (Gaurav 2026-08-03).** Define, per COA, which fields are REQUIRED (damage/cleaning/incidentals 502x → trip_id + intercom ticket_id; insurance excess 5036 → claim ref/rego). Gate is at CREATION, not draft→pending: **no draft is created** unless the COA's required fields are present AND **valid** — trip_id validated against TMS, ticket_id validated against Intercom (validity, not mere presence). COA chosen at upload so the form knows what to demand. Dep: TMS trip-lookup + Intercom ticket-lookup validators. | ☐ |
| II-6 | Decision needed: re-enable gated auto-approve? (reverses 2026-07-31 no-auto-approve policy). If yes, trust gate = known vendor + deterministic COA + amount<T + not-dup + high-confidence. | ☐ (Gaurav call) |
| II-7 | Approval routing moved to its own module → see **§2.10 Approval Workflow**. | → §2.10 |

---

## 2.10 Approval Workflow — COA-driven, two-step, scoped queue with AI case context (MEGA-TASK, Gaurav 2026-08-03)

PRD: `wip/APPROVAL_WORKFLOW_PRD.md`. Module 3 of 3 (Ingestion → **Approval** → Payout). COA is the anchor; reuses `finance_approval_rules` (coa_prefix + entity + amount tier + priority + action + Slack approver + escalation — EXISTS). Chain: upload(valid) → approval queue (1st→2nd sign-off) → posted → payable.

| ID | Item | State |
|----|------|-------|
| AW-1 | Extend `finance_approval_rules`: `approval_steps` (0/1/2) + `first_approver_role` + `second_approver_role` + `escalation_role` (approvers mapped to permission module) | ☐ |
| AW-2 | **LOCKED (Gaurav 2026-08-09): ONE finance-owned table + FE settings module, sheet retired.** Build `finance_coa_config` — one row per COA, columns = the `COA_APPROVAL_CONFIG_SHEET.csv` shape (threshold, approver_1, approver_2, second_approver_above, needs_trip_id, needs_intercom_id, other_required, auto_approve_ok). Both gates read it live: **door gate** (required anchors at upload/raise) + **sign-off gate** (approver routing draft→approved). Companion append-only `finance_coa_config_audit` (coa, field, old, new, who, when) + per-row history view. **Finance Settings module (admincontrols, finance-gated) = editable grid; edits behaviour by editing a row, no importer/CSV/eng ticket.** Sheet `COA_APPROVAL_CONFIG_SHEET.csv` is the seed (partly filled), then retired. **Start flat (one threshold/COA); add amount-band child rows only if a real account needs >2 tiers.** OPEN before build: approver identity = Slack ID (old `finance_approval_rules`) → **dashboard role/user** (target). Supersedes the old `finance_coa_field_requirements` split — one table, not two. | ☐ |
| AW-3 | `finance_invoice_metadata` (trip_id / intercom_ticket_id / rego / claim_ref + validation result) | ☐ |
| AW-4 | `finance_invoice_approvals` (append-only per-step sign-off log: step, approver, decision, reason, ts) | ☐ |
| AW-5 | Upload validation gate — trip_id→TMS lookup, ticket_id→Intercom lookup; invalid ⇒ no draft | ☐ (dep: TMS + Intercom validators) |
| AW-6 | Two-step chain in `submit`/approve: pending → 1st-approved → 2nd-approved → post JE; or auto | ☐ |
| AW-7 | **Scoped Approvals tab** (admincontrols) — each logged-in approver sees only their step's queue; Approve/Reject/Return | ☐ |
| AW-8 | **APPROVAL AGENT** (Gaurav 2026-08-04). Supporting-info schema per request: **trip_id (REQUIRED)** + intercom_ticket_ids + guest_id/host_id/car_plate (optional) + team remarks. TWO-SOURCE BRIDGE: NOW parse Retool free-text to populate (E2, per DQ-85); LATER team enters fields at upload. Agent then ENRICHES (trip→TMS, tickets→Intercom), SYNTHESISES a 2–4 line brief, and FLAGS risk ('Unable to charge guest', duplicate, amount-vs-ticket, missing approval). Shown to reviewer + final approver. | ☐ (dep: TMS + Intercom read) |
| AW-9 | Auto-approve dial + contract-level auto-flow (conservative start → relax per COA) | ☐ |

**Open inputs from Gaurav (PRD §9):** approver matrix (who 1st/2nd per COA/amount) · amount threshold(s) · full per-COA required-fields map · approver→permission mapping · TMS validation access · Intercom read access · notification channel · auto-approve start posture.

---

## 2.11 Employee Claims + HR enablement — the 5th component (MEGA-TASK, Gaurav 2026-08-03)

Employee claims are a SEPARATE track from vendor invoices: employee self-submits + receipt (own-scoped `finance.expenses`), approved by their **manager** (org hierarchy), reimbursed to their bank. Distinct COAs (6010-6014 claims; 6000/6003 + 5061-5063 salary). Downstream (approve → pay → JE → audit) shared with the other modules.

**DB reality (checked 2026-08-03):** `users` (116; 78 active) is the identity spine — has `manager_id` (63 populated), `google_org_unit` (all 116, Google-synced), `is_employee`/`employee_type`/`bank_account_number`/`bank_code` (all EMPTY), `slack_id`/`intercom_id`. `hr_employees` + all payroll tables = **0 rows** (scaffolded, unpopulated). The front-end **HR module EXISTS** (`hr-resources/`: onboard individual+bulk, offboard, employee tab) and its onboard form already captures **payroll entity, salary_expense_code, employee_type, bank_account_number, bank_code**.

| ID | Item | State |
|----|------|-------|
| EC-0 | **Complete the HR module = THE single front-end for the full employee record** (onboard + EDIT). **✅ CORE BUILT 2026-08-03:** finance-api `_employee_dict`/`EmployeeUpdate`/`update_employee` join+write the shared `users` row (bank/manager/is_employee) via a new lightweight `User` model — this unblocked the `hr_employees→users` FK that had silently blocked ALL onboarding. BFF PUT + compensation proxies added. admincontrols **EmployeeDetailDrawer** (Identity/Employment/Ledger/Bank/Compensation sections) wired into HREmployeeTab (row→edit). Verified: list joins name+manager, PUT writes users fields, compensation add works, tsc clean. LEFT: run onboarding for all ~78 (EC-1), manager-enrich from Google (EC-2). | ✅ core |
| EC-1 | **Run HR onboarding for all ~78 active staff** (form exists) → populates hr_employees + is_employee + bank details. HR team task. | ☐ |
| EC-2 | Add **manager** to the onboard form (or enrich `manager_id` from Google sync for the missing 53) — needed for claim approval routing | ☐ |
| EC-3 | Add **compensation capture** (gross + currency + effective_from → `hr_compensation`) — for payroll (not claims) | ☐ |
| EC-4 | Employee Claims module: self-submit surface (own-scoped) + receipt upload + manager-approval route off `users.manager_id` + reimbursement via payout rails | ☐ |
| EC-5 | Remove employee-claim/payroll COAs (6010-6014, 6000-6003, 9000, 5061-5063) from the vendor approval sheet — they route through Claims/Payroll | ☐ |
| EC-6 | ✅ DONE 2026-08-06 — **Onboarding governance (POL-104).** Start date now MANDATORY to onboard (`hr_onboarding_service._validate_and_onboard_one` rejects missing/invalid before any write; persists `users.date_of_joining`; onboard modal adds a required date field). Onboard-before-offboard enforced (POL-102) + new guard: offboard_date can't precede date_of_joining. Payee-bank routes gated behind `finance.payouts` (GET read / POST-PUT write / DELETE admin). Super-admin NOT exempt from HR (bypass stands). Verified: 4 behavioural gate tests pass (stubbed-db reject paths, no writes), finance-api imports OK, admin-bff + admincontrols tsc 0 errors. | ✅ |

**HR-team fill-list (per employee, via existing HR module):** payroll entity · salary expense COA (6000 corp / 6003 director / 5061 on-ground / 5063 support) · employee type · bank account number + code · **manager** (add) · gross comp + currency + effective date (add, for payroll).

---

## 2.11b Invoice State Machine (POL-107) — backend built 2026-08-06, NOT yet deployed

Canonical spec: `documentation/wip/INVOICES_STATE_MACHINE.md`. 10 statuses; 3 new (`reconcile`, `paired`, `needs_fix`).

| ID | Item | State |
|----|------|-------|
| ISM-1 | Enum + `reconcile`/`paired`/`needs_fix` (no DB migration — status is varchar) | ✅ code (branch `finance_work`, undeployed) |
| ISM-2 | submit() routes guardrail failures (dup/no-cp/missing) → `needs_fix` w/ reasons in `ai_extraction_raw.needs_fix`, re-submittable after fix (clears stamp) | ✅ code + 4 behavioural tests pass |
| ISM-3 | approve() re-asserts duplicate HARD-BLOCK (POL-106) | ✅ code + test |
| ISM-4 | matches: provisional pair `reconcile→paired`; unpair (last match) `paired→reconcile` | ✅ code (AST-verified) |
| ISM-5 | posting engine `vr2_post_provisional` accepts `paired` (flips `paired→paid`) | ✅ code |
| ISM-6 | FE (admincontrols branch `260802_finance_recon`): `reconcile`/`paired`/`needs_fix` badges + status filters, loud DUPLICATE banner (POL-106), needs_fix reasons, submit→needs_fix alert, action guards. **Also fixed the failed build** (my `start_date` omission on OnboardPayload type). `npm run build` green. | ✅ code (undeployed) |
| ISM-7 | ✅ DONE 2026-08-06 — supervised triage applied to prod (`scripts/triage_invoices.py`, backup `wip/triage_backup_20260806_012111.json`, reversible). Result: **614 reconcile, 50 paired, 87 needs_fix, 151 pending_approval** (150 new zilla tasks, 76 high-risk, AI-enriched cards). Tripwire ALL GREEN: reconcile+paired=664, 237 covered, **0 new JEs** (6553→6553), 150 open zilla tasks == 150 pending_approval. 12 review-arm invoices raised on submit (not-invoice/edge) → auto-routed to needs_fix. Prereq fix DQ-90 (dedup indexes exclude pre-ledger states, mig 052) applied first. | ✅ |
| ISM-9 | **Approval Agent v2 (POL-109)** — `scripts/approval_agent_v2.py`, ClickHouse-sourced (Intercom tickets/conversations + trip via `au_transactions`/`au_listings`/`au_users`), Postgres counterparty double-pay, Sonnet card = Summary + Risk flags + Confidence. Trip/ticket-led; host/guest/rego corroborate only. Proven on inv 799 (Nova Smash, full incident + trip + 5 risk flags, conf 74). `--apply` overrides `tasks.body` for the 150 (backed up `wip/approval_agent/card_backup_*.json`, idempotent/resumable). REPLACES the mistaken `_ai_contract_review` wiring. ✅ backfill DONE (150/150 v2 cards, 37 double-pay flags). ✅ WIRED ON SUBMIT: `src/services/approval_card_service.py` (server-side port using `clickhouse_client`) called by `_enter_pending_approval` — every submit now generates a v2 card (best-effort, minimal-card fallback, never blocks the task). FE card v2 + oldest-first sort deployed. LEFT: FE view/edit invoice actions on the card. | ✅ live |
| ISM-8 | Approval = a TASK (POL-108). submit()→pending_approval atomically enqueues an assigned `invoice-approval` task (the assignee gate; default zilla) with the AI contract-review attached as the card body; approve/reject/void close the task; task-dispatch reject wired. FE: clean approval card in the live `MyTasksTab`. Reuses the existing generic task system (no new tables). Behavioural test: gate creates assigned task; imports OK; both builds green. | ✅ code (undeployed) |

**Deploy gate:** ISM-6 (FE) must land before merging to main, else the FE shows "submitted" for an invoice that actually went to `needs_fix`.

## 2.11c Requests surface — going-forward daily-use (POL-110, Gaurav 2026-08-06)

Two audiences: **Accounting** (finance-only, unchanged) vs **Requests** (everyone, NEW top-level). Requests = **Raise** (host payout / guest payout / vendor invoice / my claim → same task/approval machinery) + **Track** (LIBERAL for vendor/host/guest — all requests + paid; PRIVATE for own claim). Consolidates 5 scattered items; only new backend = host/guest payout-REQUEST; claims relocate here; Bottle's paid-status feeds Track.

| ID | Item | State |
|----|------|-------|
| RS-1 | ✅ 2026-08-06 — **Host/guest paid-status lookup** (Track data). `src/services/host_payout_service.py` + `GET /api/finance/host-payouts?q=&market=&limit=` over ClickHouse `au_/sg_payout_entries` (join `{mkt}_users` for host name). Searchable by host id, trip id/number, payout id, transaction id, description; `payoutStatus='paid'` = truth. Verified: tripNumber→paid row (Ewacom $3,375), hostId→5 paid. | ✅ code |
| RS-2 | Requests **Track** tab (FE): liberal vendor/host/guest search (uses RS-1 for host/guest + counterparty history for vendors) + own-claim private | ☐ |
| RS-3 | Requests **Raise** tab (FE) + host/guest payout-REQUEST backend (the one new pipeline); vendor-invoice reuses upload, claim reuses claims | ☐ |
| RS-4 | Relocate claims from finance module → Requests | ☐ |
| RS-5 | Slack lookup agent over the same data | ☐ (later) |

### RS-3 RAISE FLOW — SPEC IN PROGRESS (defining, Gaurav 2026-08-09; DO NOT BUILD until locked)

**Principle: keep it SIMPLE — we don't have per-COA info yet, so gate ONLY where policy exists; everything else is amount + free text.** One form, request type picked first, then form adapts. All types → the EXISTING approval-task machinery (v2 card → pending_approval → assignee queue, POL-108). Every request carries a **free-text reason** (Gaurav: must-have).

- **RS-3a — Vendor invoice.**
  - **Vendor picker**; if absent → **"Request new vendor"** = create a PENDING vendor for finance to CONFIRM, never auto-create (avoids the counterparty pollution cleaned 2026-08-07/08). **LOCKED (Gaurav 2026-08-09): new-vendor approval is a FINANCE TASK — it surfaces in finance's approval workflow, all vendor details supplied by the team there. Until the vendor is finance-approved, the invoice STAYS IN DRAFT (cannot progress to approval/payment).**
  - **COA drives required ANCHORS** via a per-COA config: e.g. damage/cleaning/incidentals (502x) → **trip_id** (or **vehicle/rego** if no trip) **+ intercom ticket**; insurance excess (5036) → **claim ref / rego**. All other COAs → amount + free text only. **Config table is AW-2 `finance_coa_config` (LOCKED — one table + FE settings module, sheet retired) — NOT built yet.** Start tiny (3–4 COAs).
  - **Real-time VALIDATION** (validity, not presence): trip_id/rego → ClickHouse (`au_/sg_transactions`, listings) + show trip back (dates/guest/host); intercom ticket → Intercom. Invalid ⇒ warn/block.
  - Doc upload reuses ingestion.
- **RS-3b — Host / guest payment.** `user_id` REQUIRED (validate → show name); `trip_id` optional (validate if trip-related); amount/currency/free-text. **On host-payout approval → insert into the payout-entry sheet via an EXISTING API** ("sits in payout entry, settled via the existing rail, NOT auto-paid"). **Gaurav will provide the API + field mapping.** Guest payment → its own refund rail. **PRINCIPLE (Gaurav 2026-08-09): the interim must do EXACTLY what IMS would do on BOTH charging and payouts — same request shapes, same rails, same accounting — only TEAM-DRIVEN instead of event-driven. The manual raise is IMS's hand-crank; cutover swaps the hand for the event, nothing else.** ⇒ charging/invoice-creation mechanics (PGW-first-then-Stripe? line items?) must match IMS — captured in §2.11d.
- **RS-3c — My claim** reuses claims (RS-4 relocation).

**What EXISTS vs to-build (verified 2026-08-09):**
- ✅ **Approving-AUTHORITY table** `finance_approval_rules` (coa_account_prefix + amount_min/max + vendor_type + entity_id → approver + escalation + timeout) — BUT Slack-approver based (old engine); **modernize to role/assignee (POL-108)** for the going-forward flow.
- ☐ **COA→required-ANCHOR-fields** config (AW-2) — the "which COA demands trip/ticket/rego" table — NOT built. This is the RS-3a enabler.
- **Vendor must-have fields** (DB enforces only name/type/gst/status today): for a usable approved vendor = **name · type=vendor · entity · default COA · GST-registered + ABN/tax-reg · currency**; payee bank (separate `finance_payout_bank_accounts`) only when paying via Wise.

**DECISIONS LOCKED (Gaurav 2026-08-09):**
- (1) **Approver identity → dashboard role/user** (retire Slack IDs). YES.
- (2) **Amount tiers → keep it simple: one threshold per COA** for now (flat; add bands only if a real account needs >2).
- (3) **Config sheet → filled IN-APP once the FE Finance Settings module is live** (do not chase the CSV; ship the module with defaults, finance completes it in-app).
- (4) **Host/guest payout-entry API** — Gaurav will provide; **build assuming we have it** (define a clean insert-adapter seam; wire the real endpoint when handed over).
- (5) **New vendor → finance-approved as a finance TASK** (shows in finance approval workflow, team supplies all details there); **invoice stays in DRAFT until the vendor is approved.**

**STILL OPEN (Gaurav said WAIT / provide later):** host-payout-entry API endpoint + exact field mapping [Gaurav to hand over]; starter COA→anchor list (which 3–4 COAs demand trip/ticket/rego first) [Gaurav/finance to seed in the new module].

### RS-3 ↔ IMS ALIGNMENT (Gaurav 2026-08-09 — MUST NOT BUILD AGAINST THE GRAIN)
The host/guest payout-request bridge (RS-3b) is a **TEMPORARY** stand-in for what IMS will auto-generate. Per FINANCIAL_LEDGER_PLAN.md the interim is deliberately built to the **IMS incident shape** so cutover is a source-swap, not a rebuild. Two hard constraints Gaurav set for anything we build now:
- **Team selects INCIDENT, never COA.** The COA (and the three-party money legs) must be **derived automatically** from the incident type/sub-code — mirroring how IMS will do it. Do NOT put a COA picker in the host/guest incident-request flow. (Vendor-invoice RS-3a is different — that IS a COA-anchored AP flow and keeps its COA selection.)
- **When IMS goes live, this bridge is REMOVED.** Host/guest payout requests then auto-generate from IMS incidents. So the interim must carry the same keys (trip_id, guest_id, host_user_id, listing_id, type, sub_code) and feed the SAME incident sub-ledger projection (STEP-3), not a throwaway path.

**IMS FINDINGS (verified against `tms/tms-incidentals-service` code+docs, 2026-08-09):**
- **IMS does NOT map incident→COA. Explicitly out-of-scope** ("IMS does NOT write ledger entries… emits events consumed by an Accounting Service… does not exist yet"). **finance-api IS that Accounting Service (ledger-plan D-1).** ⇒ **the incident-type→COA map is OURS to own** — same finance-owned config pattern as AW-2. This *confirms* Gaurav's rule (team picks incident, COA derives) and tells us WHERE the mapping lives: here, not IMS.
- **Taxonomy to mirror:** IMS keys incidents on `type_code` (+ optional `sub_type_code`), ~29 codes live in `ims_incidental_type_config` (e.g. `excess_mileage`, `late_return`, `accident_damage`[sub: guest_injury/car_damage/third_party_property], `tolls`, `parking_fine`, `traffic_fine`, `cleaning`, `damage`, `host_cancellation_penalty`). **Our interim incident-type list + COA map MUST key on these exact `type_code`s** → cutover = source-swap, zero remap.
- **Money shape:** `amount_guest_minor` (int, +owes / −refund), `amount_host_delta_minor` (+credit / −debit), `amount_platform_delta_minor` (derived), `currency_code`. **Minor units** (convert to decimal at our seam per O-4).
- **State machines:** guest `paymentStatus` (NOT_REQUIRED→PENDING_CHARGE→CHARGE_IN_PROGRESS→PAID / CHARGE_FAILED→INVOICE_PENDING / REFUND_*→REFUNDED / WRITTEN_OFF); host `payoutStatus` (NOT_APPLICABLE→HOST_CREDIT_PENDING/HOST_DEBIT_PENDING→ADJUSTED; BLOCKED_PENDING_*). Mirror these enums in our obligation model (IT-1).
- **Cutover event:** `incident.approved.v1` (ref-ids + host/guest amounts) is the payout/booking trigger; `incident.payment.paid.v1` = guest knock-off; `incident.payout.credit/debit.pending.v1` = host entry create. Ref-ids only for pricing → `Pricing.GetPrice` hydration later.
- **Build status:** incident model + guest PGW leg LIVE in IMS; **host Payouts Service is a STUB (not implemented)** → host auto-payout is NOT imminent; **IMS does not yet feed finance at all** (no accounting consumer). ⇒ **the interim bridge (payout-entry sheet for host, Raise flow for guest) is genuinely needed and will run for a real while — not a throwaway few-week stopgap.**

**⇒ NEW CONFIG ARTIFACT (folds into AW-2 pattern):** `finance_incident_coa_map` — key `(type_code, sub_type_code?)` → COA for each leg (guest AR/revenue, host AP `2120`/debit, platform). Finance-owned, editable in the Finance Settings module, audited. Seeded from the IMS `ims_incidental_type_config` code list. This is the O-3 "incident chart-of-accounts map" made concrete. IMS-event consumer at cutover reads the SAME table.

**RECEIVABLES — RESOLVED to option (a), pending one sub-call (Gaurav 2026-08-09):** guest incident receivable enters via the **guest CHARGE request in the Raise flow** → books **Dr guest-AR / Cr incident-revenue** (counterparty-tagged) at approval; **PGW/Stripe settlement (`payin_id`/`payment_id`) is the knock-off** (Dr bank / Cr guest-AR). Matches IMS `amount_guest_minor>0` + paymentStatus, and IT-4/IT-9 guest leg. At cutover `incident.payment.paid.v1` replaces the manual signal. **OPEN sub-call:** does our tool **create the Stripe invoice itself** (Gaurav-lean per no-build-cost-weighting rule; true IMS behaviour) OR **capture charge + Stripe reference** while team keeps raising in Stripe (lighter, keeps a manual step). Awaiting Gaurav.

## 2.11d Charging & Invoice-Creation Flow — interim mirrors IMS (Gaurav 2026-08-09; DO NOT BUILD until "everything else" is fixed)

> Own section because it's the trickiest seam: HOW a guest charge / dunning invoice / refund / host payout is actually CREATED, so the interim team-driven flow produces the SAME artifacts IMS will (line items, PGW→Stripe order, refs) — cutover = swap hand-crank for event, zero rebuild. **Sequencing (Gaurav): fix everything else first, then build this.**

**PRINCIPLE:** interim does exactly what IMS does on charging AND payouts, team-driven. No divergence in what hits PGW/Stripe or the payout rail.

**TRACE RESOLVED (verified against `tms/tms-incidentals-service` code, 2026-08-09):**
- **CH-1 Guest charge = PGW-FIRST, confirmed** (Gaurav's recollection is right). IMS never touches Stripe directly. Path (`payment-orchestration.service.ts` `chargeGuest`): SM `PENDING_CHARGE→CHARGE_IN_PROGRESS` **then** `pgw.CreatePayment(...)` → PGW creates/settles the **Stripe** object → result fires `PAID` (P6) or `INVOICE_PENDING` (P7). PGW is the payment boundary; `source=INCIDENTS`, `provider_code="stripe"`.
- **NO LINE-ITEM EDITOR NEEDED.** The PGW `CreatePayment` request carries **ONE payin per incident** with **three aggregate amounts** — `amount_gross` (guest), `amount_host_share`, `amount_platform_share` — not itemized lines. IMS *does* hold internal `pricingSnapshot.line_items[]` tagged by `meta.leg` (`charge_guest`/`payout_host`/`refund_guest`) but **sums them by leg before PGW**. ⇒ interim UI = **single amount + incident type**, three-party split derived, no multi-line editor.
- **Incident type → PGW `business_type`** hardcoded map in `payment-orchestration.service.ts` (~L48-66): 7=LATE_FEE, 8=CLEANING, 9=DAMAGE, 10=FUEL, 11=TOLL, … (this is a SECOND mapping alongside type→COA; both keyed on the incident type).
- **CH-2 Dunning invoice = the PGW Stripe-invoice** object, created by PGW's `HandlePaymentFailed(payin_id)` on charge failure (`INVOICE_PENDING`). PGW hydrates it from the original payin (business_type + amount_gross); IMS adds no line items. **This is the exact analog of "team creates an invoice in Stripe today."**
- **CH-3 Refund = PGW `CreateRefund`** per payin, newest-first fan-out until covered; single amount each; NOT a new invoice. Sibling `IncidentRefund` rows track attempts.
- **CH-4 Host payout = Payouts Service `createPayoutAdjustment`** — single **signed** `amountMinor` (+credit/−debit) + refs + reason; **not itemized**; refs-only (Pricing hydrates). **STUBBED in IMS (not live).**
- **CH-5 RESOLVED on UI: single amount + incident type, no line-item editor.** Still OPEN: does the interim **call PGW itself** (IMS's exact rail) or keep **Stripe-direct** as the team does today + book the ledger — see decision below.

**RAIL DECISION — LOCKED (Gaurav 2026-08-09): PGW is NOT live ⇒ interim rail = STRIPE-DIRECT, behind a swappable RAIL-ADAPTER seam.** IMS's eventual rail is PGW→Stripe; PGW isn't callable yet, so the interim can't route through it. Design so the cutover is a **rail swap, nothing else**:
- **`PaymentRail` interface** (one seam) with methods matching IMS's PGW shape: `charge(incident)` (single amount + 3-party split), `issueInvoice(incident)` (dunning), `refund(incident, amount)`, `payoutAdjustment(host, signedAmount)`.
- **Interim impl = `StripeDirectRail`** — our tool calls the **Stripe API directly** (charge/invoice/refund), mirroring what PGW would do; guest knock-off on Stripe settlement webhook. Host leg → **payout-entry sheet** (IMS Payouts Service is ALSO stubbed, so the sheet stays the host rail).
- **Future impl = `PgwRail`** — swap in when PGW lands; UI, incident model, sub-ledger projection, and the raise action all UNCHANGED.
- **Invariant kept constant across the swap:** the RAISE action (amount + incident type + refs) and the LEDGER projection. Only the rail impl and the trigger (team-click → IMS-event) change at cutover.

**FINAL SUB-CONFIRM (create-vs-capture):** recommend the interim tool **CREATES the Stripe object itself** via `StripeDirectRail` (removes today's manual Stripe step; matches IMS's create-then-settle shape; cutover = pure rail swap) rather than the team creating in Stripe manually while we only capture a reference (lighter now, but keeps a manual double-entry and diverges more from IMS). Pending Gaurav's nod.

## 2.12 Finance Platform Buildout — SEQUENCED MASTER PLAN (Gaurav 2026-08-04)

Ties the 10 use cases (`wip/USE_CASE_MODULE_MAP.md`) + modules §2.8–2.11 into ONE dependency-ordered backlog. The spine is ACCESS SCOPE (own < read < write < admin), reusing the existing module-grant system. Priority 1 (Gaurav): **give access** — Phase A first. Deps in ().

**Phase A — ACCESS FOUNDATION (priority 1; gates all; today-achievable)**
| ID | Item | Dep |
|----|------|-----|
| B1 | ✅ DONE 2026-08-04 — added `finance.payouts` + `finance.payment_requests` to canonical `admin-bff/constants/modules.ts` + FE mirror (both MODULES + FINANCE_SUBMODULES lists). | — |
| B2 | Route + tab gating. ✅ **Payouts slice done** — FE Payouts tab `requiredModule='finance.payouts'`; BFF payout routes gated (`.use` read to view · `write` to raise/cancel · **`admin` to approve** = maker-checker at the gate). Admin-role bypasses (Gaurav keeps full access); non-admins restricted by grant. LEFT: gate HR/Claims/Counterparty tabs + per-module route gates as those modules land. | B1 |
| B3 | ✅ DONE 2026-08-04 — Own-scoping live on claims (`owner_user_id`; list filters to owner + manager; admin all). Self-employee view (#7) still to wire into HR. | B1 |
| B4 | ✅ DONE 2026-08-05 — **Access matrix applied** (Gaurav decisions). Full finance+HR admin: gauravs/dirkjan/zilla (all 9 finance.* + hr + payouts-approve). HR admin only: rahul (payroll+expenses+hr). Every other staff (77 GW users): finance.invoices=write (upload + vendor-status view) + finance.expenses=own (own claims). Legacy monolithic `finance` grants WIPED (8→0); replaced with precise finance.* matrix (187 rows). Roster = admin-bff `users` (Google-Workspace-synced; `is_employee` flag unreliable — only 1 set — so "employee" = all 81 non-deleted @drivelah.sg users). Same `collections-db` holds `users`/`user_permissions`. Backup `wip/user_permissions_backup_20260805_063110.json`. Verified per-user. NB: BFF gates live per-request (immediate); FE nav needs 1 re-login (JWT). Invoice level = write (edit-capable) for now, refine to upload-only later (Gaurav). | ✅ |

**Phase B — RESTRICTION + QUICK WINS (today-achievable)**
| ID | Item | Dep |
|----|------|-----|
| B5 | ✅ DONE 2026-08-04 — Counterparty restriction (#8). Categorization already existed (`type`: vendor 466 / employee 122 / investor 16 / government / bank / other). Added `exclude_types` to list + admin-gated 403 on GET/:id + statement; BFF forwards `X-CP-Restricted-Access` (admin→'1'). Verified: non-admin list hides employee+investor, GET employee→403; admin sees all. | ✅ |
| B6 | ✅ DONE 2026-08-04 — Payout behind `finance.payouts` + maker-checker (= VP-8). Payouts tab + routes gated; admin-role bypass keeps admins full. | B1,B2 |

**Phase C — EMPLOYEE CLAIMS (#5,#6)**
| ID | Item | Dep |
|----|------|-----|
| B7 | ✅ DONE 2026-08-04 — Employee Claims module: `finance_employee_claims` + service (own-scoped, manager-routed, category→COA, approve posts Dr 601x/Cr 2303) + routes + BFF (gated finance.expenses, identity forwarded) + FE Claims tab (submit + my-claims + manager approval queue). Verified: create→submit→manager-approve→JE; employee blocked from self-approve; owner/manager see it, stranger sees 0. LEFT: receipt upload + reimburse-via-payout. | ✅ coreger_id`, reimburse via payout rails | B3, HR managers |

**Phase D — APPROVAL WORKFLOW (#1) + INGESTION GATE (#2)** (multi-session; needs integrations)
| ID | Item | Dep |
|----|------|-----|
| B8a | ✅ DONE 2026-08-04 — **My Tasks queue** (company-wide inbox; Gaurav "company wide ideally"). Generic `tasks` table + `task_service` (own-scoped: assignee_user_id OR assignee_role∈caller's modules; admin all; idempotent enqueue on source_ref+type) + `/api/finance/tasks` (GET/count/:id/act, identity via X-User-Id/X-Is-Admin/X-User-Roles) + BFF proxy (forwards modules as X-User-Roles) + FE **My Tasks tab** (grouped by type, expandable body-card, typed approve/reject). Wired: claim submit→task to manager; payout≥threshold raise→task to `finance.payouts`; acting routes back into source service (re-checks its own perms) + closes task; direct approve/reject closes the linked task. Verified E2E (claim submit→manager task→approve→JE posted+task done; stranger sees 0) — test rows cleaned from prod. LEFT: invoice-approval enqueue wires in with B8. | B3 |
| B8 | Approval engine — two-step chain feeding B8a: COA-routing (invoices) + manager-routing (claims); append-only sign-off log (AW-1/4/6/7) | COA sheet, HR, B8a |
| B9 | **Approval Agent** — supporting-info schema (trip_id req + tickets + guest/host/rego + remarks); Retool-parse NOW / team-entered LATER; enrich (TMS+Intercom) + brief + risk flags (AW-8) | TMS + Intercom read |
| B10 | Upload COA-required-field gate (II-5/AW-5) — validate trip_id (TMS) + ticket_id (Intercom) at upload | TMS + Intercom validators |

**Phase E — PAYMENT REQUEST BRIDGE (#3, NEW, temporary)**
| ID | Item | Dep |
|----|------|-----|
| B11 | Payment Request module — ops raises guest/host payment → approval → payout rails; removable when TMS ships | B6, B8 |

**Phase F — PAYOUT ARM (#10 live)**
| ID | Item | Dep |
|----|------|-----|
| B12 | Arm real payout — dedicated gate live + SCA public key uploaded (Gaurav) + real send | B6, SCA key |

**No-conflict invariants:** `finance.expenses`(own) is the SINGLE own-scoped module for claims (#5,#6) AND self employee view (#7). Payout is gated ONCE (B6) then armed (B12) — no second gate. Approval (B8) is the SINGLE queue for both invoices and claims — no parallel approval systems. The counterparty `category` (B5) is the same tag that separates investor/employee accounts (#8). HR (`hr`) stays separate from User-Mgmt (permissions).

**Realistic "today":** Phase A (B1–B4) + Phase B (B5–B6) — delivers ACCESS (the priority) + the two restrictions. Phases C–F are follow-on; B9/B10 are blocked on TMS + Intercom integration (not same-day).

---

## 2.13 Compliance Overview module — ⏸ PARKED (Gaurav, 2026-08-08)

Central compliance register + overview for admin.drivelah.com (peer tab to Finance/HR). Prompted by the AFCA systemic-issue mess (missed deadline, $36.7K unpaid fees, ASIC-referred determination) — a spreadsheet can't defend the failure mode (deadlines missed / matters dropped). **Decision (Gaurav): build lean on the EXISTING task/tracker rails — a "compliance matter" is a new task TYPE, NOT a bespoke module.** Not Sheets-only (structurally can't prevent the failure), not a heavyweight build (over-solutioning).

- **CO-1** Matter schema: type (AFCA/ASIC/tax/super/BAS/dispute), authority, owner, deadline + recurrence flag, status, $ exposure, docs, next action. Reuse task assignee/due/status/audit; link to finance (fees = pay-queue invoices, settlements = payouts).
- **CO-2** Two shapes: RECURRING statutory (tax/super/BAS/ASIC-annual — cadence-driven, auto-raise next) + EVENT-DRIVEN cases (AFCA/ASIC/disputes — grow over time).
- **CO-3** Overview tab = RAG board + top-line strip (# overdue · # due-this-month · $ exposure · next critical deadline · regulator-reportable). Default NOT green — unowned/undated matters show at-risk (the AFCA gap). Board-showable for weekly Gaurav/DJ review.
- **CO-4** Non-negotiable = PROCESS: one accountable owner + weekly cadence + loud deadline alerts. Tool enforces; without it, it rots.
- **Bootstrap:** capture live obligations in a Sheet THIS WEEK (AFCA/ASIC/tax/super/BAS/R&D) so nothing's invisible, then fold into the tracker.

---

## 2.14 GST Engine — cash-basis, TWO-account cash-only (LOCKED 2026-08-13; POL-120)

> **★ THE GST MACHINE IS NOW LOCKED AS A LANE-BASED PROCESS (POL-123, Gaurav 2026-08-14) — see `GST_ENGINE.md` §0.** Ordered: (1) entity gate → (2) bank-leg gate (`finance_bank_accounts.coa_account_code`; no bank leg = accrual = no GST; direction from the bank side) → (3) branch by lane: **economic events** = contra COA flag + 1/11, no invoice/vendor; **bank/categorization** = contra flag → linked-to-invoice? (invoice `tax_amount`) : (vendor-registered? 1/11). Supersedes the four-account council model. **MACHINE FINALIZED (Gaurav, 2026-08-15, `finance_work`).** Three lanes live (`_lane_a_gst`, `_resolve_gst`, `_input_gst_reclass`). **ZERO extra markers: no COA flags beyond `gst_applicable_<mkt>`, no event set, NO schema migration** (059 deleted). **Refunds land as INPUT by design** (Gaurav ruling: box 7 net identical to reversal treatment; accepted trade-off = 1A/1B grossed up, G1 includes refunds). Bank-lane input = the VENDOR GATE alone (claim-by-default retired, vendor list fixed instead — DQ-101, sweep §2.0b). Unit 21/21 (`pr1_gst_hook_verify.py`). **Final H1 mock (`gst_h1_mock_locked.py`): box 7 net within $2,458 of trusted** — Cycle & Carriage correctly excluded (~$990) + no-counterparty payouts (~$1,392, VR-SWEEP-3) + 5011 Stripe intentionally claimed (+$291) + minor dispute-direction effects; 1A/1B each ~$39k higher by the refund-as-input design (cancels in net). **✅ PROD COA MAPPING APPLIED 2026-08-15 (supervised, VR-1c):** 1310/1330/1900/7000→f, 1200/2100/2120/4027/4028/6502→t (1300 stays t); before-state backup `documentation/wip/prod_coa_gst_backup_20260815.csv`; verified after. **✅ PROD VENDOR REGISTRATIONS APPLIED 2026-08-15 (supervised, VR-1c):** the 12 AU vendors (§2.0b VR-SWEEP-1) marked AU-GST-registered on prod (safety-checked: IDs==names, no dupes, all previously empty; backup `documentation/wip/prod_vendor_gst_backup_20260815.txt`; verified after). REMAINING BEFORE ENGINE DEPLOY: deploy engine code only. NO schema migration needed.

> **✅ PROD MIGRATED 2026-08-14 (supervised, VR-1c).** `gst_h1_prod_migrate.py` (consolidated, preview-verified, invariant-gated) carried the H1 restatement to prod (collections-db): opening park + 63 bill mutations + 482 per-txn GST + payroll (25 repoints, 6 accruals). Independently verified on prod: 1350 $200,994.70 · 2301 −$30,017 · 2302 −$15,040.65 · 2304 $0 · imbalance $0.00 · H1 base untouched (2,821) · BAS Q2 net −$14,791.60 / H1 −$19,311.01 (matches clone). Reversible = void by source (`gst_h1`/`gst_h1_opening`/`payroll_h1_restate`); backup `gst_h1_prod_migrate_backup_1786678591.json`. STILL TO DEPLOY: the CODE (BAS endpoints, classify fix, gross-booking, BFF/FE) via git/Render — changes going-forward behaviour.

> **⚠ CURRENT STATE (2026-08-14).** Gaurav pivoted off the four-account/deferred model to **two accounts, cash-only** (1350 input, 2500 output; NO 1355/2505, no deferral, no P&L/AR/AP netting). Poster = `documentation/wip/gst_h1_simple.py` (ran on clone: H1 BAS −$22,631.85 refund). **BAS REPORT ENGINE BUILT + VERIFIED (POL-120):** `report_service.get_bas()` + `GET /api/finance/reports/bas` — full ATO Activity Statement (G1/1A/1B/box7 GST; W1/W2 PAYG; 8A/8B/box9). Verified on clone to the cent: **Q2 net −$15,581.81, H1 net −$22,631.85 refund**; quarters sum to H1. Two principled 1A/1B exclusions: ATO/BAS settlements (bank↔GST-control, DQ-97) and opening parks. **Legacy-redate cleanup done on clone (DQ-98):** voided the blunt Jan-1 opening park, reposted re-dated 1350→3200 reversals so period movement reads true (P&L/AP untouched, balance identical); `documentation/wip/gst_legacy_redate.py`. **OPEN:** (a) frontend BAS view (worktree) + Interceptor verify; (b) PAYG W1/W2 near-zero — payroll postings not wired; (c) run legacy-redate + two-account restatement on PROD (supervised, VR-1c); (d) rewrite POL-119 + `GST_ENGINE.md` four→two. **SOURCE-BOOKING DONE (POL-121, 2026-08-14):** invoice approval now books GROSS with **no 1350 line** (`invoice_service.py` 3-line GST branch removed); H1's 63 bills mutated at source on the clone + the 6 in-period redate JEs voided (pre-2026 park stays); BAS unchanged. Interim: expense sits gross until the cash-time expense-side claim is built (deferred). **GST NOW POSTED PER-TRANSACTION (2026-08-14):** the 12 monthly lump JEs replaced by 494 per-source-txn GST JEs (`gst_h1_pertxn.py`, ref `GSTX-<jid>`, counterparty in the memo) — 1A/1B identical, but 2500/1350 drill natively to each source entry for accountant line-by-line proof. **PAYROLL RESTATED (POL-122, 2026-08-14):** AU payroll moved to the gross model on the clone (`gst_h1_payroll.py`) — 25 salary lines re-pointed to 2304, 6 monthly accruals; BAS **W2 now = ACE exactly** (Q1 $20,215 / Q2 $9,802), W1 our-net gross, 2301 PAYG −$30,017 + 2302 super −$15,040.65 open payables; settlements (ATO/BAS split + super BPay) not yet posted; runbook `H1_2026_AU_GST_CLEANUP.md §8`. **Everything below describes the RETIRED four-account model — pending rewrite.**

Canonical model = **`documentation/wip/GST_ENGINE.md`** (POL-119 now points there, inline mechanism removed). Principle + two-rule model (A accrual defers into 1355/2505, B cash recognises/releases into 1350/2500) + six journal cases + BAS calc + the **§7 applicability map (LOCKED 2026-08-13)**: 2×2 direction×time, each cell picks D1 invoice-tax / D2 COA-flag (host+incidental payouts = claim-by-default) / D3 vendor-gate; deposits excluded; only §8 (D3 foreign/no-cp) resolves from the Q2 sheet. Cash-basis: GST claimable/payable ONLY at cash movement; **GST never touches the P&L** (P&L always net). Q2 dry-run sheet built: `gst_q2_by_txn.csv` (988 classified cash lines) + `gst_q2_vendors_to_register.csv` (34 AU vendors); nothing posted.

**Q2 CASH RESULT (classify(), 2026-08-13):** 2500 $75,775 · 1350 $91,357 (host claimed) → **−$15,582 REFUND** / $24,179 (host gated) → $51,597 payable. Deferred @30 Jun (JE-date, booked bills): 1355 $20,602 · 2505 $49,342. Files: `gst_q2_all_lines.csv` (combined by-txn, all 4 accounts) + `gst_q2_account_summary.csv`. **PROD CONFIG FIXES (foreground+verified, from Gaurav's line-remark review):** (a) `gst_applicable_au`→FALSE on 6600 Bank Fees, 1320 Loans, 6004 Staff Health Insurance, 4028 Infringement/Fines (GST-free financial-supply errors; bank fees were excluded only by luck of the vendor gate before). (b) 3 AU vendors registered `[{country:AU}]` no-number: Quickbooks(302)/WOTSO(326)/IWG(551) → now claim. (c) engine: AP invoice NULL tax_amount + gst-applicable expense COA → gross/11, no vendor gate (invoice = substantiation), +$1.3k. OPEN: broader AU review vendors still to register; insurance/payment-fee COAs (5010/5031/5035/5036/4011/4030) → Kaveesh.

**Accounts:** 1350 GST Receivable (Input Tax, claimable) · 2500 GST Payable (Output Tax) · **1355 (new) GST Receivable – Deferred (Unpaid Purchases)** · **2505 (new) GST Payable – Deferred (Uncollected Sales)**. Single-control 2510 rejected. BAS = Δ2500(cash-collected) − Δ1350(cash-paid); deferred accounts invisible to BAS.

**ENGINE BUILT (2026-08-13):** `src/services/gst_service.classify()` — the ONE locked decision function (pure, 15-case verified), NO rules table. Reads 3 facts (entity reg, `gst_applicable_au` eligibility gate, invoice `tax_amount`/vendor registration). Routes deferred vs realized by whether the leg touches bank; refunds reverse output; deposits excluded; host claim-by-default parameterised. Payment = move (Dr deferred/Cr realized), not reversal. Partner account = verification tripwire only. **Q2 BAS PROOF generated** (`documentation/wip/gst_q2_proof.py` → `GST_Q2_2026_BAS_PROOF.md` + `gst_q2_by_txn.csv`, dry-run, no writes): Output GST $75,775; Input $89,537 (host claimed) → **net −$13,761 REFUND**, OR Input $22,358 (host gated) → **net $53,417 payable** — the $67k swing is the open host-payout policy call. **NOT YET DONE:** wire `classify()` into live posting sites (invoice_service.approve → 1355; payment → 1350; economic events) to REPLACE the inert/wrong-timing engine (DQ-25 dormant + POL-118 $245k accrual-at-approval on 1350) — staged, prod-posting change, needs supervised rollout per VR-1c rule.

- **G-1** Create accounts 1355 + 2505 (deferred input/output). Migration.
- **G-2** Restrict the COA GST tick-boxes (`gst_applicable_au/sg`, mig 057 ✅) to revenue/expense/COS/capex-asset account types only — grey out liability/equity/bank/cash/receivables; depreciation stays untickable (non-cash). FE.
- **G-3** ✅ DONE (backend) — vendor GST registration is now PER-COUNTRY (vendors are global). New JSONB col `finance_counterparties.gst_registrations` = `[{country:'AU'|'SG', registration_number}]` (mig 058, on prod). **Back-populated from invoice history**: a vendor that charged GST on an AU/SG invoice → that country added, ABN/GST no. pulled from extracted `vendor_tax_id` — **118 vendors (95 AU, 25 SG), 113 with a stored number.** Legacy `is_gst_registered`/`tax_registration_number` deprecated. Gate helper `counterparty_service.registered_in(cp, country)` + `market_for_entity(entity_id)` (3→AU, 1/2→SG). **Direct-expense gate = entity registered AND account `gst_applicable_<country>` AND `registered_in(vendor, country)`.** Invoiced purchases read GST from the invoice's own `tax_amount`. REMAINING: FE editor for per-country registrations on the counterparty modal; auto-add country when a new GST invoice lands; unknown vendors (no history) default off + reviewable.
- **G-4** STOP posting GST at `invoice_service.approve()`. Bill books: `Dr expense/asset(net) + Dr 1355 / Cr AP(gross)`. No 1350 at bill.
- **G-5** Payment hook (input): on cash-out settling AP, `Dr 1350 / Cr 1355` for the paid portion's GST (proportional on partial). Direct no-invoice payment: `Dr expense(net)+Dr 1350 / Cr Bank`.
- **G-6** Sales side: invoice raised → `Cr 2505`; cash collected → `Dr 2505 / Cr 2500`; Stripe direct → `Cr 2500`; write-off/never-collected → reverse 2505 (no GST owed). Sales deferral is REQUIRED (invoices paid late/never).
- **G-7** Reverse the $245,594 already on 1350 from old accrual postings; re-derive on the cash model.
- **G-8** BAS report = quarter's cash-basis 2500 − 1350; reconcile against the accountant's lodged BAS.

Gate: entity GST-registered (AU=0.10 / SG=null) AND account gst_applicable_<country> AND (purchases) vendor is_gst_registered.

**BUILT SO FAR (the BRAIN, verified):** `gst_service.py` (gate + 1/11), 4 accounts (1350/2500/1355/2505 on prod), per-country account flags (mig 057 + 96 AU ticked), per-country vendor `gst_registrations` (mig 058 + 118 backfilled). **NOT BUILT (the HANDS) — BUILD NOW:**
- **G-H1 POSTING HOOKS** — inject the GST line into the SAME journal at DRAFT-CREATE on BOTH routes (bank-match builder + economic-events builder); it rides the parent JE's draft→posted lifecycle (NO separate GST journal, no orphan). Rule: GST recognised at the CASH/reconciliation step, to 1350/2500 (or deferred→live flush at payment).
- **G-H2 CLEANUP / BATCH RUNNER (DOING FIRST)** — restate the existing accrual GST on 1350 (only src=`invoice_approval`, ~$245k 2023-2026; ~$29.5k is H1-2026). Two ops, **scope 2026-onward** (pre-2026 lodged/closed = opening balances): (a) **unpaid** approved invoices → GST reclass **1350 → 1355** (deferred, off BAS); (b) **PAID** approved invoices → **reverse the approval-dated 1350 GST + re-post dated at the CASH PAYMENT date** (right account, right BAS quarter — the timing fix Gaurav flagged 2026-08-13). Also the revenue "flip": output GST on AU cash collected → 2500 (revenue currently gross, $3.0M AU, 2500 only has $24k QB relics).
- **DISCIPLINE (Gaurav, 2026-08-13): ALWAYS DRY-RUN FIRST.** Every ledger-restatement op produces a per-item report (what moves where, amount, BAS quarter) shown to Gaurav BEFORE any write; then execute supervised + backed-up + before/after tripwire (VR-1c rule). Never bulk-write prod blind.
- **DECISION — QUARTER-SCOPED, NOT GLOBAL (Gaurav, 2026-08-13):** the GST cleanup runs **quarter by quarter, by CASH/payment date — NOT account-by-account across all time.** **Q2 (Apr–Jun 2026) FIRST** (BAS priority). Rationale: cash-basis GST date = the invoice↔payment pairing, and **pre-H1 pairing is still in flux** — fixing older periods now = fixing twice. **Q2 scope (by cash date, reaches back to earlier-accrued invoices only if PAID in Q2):** (input) invoice paid in Apr–Jun → GST dated to payment → 1350; still-unpaid → 1355; (output) AU cash received in Apr–Jun → 1/11 → 2500; revenue flipped net. **Q2 BAS = Δ2500(collected) − Δ1350(paid).** **PRE-Q2 (Q1 + earlier) WAITS** — its GST reconciliation happens AFTER its invoice↔payment pairing is complete, as part of the pre-April consolidation. Dry-run scratch: `documentation/wip/gst_cleanup_dryrun.json` (all-years classify: 486 lines/$245,594 → 440 re-date / 46 reclass; 270 lines/$117,765 change quarter).

---

## 2.15 PR-Review Action Set — 3 PRs (finance-api, admincontrols, admin-bff), pre-merge (Gaurav walkthrough 2026-08-14)

All 16 findings walked one-by-one with Gaurav; each locked below. Merge the 3 PRs only after the LOCKED fixes land. PRs are MERGEABLE (admin-bff #23 conflict already resolved). Sources: the GST/BAS + reports work across the three repos.

| ID | Finding | Decision (LOCKED) | Repo | State |
|----|---------|-------------------|------|-------|
| PR-1 | Payment-time GST claim was never wired into live posting — `classify()` only ran from wip scripts, so going-forward gross-booking has no cash-time GST | ✅ **DONE (2 of 3 sites, `finance_work`, undeployed).** `classify()` now drives GST at the two CASH sites: (a) `categorization_service._create_simple_entry` — replaced the retired `_should_apply_gst`/`gst_rate` split with `_resolve_gst()` (input vendor gate/DQ-99, output no-gate, override preserved); (b) `invoice_service.create_ap_payment_entries` — new `_input_gst_reclass()` claims the paid slice's input GST `Dr 1350 / Cr <expense COA>` at cash payment (POL-121), scales on partial pay. **`economic_events.project_month` GST hook REVERTED (DQ-100):** that lane is accrual-structured (revenue Cr 4xxx and cash Dr 1019 live in SEPARATE JEs), so cash-basis GST cannot be derived from one projection entry — it mis-fired at accrual and missed the cash leg. **Verified:** 12/12 decision checks pass (`documentation/wip/pr1_gst_hook_verify.py`). **⚠ OPEN (output GST on Stripe revenue):** the Stripe revenue lane flows through economic events / payout imports, NOT bank-statement categorization, so going-forward OUTPUT GST on Stripe revenue is currently uncaptured — needs a per-cash-transaction pass mirroring `gst_h1_pertxn` (input side IS covered). **⚠ FOLLOW-UP:** `tests/test_gst.py` (22 cases) encodes the RETIRED model → rewrite to `classify()`; un-runnable in SQLite harness (pre-existing `ARRAY` incompat). **BLOCKS shipping gross-booking to prod → deploy gated, VR-1c.** | finance-api | ◐ code (input done, output-on-Stripe open) |
| PR-2 | `approve_vendor` swallows failures silently | ✅ **DONE.** `except: pass` replaced — logs, rolls back only the failed submit, enqueues a `finance.invoices` follow-up task, returns `failed_invoices`. | finance-api | ✅ code |
| PR-3 | Approvals queue leaks other users' items | Admins may view others' queues; everyone else scoped to their own identity. | admin-bff | ⏳ TODO |
| PR-4 | Invoices tab vendor display | Add optional `vendor_name` to the raw-extraction type; **display already prefers the canonical counterparty name** (confirmed `InvoicesTab.tsx:414/994`). Always show the resolved canonical counterparty name. | admincontrols | ⏳ TODO |
| PR-5 | `Row`/`SectionHead` defined inside render (remount churn) | Lift both to module scope. | admincontrols | ⏳ TODO |
| PR-6 | (deferred) | **Deferred — do not fix now**, revisit later. | — | ⏸ PARKED |
| PR-7 | Download button swallows errors | Catch errors and surface a message. | admincontrols | ✅ LOCKED |
| PR-8 | JE modal hides a legitimate $0 leg (falsy check) | Fix the falsy check so a real $0 line renders. | admincontrols | ⏳ TODO |
| PR-9 | 3 admin-bff routes fall to broad `finance.ledger` gate | `/coa-config`→`finance.settings`; `/enrichment/validate`→`finance.invoices`; `/my-requests`→any finance user (already self-scoped). | admin-bff | ⏳ TODO |
| PR-10 | Dynamic SQL column/table names built by string (`gst_service`, `enrichment`) | ✅ **DONE.** `enrichment_service._mkt()` allowlist wraps all 10 `{market}`/`{mk}` table interpolations; `gst_service` col annotated (already a two-value ternary). | finance-api | ✅ code |
| PR-11 | `xlsx` lib abandoned w/ CVEs | **Keep it** (write-only, never parse); document the risk. `npm audit` will still flag. | admincontrols | ⏳ TODO |
| PR-12 | 5 prod-mutating migration scripts on `main` | **Pull them out of the PR entirely** (migration done; keep off main). | finance-api | ⏳ TODO |
| PR-13 | 2 BAS proxy routes hardcode `User-Agent` instead of shared headers | Switch to shared headers **after** confirming no auth/trace header is dropped. | admin-bff | ⏳ TODO |
| PR-14 | JE modal missing a11y (no Esc-close, focus trap, dialog role; filter labels unlinked) | Add the accessibility basics. | admincontrols | ⏳ TODO |
| PR-15 | `getJournalEntry` `console.log`s full JE data | Remove the logs. | admincontrols | ⏳ TODO |
| PR-16 | Approval-chain routes exist but unwired; trust a spoofable `X-User-Email` | **Option A — wire the routes; trust the BFF-set identity** (X-User-Email is set server-side by the authenticated BFF, never client-supplied — document this trust boundary). Scope = invoices only (economic events / recon / direct JEs never route through COA approval — verified `invoice_service.py:2032-2046`). Default approver = `zilla@drivelah.sg` via `approver_1 or _DEFAULT_APPROVER_EMAIL`. **Action:** audit `finance_coa_config` for `auto_approve_ok=true` (below-threshold bypass) or non-zilla `approver_1`; neutralize both for now so every raised invoice lands on zilla. | finance-api + admin-bff | ⏳ TODO |

**Clear fixes (one right answer each): PR-8, PR-10, PR-13, PR-14, PR-15 — locked as no-brainer hardening.**

**✅ ALL EXECUTED + PUSHED 2026-08-15.** Round 1: finance-api `e97498f` (PR-2/10/12/16 + finalized GST machine), admin-bff `4874a38` (PR-3/9/13), admincontrols `1145e98` (PR-4/5/7/8/11/14/15). PR-16 audit: prod `finance_coa_config` clean (0 auto-approve, 0 non-zilla approvers). **Round 2 (fresh 3-reviewer pass, all prior fixes verified present):** finance-api `92e3d98` — decide() audit-row-after-state (F2), coa-config changed_by header-only (F5), my-requests scoped cp fetch (F7), BAS bank set from finance_bank_accounts (F10), enqueue-failure logging (F11), minor F8/F9 + dormant-branch comments; admin-bff `d62db56` — **3 identity-forging holes closed** (approver/changed_by/uploaded_by pinned to authenticated user), BAS routes + GET metadata gated, who=email, scope=all admin-only; admincontrols `cdc9cc1` — effect guards (A1/A2/A8), stable keys (A3), aria (A5), BAS state reset on tab switch (A7). **Deferred to follow-up (logged, non-blocking):** F3 queue_for N+1 single-query rewrite, F6 raise_invoice → counterparty_service.create unification, A4 onDownload useCallback (moot until BasView memoized), A6 validateAnchors → apiClient. **✅ ALL 3 PRs MERGED TO MAIN 2026-08-15** (finance-api #26 `fdf04d2`, admin-bff #23 `46f35f4`, admincontrols #69 `5b6973a`) — the GST engine + BAS reports + fixes are on main (Render deploy of finance-api main = the engine going LIVE on prod posting). **New working branches cut from fresh origin/main in all 3 repos: `260815_finance_recon_work`** (pushed with upstream; payout-module worktrees on 260814 untouched). Deferred: PR-6.

---

## 3. Decisions

| Decision | Resolution |
|----------|-----------|
| **Payment-provider mental model** | Providers (Stripe, Grab, OCBC, Wise) = permanent bank/cash accounts; economic events (revenue/COGS) = swappable source (ClickHouse views now → TMS PGW ledger later); both post to one ledger. Frame as "provider ingestion + economic-event recognition," not "Stripe sync." (`IDEAL_STATE §1`) |
| **Stripe source = existing ClickHouse views** | Read the battle-tested views via a thin adapter; do NOT re-home view logic into Python (v3.0 dropped). Patch the `code='2'` gap narrowly. |
| **Source-adapter abstraction** | Deferred (YAGNI). Read views directly now; wrap behind a thin `EconomicEventSource` interface when the PGW ledger is real. `category_id` (finance-owned COA map, §4 F-1) keeps the swap cheap. |
| **The ledger gate** | A JE counts in reports only when `status=POSTED`. Bank/cash route: categorize → DRAFT → reconcile/approve → POSTED. Accrual/direct route (Stripe, payroll, depreciation, invoice approval, manual): POSTED on the spot. Reconciliation governs only the cash route. (`IDEAL_STATE §1`) |
| **Canonical payroll service** | `hr_payroll_service` (`/api/hr/payroll-runs`) — rich per-employee engine. The duplicate `/api/payroll/runs` endpoint was **removed**; `payroll_service` retained only for `create_payroll_payment_entries` (the categorization knock-off helper). |
| **AP knock-off** | Deterministic 3-case match (NOT AI), Phase 1.5; invoice COA wins; cross-entity → IC receivable/payable pair. Spec: `IDEAL_STATE §3`. |
| **AP credit-leg routing** (2026-07-31) | Offset liability is PURELY COA-driven (no counterparty logic — chart already segregates employee-facing accounts). `finance_accounts.offset_account_code` NOT NULL DEFAULT '2000'; stamped 6010-6014+5062→2303, 6002→2302, 6001→2300, 6000/6003/5061/5063→2304, 9000→2305 (new Income Tax Payable). Approval + payment both use resolved liability. Migrations 048+049; POL-77/78. |
| Doc structure | `documentation/` root = `STATUS.md` + `IDEAL_STATE.md` only; SYSTEM_OVERVIEW + API archived to `wip/`; diagrams in `visuals/`. (CLAUDE.md Rules 2/4) |
| Employees as counterparties | `finance_counterparties.type="employee"`; `users` table is source of truth; counterparty is a synced read-copy (§3.7.1) |
| **Counterparty dedup** (2026-07-31) | Executed Gaurav's cleanup CSV: 34 actions (17 merges, 6 deletes, 6 renames, 5 vendor→employee), 631→608, zero dangling refs. Merges repoint invoices+txns→winner, fold aliases, then delete loser. Remaining: alias enrichment (~314 with none). See DQ-65/66, task #6. |
| **Txn-matching quality** (2026-07-31) | Alias-ambiguity audit (DQ-68) → +6 duplicate merges (608→602), global alias clean (1458→687, raw statement lines stripped), and tiered L1 matcher replacing first-match-wins (POL-81, DQ-69): exact counterparty_name out-ranks description-footer substring; top-tier ties → LLM. L1 ambiguity 189→0. Invoice ingestion = LLM-always over whole book (POL-80). Historical attributions not re-run (forward safeguard). |
| Salary expense COA (Option C) | Derived from `teams` at onboarding (CS→5063, On-Ground→5061, else→6000); recalc on team change |
| Categorization: rules before defaults | Phase 4A rules win over Phase 4B `default_account_code` (§3.7) |
| **Import decoupled from categorization** (2026-05-23) | Add an `IMPORTED` transaction status (= imported, engine never run). Import gains `auto_categorize` (default True = current behaviour; False = stage as `IMPORTED`, no run). `run()` processes `IMPORTED` + `PENDING`; an `IMPORTED` txn the engine *ran on* but didn't match → `PENDING` (so `IMPORTED` strictly = untouched). Status is a string column (`native_enum=False`) → **no DB migration**. |
| **Historical reconciliation = replay both legs** (2026-05-23) | To rebuild 6 years of books: **stage bank txns (`IMPORTED`) → bulk-import + bulk-approve invoices (creates AP accruals) → run categorization on the bank txns** (AP knock-off settles them; rest categorized). Replaying invoice accrual + bank payment nets to `Dr Expense / Cr Bank`, AP → 0, no double-count. Retroactive AP + payroll knock-offs cover out-of-order arrivals. |
| **No separate historical-invoice mode** (2026-05-23) | Invoice `DRAFT` is *already* the "imported, LLM-extracted, not posted" stage (the invoice analog of `IMPORTED`). Accrual JE fires only at `APPROVED`; settlement via `record_payment` (the knock-off). So historical invoices use the standard path — just need a **bulk-approve**. |
| **Goal order: H1-26 finalisation → historical → future-forward** (2026-07-24) | H1-26 closed first as the pilot tie-out; "finalised" = TB + P&L + BS + GST summary + consolidated USD (D5). |
| **No historical payroll runs** (2026-07-24) | Historical salary/CPF lines book as DIRECT EXPENSE via party defaults (6000/5061/5063/6003, CPF→6001). Real payroll runs start **H2-2026** (D2). |
| **AP boundary = invoice availability** (2026-07-24) | Full AP leg (approve→accrual→knock-off) only for periods with invoices (~Jul-25→Jun-26); earlier history books expense-on-payment (same net P&L) (D3). |
| **H1-26 revenue via Stripe sync** (2026-07-24) | E2E-verify the views adapter vs ClickHouse first, then run Jan–Jun-26; bank settlements stay transfers (D4). |
| **Engine cadence — AMENDED (2026-07-25 evening): categorization is ALWAYS explicit** | ~~event-driven auto-run on import~~ → ALL imports/syncs (Wise, file, payouts) land as `IMPORTED`; the engine runs only on explicit trigger (API/UI/walk). `auto_categorize` defaults flipped to False everywhere (service + upload route); callers must opt IN. Month-end = review + close only. |
| **Hard period lock** (2026-07-25, Q3) | Once a period is closed, no JE in it can be created/edited/voided; corrections = adjusting JE in the open period. Build = A-9; prerequisite for the A-8 sign-off. |
| **AI always human-reviewed** (2026-07-25, Q4) | Deterministic routes (transfers/rules/defaults/knock-offs) flow to reconciliation; EVERY AI categorization routes to NEEDS_REVIEW regardless of confidence, until H1 proves accuracy — then relax by evidence. Build = A-11. |
| **Feedback loop: auto-corpus, gated tables** (2026-07-25, Q5) | Every human-approved categorization auto-appends to a live corpus (frozen corpus_v2 untouched). Rules/counterparty changes remain gated: overrides of deterministic routes collect into a periodic exception report → one-by-one walk. Reasons captured in chat land in KNOWLEDGE.md. **Interface for now = Claude Code sessions** (no chat-agent build; dashboard chat agent = Phase C). |
| **H1-first sequencing reconfirmed** (2026-07-25, Q6) | Historic replay only after the H1 pilot proves the machine. H1 opening balances = QB's 31-Dec-25 close; adjusted later via adjusting entries if the historic rebuild moves them. |
| **Stripe two-sided model confirmed** (2026-07-25, Q7) | ClickHouse views → monthly economic JEs (POSTED direct; ALL revenue/COGS recognition, incl. code='2' patch). Bank Stripe lines = payout settlements = internal transfers only, never P&L. Stripe balance = permanent cash account; rare bank→Stripe top-ups also transfers. E2E-verify views before the H1 run. |
| **4-layer system test** (2026-07-25, Q8) | (1) automated suite · (2) H1 run under the full review gate = the rules/counterparties/AI live test · (3) accounting invariants incl. the per-account monthly statement-balance tie-out (build = A-10) · (4) QB cross-check (A-8) as acceptance, adjudicated under POL-21. The H1 pilot IS the system test. |
| **Economic events are STAGED, not direct-posted** (2026-07-25) | New table `finance_economic_events` (source, event_type, period, amounts, payload → je_id): the Stripe sync stages view-derived events there, a projector posts the JEs. Every JE traces to a source row; reruns idempotent; A-5b E2E verification = diff staged rows vs ClickHouse. **This table IS the future PGW landing surface** (XSD-76 event feed writes `source='pgw'` rows into the same table — the EconomicEventSource seam materialized as data). Stripe order flipped: A-5 before A-7 reports (no revenue → reports meaningless). |
| **Depreciation source = HYBRID** (2026-07-24, D1 CLOSED) | Study done (1,478 QB D&A legs; SG monthly-complete to Mar-26; Ventures lumpy annual; **QB entries STOP 31-Mar-2026**). Agreed: **mirror QB entries** for history + Jan–Mar-26 · **we compute Apr–Jun-26** (extend run-rate) · **module owns from Jul-26** with schedules seeded from QB carrying values (remaining book value ÷ remaining life). Devices per POL-4: subscriptions = expense, purchases = capitalize+depreciate; locate the ~2.7M Sep-24 purchase at replay (FLOW-10). |
| **Only 3 real entities — "Fleet" entities are mock RMS buckets** (2026-06-01) | `Drive lah Fleet` (SG) and `Drive mate fleet` (AU) are **mock entities** created only to separate out **RMS (Rental Management Service) payments**. They are NOT independent legal entities. Fold their QuickBooks GL into the parent on import: **Drive lah Fleet → Drive lah Pte Ltd (SG)**; **Drive mate fleet → Drive lah Australia Pty Ltd (AU)**. Real entity set = **3**: Drive lah Pte Ltd (SG), Drive lah Ventures Holding (SG), Drive lah Australia Pty Ltd (AU). |
| **RMS Fleet flows: eliminate at mapping, recognize revenue once** (2026-07-10) | On RMS trips we were the **registered host**: guest's payment = revenue, recognized **once, at Stripe** (old COA had no RMS line → blended). The "host share" flowing platform → **our own connected account → our bank** ("Due to Fleet") is **our own money moving** — map as **internal transfer, never P&L** (mapping 4001 GBV-RMS to these inflows would double-count revenue). Payouts to the real car owners ("Due from Fleet") → **5001 Host Payouts – P2P RMS** (COGS). This replicates, at the mapping level, the elimination the mock-Fleet consolidation used to do ($100 rev − $80 self-payout ⊕ $80 rev − $60 owner cost ⇒ $100 rev − $60 cost). RMS is a **business line** in the new COA (4001/4003 · 5001/5003 · 2120) — no clearing accounts, no mock entity. The historical **4000 vs 4001 revenue split requires booking-level RMS data** (agreed; bank lines can't provide it). Locked in `coa_bridge.csv` (approved) — do not revisit. |

---

## 4. System Topology & Cross-Service Dependencies

### 4.0 Repos & data stores

| Repo / store | Role | Local? |
|--------------|------|--------|
| **finance-api** (this) | Python/Flask finance backend + ledger | ✅ here |
| **admincontrols** | NEW front end (finance UI) | ❌ not cloned |
| **admin-bff** | NEW middleware/BFF (proxies finance-api; the `users` table lives here) | ❌ not cloned |
| **new-monitor-api** | CURRENT front end + BFF; has finance-system branches | ✅ `../new-monitor-api` |
| **tms** (`tms-pricing-service`, `tms-trips-service`) | Pricing + trips; future **PGW ledger** economic-event source | ✅ `../tms` |
| **collections-db** (AWS RDS, ap-southeast-2) | ⭐ where the finance tables sit now — finance-api connects via `DATABASE_URL` | remote (live — **read-only**) |

> The live DB is shared/production — inspect read-only, never mutate. Dependent repos have their own finance-system branches.

### 4.1 Finance ↔ TMS obligations

Source of truth: `tms-trips-service/docs/migration/CROSS-SERVICE.md`. Finance owns:

| # | Obligation | Status |
|---|------------|--------|
| F-1 | Owns the `code → category_id` COA map (PGW/Payout store the FK) | Pending — publish map |
| F-2 | Provides the GST taxability map → seeds `ps_line_item_definitions.gst_treatment` | Pending — **single outstanding blocker for the TMS pricing lane** |
| F-3 | Migrate finance reporting to consume the TMS economic-event source; retire the raw-Stripe revenue/COGS source. **Mechanism DECIDED 2026-05-25 (TMS CROSS-SERVICE `XSD-76`): an EVENT FEED, not finance-api querying PGW's DB.** PGW + Payout **emit events** (`{event_type, payment/payin refs, actual amount, pricingId}`, transactional outbox) → finance-api's **economic-event adapter** (the `EconomicEventSource` seam) projects them into JEs (`event_type → JE template` + per-line `account`/`gst`/`earned_at` from pricing). PGW/Payout post NO JEs. Awaiting the **PGW event catalog** (TMS PGW STATUS TD-22 — every emitted event + payload + which finance-api consumes). | Not started (event-feed; XSD-76) |
| F-4 | Owns USD consolidation as a reporting layer above per-tenant ledgers (owns the FX rate) | Not started |
| F-5 | `earned_at`: trip-revenue lines = trip completion; all others = invoice creation | Locked 2026-05-21 |

> The PGW ledger is a future economic-event source slotting in behind the source-adapter seam — it does NOT replace the cash rails (Stripe/Grab stay bank accounts). Plan for Stripe + PGW sources concurrently.

### 4.2 Front-end / BFF wiring NEEDED (owned by admincontrols + admin-bff)

These finance-api endpoints work but are **not yet surfaced in the UI**. Wire each UI action → endpoint (via admin-bff, JWT):

| UI action | Endpoint |
|-----------|----------|
| Onboard roster (bulk) / one / offboard | `POST /api/hr/onboard/bulk` · `/onboard/{user_id}` · `/offboard/{user_id}` |
| Sync employees (button + cron) | `POST /api/jobs/sync-employees` |
| Set salary / add deduction rule | `POST /api/hr/employees/{id}/compensation` · `/deduction-rules` |
| Create payroll run (DRAFT) → review → submit | `POST /api/hr/payroll-runs` (dup-guarded, 400 on duplicate) · `GET .../{id}/items` · `POST .../{id}/submit` |
| **Economic Events screen** (NEW, A-5a): month picker + Stage button · staged table (event_type × amount × status × JE link) · **Post month** button (human-only) · payout-line import button | `POST /api/accounting/economic-events/stage` · `GET /api/accounting/economic-events` · `POST /api/accounting/economic-events/project` · `POST /api/accounting/economic-events/import-payouts` |

> `/api/payroll/*` was removed — `/api/hr/*` is the only payroll API. Bank import + categorization screens already exist; HR/payroll screens are the gap.

---

## 5. Module Maturity

| Module | Status |
|--------|--------|
| COA / Entities / Ledger / JE posting | ✅ Ready (high confidence) |
| Bank import (OCBC/CBA/DBS/Wise) | ✅ Ready |
| Categorization engine | ✅ Ready (hardening items in §2.2) |
| GST handling | ✅ Computation ready (no return report) |
| Counterparties + HR/Employee sync | ✅ Onboarding now creates comp/deductions |
| Invoices / AP | ⚠️ Code OK, under-tested |
| Payroll | ⚠️ Engine works; needs real salary data (§2.3) |
| Depreciation / Amortization | ⚠️ Code OK, barely tested |
| Reconciliation | ⚠️ Suggestions + confirm; no full bank-statement tie-out |
| Financial Reporting | ❌ Trial balance only (§2.1) |
| Stripe Sync | 🔁 Views adapter ready; E2E-vs-ClickHouse + `code='2'` pending (§2.4) |
| Multi-entity Consolidation | ❌ IC accounts only; nothing runs (§2.5) |
| Period Close / GST Returns | ❌ Not built (§2.5) |

---

## 6. Reference

- **Verify:** `venv/bin/python -m pytest tests/ --ignore=tests/stripe_sync -q` · `venv/bin/python -m mypy src/ --ignore-missing-imports` · Flask: `venv/bin/python -m flask --app src/app.py run --port 8081 --debug`.
- **Accounting basis:** accrual. Cash path (providers/bank) and accrual path (invoices/payroll/depreciation) reconcile via payable/clearing accounts.
- **Migrations:** Alembic 001 → 036 (`alembic upgrade head`).
- **Stashed:** `stash@{0}` = old stripe_sync WIP + tests (reference only; views-based `query_builder` is restored in-tree).
- **⭐ Live data state (`collections-db`, read-only, 2026-05-21):**
  - **LIVE with real data:** COA = 155 · entities = **3** (Ventures Holding SG, DL Singapore SG, DL Australia AU) · bank accts = 21 · categorization rules = **244** · counterparties = 278 (186 vendor / **81 employee**) · transactions = 730 · journal entries = 245 (151 DRAFT / 89 POSTED; 2020 → 2026-03).
  - **BUILT BUT EMPTY:** all `hr_*` = 0 · `finance_payroll_runs` = 0 · depreciation = 0 · tags/contracts/approval_rules = 0 · invoices = only 6.
  - **Takeaway:** core ledger + categorization run on real 2020→2026 data; HR/payroll, depreciation, AP are scaffolding barely exercised. (Code says 134 COA / 4 entities; live = 155 / 3 — minor drift.)
