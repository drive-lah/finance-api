# Progress Log
Run: 100dcf60-04d4-4df7-93ed-08e7b467ef89
Task: Build the Drive Lah Finance API - a new Python Flask microservice for financial management
Started: 2026-02-13 18:12 AEDT

## Codebase Patterns
- Python 3.14 with Flask 2.x application factory pattern
- Virtual environment at ./venv (excluded from git)
- Tests using pytest with pytest-flask plugin
- Configuration via .env file (template at .env.example)
- Project structure: src/ for source code with subdirectories (routes, services, models, utils)
- Requirements managed via requirements.txt
- Database: SQLAlchemy 2.x ORM with PostgreSQL
- Validation: Pydantic 2.x
- Database sessions: Use `get_db()` generator or `db_session()` context manager from src/database.py
- Migrations: Alembic in migrations/ directory, run with `alembic upgrade head`
- DB config supports both DATABASE_URL or individual DB_HOST/DB_PORT/DB_NAME/DB_USER/DB_PASSWORD
- Connection pooling enabled by default with QueuePool (configurable via env vars)
- Models: SQLAlchemy 2.x style with Mapped[] type hints, Base imported from src/database
- Enums: Use native_enum=False for VARCHAR storage (cross-DB compatible)
- Schemas: Pydantic 2.x with field_validator, model_config = {"from_attributes": True} for ORM compatibility
- All models have to_dict() method for JSON serialization
- Routes: Flask Blueprints in src/routes/, register in app.py with app.register_blueprint()
- Services: Business logic in src/services/ with singleton instances (e.g., entity_service)
- API responses: Use EntityResponse.model_validate(entity).model_dump() to convert ORM → JSON
- Route testing: Mock get_db in routes module with patch('src.routes.MODULE.get_db', mock_get_db)

---

## 2026-02-13 18:12 - US-001: Initialize Flask project with proper structure
- Created directory structure: src/routes, src/services, src/models, src/utils, tests
- Set up requirements.txt with Flask 2.x, SQLAlchemy 2.x, Pydantic 2.x, psycopg2-binary
- Created app.py with Flask application factory pattern
- Added health check endpoint (/health) and API base route (/api/finance)
- Created .env.example documenting required environment variables (DATABASE_URL, PORT, DEBUG)
- Set up .gitignore for Python projects
- Created README.md with setup instructions
- Added pytest configuration (pytest.ini)
- Wrote 6 tests for app initialization (all passing)
- Files changed: 13 files (268 insertions)
- Commit: ffbdad1

**Learnings:**
- Flask app factory pattern allows for flexible configuration and easier testing
- Application runs on port 8081 as specified
- Health check endpoints are useful for deployment monitoring
- Test client from Flask makes it easy to test endpoints without running server

---

## 2026-02-13 18:22 - US-002: Set up database connection and migration framework
- Created src/database.py with SQLAlchemy engine, session management, and connection pooling
- Implemented get_db() generator function that yields sessions with auto-commit/rollback
- Implemented db_session() context manager as alternative pattern
- Configured connection pooling with QueuePool (configurable pool size, overflow, timeout, recycle)
- Initialized Alembic with migrations/ directory structure
- Updated migrations/env.py to use our database configuration and model metadata
- Updated .env.example with individual DB_* variables (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD)
- Added connection pool configuration options (DB_POOL_SIZE, etc.)
- Updated README.md with comprehensive database migration commands section
- Documented all environment variables in README with table format
- Wrote 16 tests for database module covering URL construction, engine creation, session management, and connection testing
- All tests pass (22 total including existing app tests)
- mypy typecheck passes
- Commit: a25fb23

**Learnings:**
- SQLAlchemy 2.x uses declarative_base() from sqlalchemy.orm (not sqlalchemy.ext.declarative)
- Connection pooling with pool_pre_ping=True verifies connections before checkout
- Alembic env.py can dynamically set sqlalchemy.url via config.set_main_option()
- Tests can mock database URL to use SQLite for fast in-memory testing
- reset_engine() is useful for test isolation

---

## 2026-02-13 18:32 - US-003: Create finance_entities and finance_accounts models
- Created src/models/entity.py with FinanceEntity SQLAlchemy model
  - Fields: id, name, country (ISO 3166-1), base_currency (ISO 4217), status, created_at, updated_at
  - EntityStatus enum with ACTIVE, INACTIVE, SUSPENDED values
- Created src/models/account.py with FinanceAccount SQLAlchemy model
  - Fields: id, entity_id (FK), code, name, account_type, normal_balance, parent_code, is_active, timestamps
  - AccountType enum with Asset, Liability, Equity, Revenue, Expense
  - NormalBalance enum with Debit, Credit
  - Static method get_normal_balance_for_type() to derive normal balance from account type
  - Unique constraint on (entity_id, code) for account codes unique per entity
  - Index on (entity_id, parent_code) for hierarchy lookups
- Created src/models/schemas.py with Pydantic validation schemas
  - EntityCreate, EntityUpdate, EntityResponse for entities
  - AccountCreate, AccountUpdate, AccountResponse for accounts
  - Validation for ISO country codes, currency codes, alphanumeric account codes
- Updated migrations/env.py to import models for autogenerate
- Created Alembic migration 001_create_entities_and_accounts.py
- Wrote 28 tests covering enums, models, and Pydantic schemas
- All 50 tests pass, mypy typecheck passes
- Commit: 221deb1

**Learnings:**
- SQLAlchemy 2.x Mapped[] type hints work well for declarative models
- Use native_enum=False with SQLEnum for cross-database compatibility (stores as VARCHAR)
- Pydantic 2.x uses field_validator decorator (not validator) and model_config dict (not Config class)
- Tests using in-memory SQLite are fast and reliable for model validation
- Parent code is stored as string reference rather than FK for simpler hierarchical queries
- Numeric types: Use Numeric(precision=15, scale=2) for monetary amounts
- Fingerprint pattern: Use String(64) for SHA256 hashes (used for duplicate detection)
- Date vs DateTime: Use Date for transaction dates, DateTime for timestamps

---

## 2026-02-13 18:52 - US-004: Create finance_bank_accounts and finance_transactions models
- Created src/models/bank_account.py with FinanceBankAccount model
  - Fields: id, entity_id, bank_name, account_number, account_name, currency, status, timestamps
  - BankAccountStatus enum with ACTIVE, INACTIVE, CLOSED values
  - Unique constraint on (entity_id, account_number)
  - Foreign key to finance_entities with CASCADE delete
- Created src/models/transaction.py with FinanceTransaction model
  - Fields: id, bank_account_id, transaction_date, description, amount, reference_number, fingerprint, status, import_batch_id, original_csv_row, timestamps
  - TransactionStatus enum with Pending, Matched, Reconciled values
  - Unique index on fingerprint for duplicate detection
  - Amount stored as Numeric(15,2) for precision
- Updated src/models/__init__.py to export new models and enums
- Added Pydantic schemas to src/models/schemas.py
  - BankAccountCreate, BankAccountUpdate, BankAccountResponse
  - TransactionCreate, TransactionResponse
  - Currency validation with uppercase conversion
- Created Alembic migration 002_create_bank_accounts_and_transactions.py
  - Creates finance_bank_accounts table with all indexes
  - Creates finance_transactions table with fingerprint unique index and other indexes
- Wrote 24 new tests for bank account and transaction models/schemas
- All 74 tests pass, mypy typecheck passes
- Commit: afb0c61

**Learnings:**
- Fingerprint uniqueness is enforced at database level with unique index
- Transaction amounts can be positive (credits) or negative (debits)
- import_batch_id allows grouping transactions from same CSV import
- original_csv_row stored as Text for audit trail

---

## 2026-02-13 19:12 - US-005: Create finance_journal_entries and finance_journal_lines models
- Created src/models/journal_entry.py with FinanceJournalEntry model
  - Fields: id, entity_id, entry_date, description, reference_number, status, created_by, created_at, updated_at
  - JournalEntryStatus enum with Draft, Posted, Void values
  - Foreign key to finance_entities with CASCADE delete
  - Indexes on entity_id, entry_date, status, and (entity_id, reference_number)
- Created src/models/journal_line.py with FinanceJournalLine model
  - Fields: id, entry_id, account_code, debit_amount, credit_amount, description, entity_id, timestamps
  - debit_amount and credit_amount use Numeric(15,2) for precision
  - Foreign keys to both finance_journal_entries and finance_entities with CASCADE delete
  - Indexes on entry_id, (entity_id, account_code), and entity_id
  - Relationship with entry via back_populates for bidirectional access
- Updated src/models/__init__.py to export new models and enums
- Added Pydantic schemas to src/models/schemas.py
  - JournalLineCreate with account_code validation and non-negative amount constraints
  - JournalEntryCreate requiring minimum 2 lines (double-entry bookkeeping)
  - JournalEntryUpdate and response schemas
- Created Alembic migration 003_create_journal_entries_and_lines.py
  - Creates finance_journal_entries table with all indexes
  - Creates finance_journal_lines table with all indexes and foreign keys
- Wrote 22 new tests covering enum, models, and Pydantic schemas
- All 96 tests pass, mypy typecheck passes
- Commit: 69ee81e

**Learnings:**
- Journal entries and lines form the core of double-entry bookkeeping
- Lines have both entry_id and entity_id to support cross-entity queries (account balances per entity)
- Minimum 2 lines required for a journal entry ensures balanced debits/credits
- TYPE_CHECKING import pattern avoids circular imports when using forward references in relationships
- Relationship cascade="all, delete-orphan" ensures lines are deleted when entry is deleted

---

## 2026-02-13 19:57 - US-006: Build entity CRUD endpoints
- Created src/services/entity_service.py with EntityService class
  - get_all() to retrieve all entities
  - get_by_id() to retrieve entity by ID
  - create() to create new entity with duplicate name validation
  - update() to partially update entity fields
  - Business logic layer separates concerns from routes
- Created src/routes/entities.py with Flask Blueprint
  - GET /api/finance/entities - list all entities
  - POST /api/finance/entities - create new entity
  - GET /api/finance/entities/<id> - get entity by ID
  - PUT /api/finance/entities/<id> - update entity
  - Proper HTTP status codes (200, 201, 400, 404, 500)
  - Pydantic validation for request/response data
- Updated src/app.py to register entities_bp blueprint
- Wrote 14 comprehensive tests in tests/test_entities.py
  - List entities (empty and with data)
  - Create entity (success, with status, validation errors)
  - Create all three MVP entities (DL Ventures, DL SG, DL AU)
  - Get entity by ID (success and not found)
  - Update entity (success, not found, partial update)
  - All tests use in-memory SQLite with proper mocking
- All 110 tests pass (96 existing + 14 new), mypy typecheck passes
- Commit: aa06b60

**Learnings:**
- Flask blueprints allow modular route organization with url_prefix
- Service layer pattern keeps business logic separate from HTTP handling
- EntityService singleton instance provides clean service access
- Pydantic model_validate() converts SQLAlchemy models to response schemas
- model_dump() serializes Pydantic models to JSON-compatible dicts
- Testing routes requires patching get_db in the routes module, not the service
- The service receives db sessions as arguments, making it easy to test

---

## 2026-02-13 20:07 - US-007: Build chart of accounts CRUD endpoints
- Created src/services/account_service.py with AccountService class
  - get_all() with filtering by entity_id and account_type
  - get_by_id() and get_by_code() for account lookups
  - validate_parent() to check parent_code exists before creating child accounts
  - create() with hierarchy validation and duplicate code prevention
  - update() with parent_code validation on updates
  - Business logic ensures parent accounts exist before allowing children
- Created src/routes/accounts.py with Flask Blueprint
  - GET /api/finance/accounts - list all accounts (hierarchical order by code)
  - GET /api/finance/accounts?type=Asset - filter by account type
  - GET /api/finance/accounts?entity_id=1 - filter by entity
  - POST /api/finance/accounts - create account with hierarchy validation
  - GET /api/finance/accounts/<id> - get account by ID
  - PUT /api/finance/accounts/<id> - update account
  - Clean error handling for ValidationError (serializes properly to JSON)
  - Returns 400 for invalid parent_code or duplicate codes
- Updated src/app.py to register accounts_bp blueprint
- Wrote 17 comprehensive tests in tests/test_accounts.py
  - List accounts (empty, with data, filtered by type/entity)
  - Invalid filters (type and entity_id validation)
  - Create account (success, with parent, invalid parent, duplicate code, validation errors)
  - Get account by ID (success and not found)
  - Update account (success, not found, invalid parent)
  - Parent-child relationship visibility in responses
  - All tests use in-memory SQLite with proper mocking
- All 127 tests pass (110 existing + 17 new), mypy typecheck passes
- Commit: 786bdf2

**Learnings:**
- Hierarchical ordering by code (e.g., 1000, 1100, 1110) naturally sorts parent before children
- Parent validation at service layer prevents orphaned child accounts
- Pydantic ValidationError needs special handling - e.errors() must be serialized properly
- Error response pattern: serialize ValidationError.errors() into JSON-safe dict structure
- Account codes are unique per entity (enforced by unique constraint on entity_id + code)
- Query parameter filtering allows flexible account retrieval (by type or entity)

---

## 2026-02-13 20:15 - US-008: Build bank account endpoints
- Created src/services/bank_account_service.py with BankAccountService class
  - get_all() with optional filtering by entity_id
  - get_by_id() for retrieving specific bank accounts
  - validate_entity_exists() to check entity_id validity before creating bank accounts
  - create() with entity validation and raises ValueError if entity doesn't exist
  - update() for modifying existing bank accounts
  - Business logic ensures bank accounts are always linked to valid entities
- Created src/routes/bank_accounts.py with Flask Blueprint
  - GET /api/finance/bank-accounts - list all bank accounts (ordered by bank_name, account_name)
  - GET /api/finance/bank-accounts?entity_id=X - filter bank accounts by entity
  - POST /api/finance/bank-accounts - create new bank account
  - GET /api/finance/bank-accounts/<id> - get bank account by ID
  - Entity validation returns 400 error if entity_id doesn't exist
  - Proper error handling for ValidationError and ValueError
- Updated src/app.py to register bank_accounts_bp blueprint
- Wrote 11 comprehensive tests in tests/test_bank_accounts.py
  - List bank accounts (empty, with data, filtered by entity)
  - Create bank account (success, with explicit status, invalid entity, validation errors)
  - Invalid currency code validation
  - Get bank account by ID (success and not found)
  - Create all MVP bank accounts (OCBC, DBS, Wise, NAB)
  - All tests use in-memory SQLite with proper mocking
- All 138 tests pass (127 existing + 11 new), mypy typecheck passes
- Files changed: 4 files, 585 insertions
- Commit: 6f364cd

**Learnings:**
- Enum values in responses are lowercase (e.g., "active", "inactive") because that's what's stored in DB
- Entity IDs must be captured before session closes to avoid DetachedInstanceError in tests
- Route filtering with query parameters (?entity_id=X) uses request.args.get('entity_id', type=int)
- Bank account ordering by (bank_name, account_name) provides consistent, readable list ordering
- Service layer validation (validate_entity_exists) prevents FK constraint violations with better error messages

---
